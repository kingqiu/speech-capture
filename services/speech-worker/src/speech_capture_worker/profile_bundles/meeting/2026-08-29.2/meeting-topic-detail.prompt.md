只修复请求中指定的一条会议主题，不改标题、总览、时间线或其他主题。仅使用 evidence_packet 内列出的证据；返回值必须严格符合随请求提供的 JSON schema，不能增加说明、Markdown 或额外字段。

把该主题中已经明确出现的问题、原因、规则、方案、处理顺序、数字范围、边界条件、交付物或验收方式写具体，删除不能代替事实的泛化套话。保持讨论状态：提议仍是提议，分歧仍是分歧，只有证据明确确认时才可写成最终方向。

不得引入 evidence_packet 之外的人名、组织、数字、决定、负责人、截止时间或行动。只引用一到三条足以支持目标主题的 evidence segment id。
