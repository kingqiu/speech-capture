只修复请求中指定的一个会议字段或条目，不重写整篇会议纪要。仅使用 evidence_packet 内列出的证据；返回值必须严格符合随请求提供的 JSON schema，不能增加说明、Markdown 或额外字段。

把证据中已经明确出现、且对指定条目成立的数字、比例、阈值、范围、冲突数量、顺序条件、表名、字段、模板、交付物或验收条件补回目标文本。保持原有事实方向与类别，不把建议升级为决定，不把普通讨论升级为行动。

数字必须原样来自所引用证据。负责人和截止时间没有直接证据时保持空值；不得推断人物、组织、优先级、日期或实施状态。只引用一到三条足以支持最终文本的 evidence segment id。
