# Speech Capture 交接记录 · 2026-07-31 晚间

> 用途：明天恢复开发时的唯一入口
> 当前分支：`agent/worker-core`
> 最新提交：`066417a Generate deterministic backend artifact package`（已推送 `origin/agent/worker-core`）
> 测试基线：`229 passed`；Ruff、`compileall` 通过
> 工作区：干净，无未提交改动
> 远端：`https://github.com/kingqiu/speech-capture.git`

## 1. 今天完成的内容

| 提交 | 内容 |
| --- | --- |
| `d02818c` | 补齐 pyannote 实际需要的 `omegaconf` 依赖 |
| `540f621` | VAD 金标准评测支持 1–2 小时长音频（10 分钟窗口 + 2 秒边距） |
| `a3183dc` | 自动连续执行所有 ASR 块，`run-asr-all`，安全恢复 |
| `eb20616` | 合成多块音频第一轮端到端测试 |
| `7578e7c` | 匿名说话人识别后端，证据落盘、降级、幂等 |
| `3063bc3` | 真实 pyannote 模型适配器验证（60 秒真实片段识别 3 个说话人） |
| `d618760` | 内容类型判断 + 证据化信息提炼，Ollama 适配器 |
| `066417a` | 后端数据产物包：raw / transcript.md / speech-record.json / note.md + manifest |

另外完成：

- Hugging Face 登录验证，账号 `imfashionai`；
- 接受 `pyannote/segmentation`、`pyannote/segmentation-3.0`、
  `pyannote/speaker-diarization-community-1` 模型授权；
- 三个真实音频的 VAD 预检通过；
- GitHub `agent/worker-core` 已同步到 `066417a`；
- 整个仓库已做敏感信息审计：真实音频、模型缓存、报告和 token 均未进入 Git。

## 2. 真实音频 VAD 预检结果

| 文件 | 时长 | 语音时长 | 语音占比 |
| --- | ---: | ---: | ---: |
| `07-28 智能体与知识库平台建设规划.mp3` | 约 50 分钟 | 约 35 分钟 | 69.8% |
| `07-01 AI驱动的运维自动化实践.mp3` | 约 82 分钟 | 约 59 分钟 | 71.7% |
| `2026-07-24 09_36_19.wav` | 约 55 分钟 | 约 36 分钟 | 66.4% |

注意：VAD 只识别语音区间，不产生文字。以上数字不是转写结果。

## 3. 明天优先要做的事

1. 恢复开发前先跑 `uv run pytest`，确认 `229 passed`。
2. 决定是否下载 Ollama 模型：
   - `qwen3:14b` 约 9 GB（默认准确配置）
   - `qwen3:8b` 约 5 GB（低资源备用）
   - 当前磁盘可用约 62 GB，两者都可以放
3. 选择第一个真实录音跑完整流水线：
   ASR → 对齐 → 说话人识别 → Ollama 提炼 → 生成产物。
   建议先跑 `07-28`（50 分钟），先出 `transcript.md` 给用户看文字质量。
4. 真实流水线确认后，进入阶段 E：处理完成与 Vault 发布协议。

## 4. 后续阶段清单

| 阶段 | 内容 | 状态 |
| --- | --- | --- |
| 阶段 D | 后端数据产物 | 已完成 |
| 阶段 E | 处理完成与 Vault 发布（租约、原子写入、重试） | 下一步 |
| 阶段 F | 正式 Worker API 和安全（配对、授权、HTTPS） | 待开发 |
| 阶段 G | Worker Manager / macOS 后台服务 | 待开发 |
| 阶段 H | Obsidian 交互设计（设计门，用户批准前不写页面） | 待开发 |
| 阶段 I | Obsidian 插件实现 | 设计批准后 |

人工修订相关功能也尚未开始：

- 说话人改名；
- 文字纠正记录；
- 录音日期纠正；
- 只重新生成总结、不破坏证据；
- 保护 `我的补充`；
- 重生成前后对比。

## 5. 常用命令

```bash
cd services/speech-worker
uv sync --extra dev --extra diarization
uv run pytest
uv run speech-capture-worker run-asr-all --data-dir runtime/dev-worker <job_id>
uv run speech-capture-worker finalize-alignment --data-dir runtime/dev-worker <job_id>
uv run speech-capture-worker run-diarization \
  --data-dir runtime/dev-worker \
  <job_id> \
  --model-revision 84fd25912480287da0247647c3d2b4853cb3ee5d
uv run speech-capture-worker run-structuring \
  --data-dir runtime/dev-worker \
  <job_id> \
  --model qwen3:14b
uv run speech-capture-worker generate-artifacts \
  --data-dir runtime/dev-worker \
  <job_id>
```

## 6. 敏感数据处理规则

- `services/speech-worker/test-data-private/` 全部被 Git 忽略；
- 真实音频、模型缓存、评测报告只存在于本机；
- Hugging Face token 只存本机钥匙串，不写入代码、manifest、报告或 Git；
- 推送前应继续做 `git status` 和 token 模式扫描。

## 7. 明天开始时的一句话结论

后端核心已经做到“可以生成四份产物”的完整度，但还没有用真实录音跑通
ASR → 说话人 → 提炼 → 产物的完整链路。明天先跑一个真实文件，确认文字和
提炼质量，再继续发布协议。
