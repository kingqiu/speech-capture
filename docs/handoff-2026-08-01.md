# Speech Capture 交接记录 · 2026-08-01（新对话入口）

> 新对话请先完整读取本文件，然后按“下一步”继续。
> 当前分支：`agent/worker-core`
> 分支状态：Stage F 已由 `bea966e` 完成并推送；Stage G 自动化核心与独立运行时已完成并验证；
> 最新提交以 `git log -1` 显示为准
> 最新 Stage E 功能提交：`9872f20 Implement atomic Vault publication`
> 测试基线：`421 passed`；Ruff、`compileall`、CLI help、依赖锁和差异检查通过
> 今日停止点：generation `19` 会议基线保持不变；真实演讲样例已补回后半段独立案例并清除
> 历史事项假待办，项目所有者已确认网络投资三个角度无需合并，演讲类型通过人工质量门；
> 真实访谈候选 generation `17` 已完成内容修复与一致性验证，并由项目所有者人工确认通过；
> 真实单人语音备忘候选 generation `10` 已完成内容修复与一致性验证，并由项目所有者人工确认通过；
> 真实通用内容候选和其余内容类型质量门均已通过；四份用户侧 Markdown 已实现并完成真实兼容审计；
> “我的补充”终端人工区域已实现原样保护；独立文字/说话人/日期校订、只重生成总结和前后
> 差异记录均已完成，artifact schema 为 `1.6.0`；Stage E 本地发布核心已实现，Worker
> SQLite schema 为 `8`；Stage F 已完成协议、跨语言类型、认证正式 API、断点上传和有界更新流；
> Stage G 可自动执行项已完成，没有进入阶段 H 前端。
> 工作区：LaunchAgent 为 `cb5675c`，Manager 状态快照为 `f21343d`，下载空间预算为 `ba8878b`，
> 模型完整性校验为 `319021c`，激活/切换/回滚为 `9854849`，诊断包为 `9ecaefc`；独立运行时
> 的最新提交以 `git log -1` 为准；私有数据仍被忽略。

## 1. 当前进度

### 真实 `07-28` MP3（第一轮，质量一般）

- runtime：`services/speech-worker/runtime/real-20260801-c/`
- job：`job_08c08aaf134346eeaedc7c0c3bf12ddb`
- ASR：106/106 完成；可读文字稿 188 段；
- 强制对齐：188/188 完成；
- 缺口：VAD 判定 11 个无语音、178 个有语音；
- 定点重转写：12 个成功、182 个失败（多为极短/碎片化缺口）；
- 状态：`aligning`，未到 `processed`，先不处理，除非用户要求。

### 真实 `2026-07-24` WAV（第二轮，已完成）

- runtime：`services/speech-worker/runtime/real-20260801-wav/`
- job：`job_c1ab923ff495494892c02075ce2f78ed`
- ASR：116/116 完成；初始可读文字稿 162 段；
- VAD：157 个缺口中 54 个无语音、103 个有语音；
- 定点重转写：35 个成功、68 个失败，新增 35 个文字段；
- 缺口复核：54 个 `non_speech`、110 个切分后的 `inaudible` 区间；
- 说话人识别：1104 条轨迹，194 个文字段已归属，3 个 unavailable，8 位匿名说话人；
- 结构化：严格 JSON Schema 重跑成功，识别为 `meeting`；3 个批次生成 47 条
  有原文证据的候选信息，0 条无证据候选；第 17 代结果复用完整校订稿主体并增加
  “初始提议—后续修正—当前方向”结构，只保留一条有明确修正措辞的排产议题；
  没有明确确认证据的 3 条旧“决定”全部降级，历史提议不再占据核心结论；当前包含
  5 条背景与参与方、2 条核心结论、3 个主题、1 条讨论演变、5 位实质发言者观点、
  0 个已确认决定、3 个待办、2 个风险和 2 个未决问题；
- 逐字稿校订：14B 连续提炼、8B 连续补标点和清理明显重复；失败批次拆小局部重试，
  第 6 代 checkpoint 达到 197/197 个有效文字段校订完成；展示稿隐藏非语音、极短
  幻觉和短促遗漏，只保留 178 个有效发言/较长缺失段；
- 状态：`processed`，产物状态为 `partial`；
- speech id：`sp_e007a3748ee4d80c9ef6`；
- 最终产物目录：
  `runtime/real-20260801-wav/jobs/job_c1ab923ff495494892c02075ce2f78ed/artifacts/`；
- 最新结构化结果：schema `1.4.0`，原主体 prompt `2026-08-01.19`，背景纠正处理版本
  `2026-08-02.1`，checkpoint generation `19`；
- manifest SHA-256：
  `6ba6498689d2044ffbc265eddee283c8506535ff25c00a73c48c9210f4fffb03`；
- `note.md` SHA-256：
  `496714f884bd8080a467ef62453f259b2d19a97515c985621473ea364468559e`；
- `transcript.md` SHA-256：
  `e85434ed8c933bc077e3b1e5907cf755fdfa2b8b4992cafa100ee81795b3d90e`；
- `speech-record.json` SHA-256：
  `04af762411507f16077aab32402cc35505d3e65ee8061aa26543f77ba92a69d1`；
- 原始 `transcript.raw.json` SHA-256 仍为
  `45bf58083c586998b690faf8bc9bfb705d3b260b5b2c971aa25ce5c73f2575ea`。

## 2. 已解决的问题

1. 真实 ASR 返回零时长/单字时间戳 → 已按停顿、长度、句末标点合并为可读段落；
2. 强制对齐器 int16 输入导致时间退化 → 已改为 float32；
3. 对齐词时间越界 → 已夹紧到段长；
4. 稳定段只能追加、不能在中间补缺口 → 新增
   `commit_gap_retranscription_segment` 中间插入；
5. VAD 缺口与对齐缺口轻微错位 → 改为以当前对齐报告的未覆盖范围为准；
6. 部分缺口与已有段冲突导致整任务中断 → 冲突缺口记为失败并继续；
7. 100MB WAV 上传需要分块 → 驱动脚本已支持 64MB 分块和幂等续跑，已包含在
   `9d9f164`；
8. 经复核的 `inaudible` 时间轴无法继续后续阶段 → 允许保持
   `transcript_complete = false` 的 partial 语义进入说话人识别，但真实 `failed`
   仍阻断；
9. 结构化输出校验异常未走降级 → 统一安全降级为 `generic + 空结论`，不接受
   非法或无证据结论。
10. `note.md` 只有 Meta 信息 → Ollama 改为严格 JSON Schema，分批失败互不影响；
    支持对 `processed` 作业安全重做结构化和原子替换产物；最终笔记以有证据的
    一分钟总览、关键信息、行动项和分歧为主体，极短遗漏仅做压缩说明。
11. 参考软件的纪要明显优于“发现项列表” → 增加跨批次全文综合，生成连贯摘要、
    核心结论、5–8 个主题、决定、待办、风险、未决问题和时间章节；每项继续绑定
    原文证据，不以无证据长文替代逐字稿。
12. 证据块号不可跨文件点击 → 改为 Obsidian `[[transcript#^block-id|时间]]` 链接。
13. 原始 ASR 无标点且混入极短外语幻觉 → 新增忠实校订层，原文保存在
    `speech-record.json` 的 `raw_text`，展示稿使用校订 `text`；非语音及明确的微小
    幻觉不进入 `transcript.md`。
14. 全量重跑成本过高 → 新增 `--document-only` 和 `--transcript-edits-only`，分别
    支持只重做全文综合、只拆小重试失败校订批次。
15. 校订后逐字稿没有反哺 Note → 校订现在先于分类与提炼完成，分批提取和全文综合
    统一读取校订文本；全文综合接收全部有效分段，而不是只接收 findings 引用的片段。
16. 固定“5–8 条重点”导致凑数和错判 → 改为按会议目的、参与方、明确结论、跨发言者
    回应、业务影响排序；核心结论和正文数量都由证据决定，章节导航由正文证据自动派生。
17. 缺少场景模板 → 提示词移入版本化的 `note_prompt_profiles.py`，为会议、访谈、课程、
    演讲、语音备忘和通用记录分别定义提炼标准；当前 prompt 版本为 `2026-08-02.8`，已验收
    会议主体最初由 `2026-08-01.19` 生成。
18. 多人会议缺少各方观点 → 新增 `speaker_summaries`；按发言量识别实质参与者，主体
    纪要遗漏时由 8B 只对缺失者做短补全，并要求至少引用一段本人发言。
19. 事实与性能门禁 → 负责人和期限只有在引用原文中能直接找到时才保留，否则自动留空；
    决定、待办、风险和未决问题不得复制同一句；最终证据使用短别名生成后还原为真实
    segment ID。14B 只负责一次主体全文综合，分类和分批初筛改由 8B 执行，校订批次从
    2400 字扩大到 4800 字。最终真实 document-only 运行约 7–8 分钟（含缺失发言者补全），
    首轮旧配置 20 分钟超时。
20. 同一议题前后观点被并列 → schema `1.3.0` 新增 `discussion_threads`，单独记录最初
    建议、后续修正、当前方向和 confirmed/tentative/open 状态；证据按时间校验，普通补充
    说明和重复脉络不会为了凑数进入该章节。
21. 早期提议被误写成决定或核心结论 → 决定必须引用含明确确认措辞的原文；没有通过
    门禁的决定降级，摘要中的无证据确定性句子删除，证据早于首次修正的相关 highlight
    不再占据核心结论，但仍保留在议题历史中。
22. 新增大字段导致 14B JSON 不稳定且完整重写过慢 → 主体纪要、多人补全、讨论演变和
    决定核对分离；旧主体可以只升级新增结构，确定性规则更新无需重新调用模型。
23. 讨论演变证据遗漏后段专有名词 → 从完整逐字稿补入更晚的同议题证据，并保留直接
    证据中的大写专有缩写；真实样例当前方向已明确显示 APS。
24. 音频提交时无法提供背景 → Worker 新增每任务可选、自由文本、多行多段的
    `recording_context`；支持创建时读取、按 revision 保存、替换和清除。
25. 背景可能污染事实 → 所有模型提示把它标记为“不可信参考而非证据”；结构化结果仍须
    引用逐字稿，事件只保存 supplied 状态和哈希；speech-record 记录应用来源但不把背景
    正文写成决定、待办或事实。
26. 修改背景后只能耗时全量重跑 → 新增 `--context-corrections-only`，对明确确认的专名
    做有界派生文本纠正；一般背景变化可走 `--document-only` 重新阅读完整校订稿，需要连
    逐字稿一起重做时才使用 `--force`。三条路径都不覆盖原始 ASR。
27. 项目所有者已明确确认 generation `19` 的 `note.md` 输出质量很高；该版本正式成为
    后续会议、访谈、课程/演讲、语音备忘/个人笔记和通用内容的 V1 质量参照。参照的是
    清晰度、完整性、准确性和证据可追溯性，不是统一套用会议模板。

## 3. 当前问题

- `07-28` 底噪重，保守静音策略判定 0 个确定静音；
- 大量极短缺口单独重转写返回空/无效结果，自动补全不可行；
- 自动审批通道出现过两次临时故障，不是代码问题；
- WAV 已按批准策略完成；质量限制是 110 个极短/碎片化范围只能标为
  `inaudible`，因此最终状态正确保持为 `partial`；
- WAV 的新版 `note.md` 已按完整校订稿重做，补回聚衣堂、优维、王总、创新院背景，
  覆盖 5 位实质发言者，并额外提供可点击原文证据、风险、未决问题、章节导航和折叠
  质量说明；项目所有者已确认 generation `19` 的内容质量很高，并将其定为 V1 基线。
- “销售预测 → 优先排产而非销售预测单点 → AI 接管 APS 排程”的演变已经单独呈现；
  原来把两者并列为最终决定的内容已移除。
- 用户确认的公司名“聚衣堂”已通过每任务补充背景统一应用到 `transcript.md`、
  `speech-record.json` 派生文字和 `note.md`；错误派生名称计数为 0。原始 ASR 与
  `speech-record.json.raw_text` 仍保留“聚一堂”，这是刻意保留的不可变证据，不是遗漏。
- 55 分钟音频首次执行深度提炼与全量逐字校订仍需较长时间；模型调用已从逐批
  交替改为同模型连续执行，后续还应补充阶段级进度，并评估仅校订缺标点段落。

## 4. 已批准的缺口策略

- VAD 无语音缺口 → `review-gap` 标 `non_speech`；
- VAD 有语音缺口 → `retranscribe-gaps` 定点重转写；
- 重转写失败的语音缺口 → `review-gap` 标 `inaudible`；
- 之后刷新对齐，允许以 `partial` 语义进入后续阶段。

## 5. 下一步

1. 保留 generation `19` 和各内容类型已验收 Note 作为质量基线，不重复运行 ASR、全文校订、
   候选抽取或长上下文综合；
2. Stage E 真实副本验证、代码审阅、私有数据排除、commit 和 push 均已完成；不要重复执行；
3. 阶段 F Worker 端已完成；Stage G 可自动完成项均已完成并真实验证；不要重新规划或重复构建；
4. 客户端明文 token 的操作系统保护存储留在阶段 H；Stage G 不开始 Obsidian 交互设计或前端；
5. `reference/`、`runtime/`、音频、逐字稿和生成 Note 继续作为私有忽略数据，不得加入 Git。

## 6. 今日收尾状态与明日恢复边界

### 已落盘、可以直接恢复的内容

- 当前有效真实任务：`job_c1ab923ff495494892c02075ce2f78ed`；
- 当前有效结构化结果：generation `19`、schema `1.4.0`、背景处理版本
  `2026-08-02.1`；复用了 generation `17` 的已接受主体，没有重跑长上下文模型；
- 当前最终笔记：
  `services/speech-worker/runtime/real-20260801-wav/jobs/job_c1ab923ff495494892c02075ce2f78ed/artifacts/note.md`；
- 新增真实演讲候选任务：`job_02de4d59c9a940dcbf58183f533f47ad`，runtime 为
  `services/speech-worker/runtime/profile-course-speech-20260802/`；当前结构化 generation
  `10`、prompt `2026-08-02.5`、覆盖修复版本 `2026-08-02.2`、2 个抽取批次、41 条合并候选；
  已补回机柜管理及网络投资相关内容并清除历史假待办；项目所有者已确认网络投资三个角度
  无需合并，演讲类型通过人工验收；
- 真实访谈候选复用同一份已校订音频证据的独立副本，runtime 为
  `services/speech-worker/runtime/profile-interview-20260802/`，job ID 不变但人工类型为
  `interview`；当前 generation `17`、prompt `2026-08-02.6`、覆盖修复 `2026-08-02.3`、
  访谈质量复核 `2026-08-02.1`，40 条合并候选，已由项目所有者人工确认通过；
- 真实语音备忘候选是从已验收真实业务录音中截取的 165 秒连续单人规划陈述，不是专门录制的
  语音备忘；runtime 为 `services/speech-worker/runtime/profile-voice-memo-20260802/`，job 为
  `job_9fccb7ca84d2497486c19b87bb2a6931`；人工类型为 `voice_memo`，当前 generation `10`、
  prompt `2026-08-02.7`、覆盖修复 `2026-08-02.4`、语音备忘质量复核 `2026-08-02.2`、
  19 条合并候选、0 条无证据候选，已由项目所有者人工确认通过；
- 语音备忘最终 Note：
  `services/speech-worker/runtime/profile-voice-memo-20260802/jobs/job_9fccb7ca84d2497486c19b87bb2a6931/artifacts/note.md`；
  当前 manifest SHA-256 为
  `dd4d7fb7b6ba4d31623e551bb1fdc45a9cf6900c86eccb43f2c74b9f7b1c0f02`；
- 真实通用候选是从 `07-28` 真实业务交流中截取的 129 秒连续说明片段，不是独立录制文稿；
  runtime 为 `services/speech-worker/runtime/profile-generic-real-20260802/`，job 为
  `job_9e7e11fec1664519b1b1678b991fe334`；自动分类和人工覆盖均为 `generic`。项目所有者随后
  同意继续执行四份 Markdown 契约，通用质量门据此作为已通过基线；当前 generation `11`、
  prompt `2026-08-02.9`、7 条合并候选、0 条无证据候选；
- 通用候选最终 Note：
  `services/speech-worker/runtime/profile-generic-real-20260802/jobs/job_9e7e11fec1664519b1b1678b991fe334/artifacts/note.md`；
  当前 manifest SHA-256 为
  `7d52d3677fa8b26ef5704134fcf55e581dacab289f53e195ca483cc4ceae2a0b`；
- 已完成并保存在工作区的核心改动：场景化提示词、完整逐字稿反哺 Note、多人观点、
  讨论演变、决定证据门禁、逐字稿校订、确定性后处理和相关测试；
- Worker 端“每任务可选、自由文本补充背景”的数据契约、revision 存储、模型传递、
  增量专名纠正、开发者命令和测试均已实现；前端输入框仍留到阶段 H 设计批准后实现；
- 四份用户侧 Markdown 已在 Worker 落地：纯净 `note.md`、顺序摘要 `timeline.md`、完整校订
  `transcript.md`、审计用 `note.evidence.md`；另有两个机器记录和 manifest；
- 当前测试基线为 `421 passed`，Ruff、`compileall`、CLI help、依赖锁和 `git diff --check` 均通过；
- Stage D、E、F 及 Stage G 可自动完成的代码和文档均已提交；本地 `reference/`、`runtime/`、
  `dist/`、`build/`、音频、逐字稿和生成 Note 均为私有或生成数据，继续由 Git 忽略，不得加入 Git。

### 明天严格从这里开始

1. 先完整读取本文件和 `docs/development-status-and-next-steps.md` 的最新计划，不重新制定
   一套顺序；
2. generation `19` 已通过用户质量验收，直接把它作为基线，不重复执行耗时的 14B/8B
   全量提炼；
3. 独立案例覆盖修复和行动时态门禁已经实现，当前真实演讲 Note 已通过人工验收，网络投资
   三个角度无需合并；
4. 访谈 generation `17`、语音备忘 generation `10` 和通用代表样例均已通过质量门；已验收
   任务不再重跑 ASR、校订或候选抽取；
5. 每种类型仍须达到会议基线，不能用结构完整或合成测试代替真实内容人工验收；
6. 新背景若只是明确专名纠正，继续用增量路径；若改变内容语境和摘要理解，才使用
   `--document-only`；只有必须重新校订整篇逐字稿时才使用 `--force`；
7. 跨类型质量门、四份 Markdown Worker 契约、`我的补充` 保护、独立文字/说话人/日期校订、
   只重生成总结和前后差异记录均已落实；Stage D 已提交并推送；
8. 若还需性能优化，先补充阶段级进度反馈和增量重跑能力，避免无必要地连续重跑两个
   长上下文模型。
9. Stage E、Stage F 已完成；Stage G 除冷重启/logout-login 人工验收和跨 Mac 发布签名外均完成。
   明天先确认是否进入 Stage H；Stage H 只做 Obsidian 交互研究、设计和用户评审，不编写前端。
   未经主要流程、状态、术语和视觉方向明确批准，不得进入 Stage I 插件实现。

### 新对话可直接复制的开场说明

```text
请先完整读取：
/Users/azhua/Downloads/Projects/speech-to-notes-obsidian-plugins/docs/handoff-2026-08-01.md

然后严格按照文档里的“下一步”和“明天严格从这里开始”继续，不要重新规划或重复 Stage G。
先说明 Stage G 已完成并停在 Stage H 边界，等待我明确确认是否进入 Stage H。保留 generation 19
会议 Note 及演讲、访谈、语音备忘、通用类型的已验收
质量基线，不重复运行 ASR 或长上下文提炼；原始 ASR 保持不变。Stage E 已完成。Stage F 已完成
版本协议、正式认证 API、持久设备配对、Vault 授权、设备管理、两阶段凭据轮换、HTTPS、安全
网络方案和脱敏审计。Stage G 自动化核心及独立运行时已经完成；保留本地已验收质量基线，不重复
模型哈希或构建。下一步停在 Stage H 边界，未经项目所有者确认不进入 Obsidian 前端。
reference、runtime、音频、逐字稿和生成 Note 都是私有数据，不得加入 Git。
```

## 7. 常用命令

```bash
cd services/speech-worker
uv run pytest

# 真实任务续跑
.venv/bin/python scripts/run_real_pipeline.py \
  --audio "test-data-private/vad/audio/2026-07-24 09_36_19.wav" \
  --data-dir runtime/real-20260801-wav

# 缺口流程
.venv/bin/speech-capture-worker retranscribe-gaps \
  --data-dir runtime/real-20260801-wav \
  job_c1ab923ff495494892c02075ce2f78ed

# 只重做全文综合，不重复转写/校订
.venv/bin/speech-capture-worker run-structuring \
  --data-dir runtime/real-20260801-wav \
  --model qwen3:14b --editor-model qwen3:8b --document-only \
  job_c1ab923ff495494892c02075ce2f78ed

# 只拆小重试失败的逐字稿校订批次
.venv/bin/speech-capture-worker run-structuring \
  --data-dir runtime/real-20260801-wav \
  --model qwen3:14b --editor-model qwen3:8b --transcript-edits-only \
  job_c1ab923ff495494892c02075ce2f78ed

# 保存或修改每任务自由文本背景（revision 必须来自当前任务快照）
.venv/bin/speech-capture-worker set-recording-context \
  --data-dir runtime/real-20260801-wav --expected-revision 11 \
  --context-file runtime/real-20260801-wav/recording-context.txt \
  job_c1ab923ff495494892c02075ce2f78ed

# 只应用背景中明确确认的专名纠正，不调用 ASR 或长上下文模型
.venv/bin/speech-capture-worker run-structuring \
  --data-dir runtime/real-20260801-wav --context-corrections-only \
  job_c1ab923ff495494892c02075ce2f78ed

# 追加一条派生文字校订；before 必须是当前 transcript.md 对应段的完整文字
.venv/bin/speech-capture-worker add-correction \
  --data-dir runtime/real-20260801-wav \
  --field transcript_text --target-id seg_00000001 \
  --before "当前校订文字" --after "人工纠正后的文字" \
  --author user --idempotency-key correction-001 --expected-revision 20 \
  job_c1ab923ff495494892c02075ce2f78ed

# 校订影响摘要时，只重生成全文总结；随后查看前后差异
.venv/bin/speech-capture-worker run-structuring \
  --data-dir runtime/real-20260801-wav \
  --model qwen3:14b --editor-model qwen3:8b --document-only \
  job_c1ab923ff495494892c02075ce2f78ed
.venv/bin/speech-capture-worker summary-revisions \
  --data-dir runtime/real-20260801-wav \
  job_c1ab923ff495494892c02075ce2f78ed
```

## 8. 敏感数据

- 真实音频、模型缓存、runtime 产物全部在本机 Git 忽略目录；
- Hugging Face token 只存钥匙串；
- 提交前检查 `git status` 和 token 模式。

## 9. 2026-08-02 真实演讲验证补充

- Worker 已支持每任务内容类型人工指定、清除和 revision 冲突保护；自动类型与人工类型
  分别保存在结构化证据和 `speech-record.json.content`；
- 更改类型后会复用校订逐字稿，但重新执行类型相关候选提取，再生成最终 Note；不会重新执行
  ASR、时间对齐、说话人识别或逐字稿润色；
- 类型提取配置已经版本化；当前为 prompt `2026-08-02.6`、每批最多 4000 字。真实样例从
  原来的 1 批拆为 2 批，解决候选达到 30 条上限后只覆盖前 9 分钟的问题；
- 全文综合同时读取完整校订逐字稿和候选索引；候选只用于查漏，所有最终内容仍须引用逐字稿；
- 无证据的 speaker 姓名、单位、角色会自动清空；演讲待办还要求与独立行动候选证据重合；
- `document-only` 综合失败时会持久保存已完成的重新抽取，下一次只重试最终综合；本轮确实
  遇到一次 14B 坏 JSON，并验证修复后的后续运行可恢复；
- VAD 和 diarization 对检测器尾部最多 50ms 的舍入误差作安全夹紧，真实样例分别验证了
  约 15ms 和 25ms 的尾部越界；超过 50ms 仍拒绝。

真实演讲候选样例：

- 音频为本机私有 `07-01 AI驱动的运维自动化实践.mp3` 的前 15 分钟摘录；runtime 与音频
  均被 Git 忽略；
- job `job_02de4d59c9a940dcbf58183f533f47ad`，人工类型 `speech`，原自动类型 `meeting`；
- 32 次原始 ASR 不变，最后创建时间为 `2026-08-02T02:12:06.522890+00:00`；
- 63 个 transcribed、44 个 inaudible、4 个 non_speech；时间轴已完全交代但文字不完整，
  因此 status 正确为 `partial`；2 位匿名 speaker；
- 主综合基线为 generation `6`，2 个批次覆盖 `seg_00000001` 至 `seg_00000068`，合并 41 条
  候选；独立案例修复与确定性时态重校验后当前为 generation `10`，没有重跑 ASR、候选提取
  或 14B 主综合；
- 最新 manifest：`e1efb0d3d63d7b46b91a8106bb5f2b02c27e4cd251abf3e0525adbd06bbdd22d`；
- 三份产物一致性通过：Note 证据链接全部能落到 transcript；展示逐字稿块均存在于
  speech-record；speech-record 的 `raw_text` 与数据库稳定段逐条一致；
- 当前质量结论已通过 generation 19 同等级人工质量门：机柜管理已经补回，网络投资内容
  已进入正文，3 条历史假待办已全部清除；项目所有者明确确认网络投资三个相邻角度无需合并，
  当前结构按此保留。

## 10. 2026-08-02 真实访谈验证补充

- 没有新增外部私有录音；从已验收演讲素材中确认 5 分 32 秒后存在持续双人追问与回答，复制
  已校订、已对齐、已说话人识别的本地持久化状态到独立 Git 忽略 runtime，不改变演讲基线；
- runtime：`services/speech-worker/runtime/profile-interview-20260802/`；job：
  `job_02de4d59c9a940dcbf58183f533f47ad`；人工类型为 `interview`，自由文本背景明确要求区分
  来访者与负责人、匿名即可、不得猜姓名单位、结论必须回到完整逐字稿；
- 首版 generation `11` 虽有 37 条证据候选，但正文是类型标签式空泛重复，且把 3 个已完成
  项目写成待办、把当场已回答问题写成待追问，因此判定不及格；
- prompt `2026-08-02.6` 明确要求按实际问题或案例成章、问题与回答对应、同证据不得复制为
  观点/依据/案例、口头停顿不是分歧、历史项目不是后续事项；
- 一次长批次坏 JSON 暴露候选整批丢失风险；Worker 现在会拆成两个较小批次重试，并在后续
  `document-only` 中只重试失败批次。generation `12` 的持久化失败点由此恢复为 40 条候选；
- 多人访谈的 speaker summary 会先检查是否至少引用本人发言，证据归属不符时删除并针对缺失
  speaker 单独补写；失败候选文档和通用校验原因只保存在私有证据中，便于增量恢复；
- 新增访谈 8B 专用质量复核版本 `2026-08-02.1`，只编辑 14B 已综合文档，检查问答对应、
  跨案例证据、历史行动、已回答问题和过度推断；同版本后续复验不再调用模型；
- 确定性后处理继续清除无未来证据的 actions、无明确约束证据的 risks、无原文支撑的“未来
  规划/探索阶段/需优化验证”，并删除远处其他案例嫁接的上线、安全扫描等状态；
- 当前 generation `17`，40 条候选、0 条无证据候选、8 个正文场景、2 位匿名参与者观点、
  0 个待办、0 个风险、1 个因录音截断而保留的待追问；最新 manifest：
  `849ea875abed44952d1b3bdbde0a08e35927ca380492376132a77418bf2564bd`；
- 当前 Note：
  `services/speech-worker/runtime/profile-interview-20260802/jobs/job_02de4d59c9a940dcbf58183f533f47ad/artifacts/note.md`；
- 三份产物一致：Note 的 19 个证据目标都存在于 transcript，53 个 transcript 展示块都存在于
  speech-record，111 个 speech-record segment 的 `raw_text` 与数据库逐条一致；
- 原始 ASR 仍为 32 次，最后创建时间仍为 `2026-08-02T02:12:06.522890+00:00`；私有副本、
  音频和产物均由 Git 忽略。

访谈 generation `17` 已通过自动、开发者审读和项目所有者人工验收，达到会议/演讲基线。
后续不再改写该基线；当前严格进入语音备忘/个人笔记质量门。

## 11. 2026-08-02 四份 Markdown 已实现

- artifact schema 升级为 `1.4.0`，结构化 schema 升级为 `1.6.0`，提示词版本为
  `2026-08-02.9`；产物 checkpoint 会比较 artifact schema，旧 processed 任务会自动重建；
- `note.md` 现在是纯净阅读版：没有 YAML 属性头、逐字稿链接、“依据”、章节导航、speech ID
  或录音技术表；只保留实际内容、普通完整度说明和 `我的补充`；
- `note.evidence.md` 承接原链接版 Note，保留属性、逐条证据链接、章节导航、记录信息和不确定
  位置；它不含 `我的补充`，防止覆盖用户手工内容；
- `timeline.md` 与场景模板解耦。14B 在同一次完整逐字稿综合中给出语义起点、标题、摘要和
  信息点，不增加第二次模型调用；Worker 确定性补齐结束边界，使所有有效校订文字恰好覆盖
  一次。旧文档缺少时间线字段时兼容为一个全范围章节；
- `transcript.md` 继续是完整校订逐字稿。最终 artifact 包共 6 个文件：4 个用户 Markdown、
  `transcript.raw.json`、`speech-record.json`，另有不计入 artifact_count 的 manifest；
- 真实通用样例生成了 3 个顺序时段：AI 需求分类、能力模块、初期切入点。`note.md` 无链接，
  `note.evidence.md` 的链接均指向同一 transcript，`speech-record.json.document` 保存相同的
  `timeline_sections`；
- 真实样例改动前后 `transcript.raw.json` SHA-256 均为
  `e1aba3f8bdf70982477ce795feb264241bb3a49b366d322dae16a96ffe06c498`，`transcript.md` 均为
  `8af536edf42b0bf1bb9351a8615f79d71041e6306d0cf305c6610210f14d717f`，16 个 segment 的
  `raw_text` 集合摘要均为 `09031cc1e85284b7cb7928a2b8336020edbd222227581bf65fa8ec7871a50410`；
  原始 ASR 和校订逐字稿没有改变；
- 当前真实 manifest 为
  `7d52d3677fa8b26ef5704134fcf55e581dacab289f53e195ca483cc4ceae2a0b`；私有 runtime 继续
  由 Git 忽略。全套 `291` 项测试、Ruff、`compileall`、`git diff --check` 通过。

其余已验收类型的 artifact schema `1.4.0` 兼容重建也已完成，全程未调用模型：

- meeting manifest：`91f3f51d5917579552bd6331e7a07092ccac26aefb4ee92abf0270395a4d2392`；
- speech manifest：`f2687518737a6b4676551d1d25df1321a763a197995425947c39a2b7da01ddd0`；
- interview manifest：`87fe52d2b30f10ad2d11d4914fc89e1dab71b57f127b673aef2c372eea47a1c3`；
- voice memo manifest：`5bdfe0ad3469c61bf085d8f9c83c50eb1e07645e38bd65952ddf530a003d6ffd`。

四个任务均验证：manifest 六个哈希匹配；纯净 Note 无属性头/逐字稿链接且保留 `我的补充`；
审计 Note 不含 `我的补充`且所有链接都能落到 transcript；timeline 无证据链接；
`speech-record.json.document.timeline_sections` 与 timeline 同源。旧任务没有模型生成的语义起点，
因此诚实兼容为一个全范围时段，不为迁移而重跑已验收长模型；新任务会生成多段语义时间线。

Stage D 的本轮实现、真实样例和兼容审计已完成。下一步先由项目所有者审阅当前 diff，并决定
是否提交/推送；此后停在 Stage E 边界等待明确确认。不要开始 Vault 发布协议或 Obsidian 前端。

## 12. 2026-08-02 `我的补充` 重生成保护

项目所有者要求继续 Stage D 后，优先落实了长期计划中“重新生成不能覆盖人工内容”的门禁：

- artifact schema 升级为 `1.5.0`；纯净 `note.md` 的 `## 我的补充` 是文件最后一个、由用户
  拥有的终端区域。Worker 重生成时只替换它前方的机器生成内容，从标题行开始将整个人工区域
  原样拼回，不解析或重写其中的普通文字、任务列表、wikilink、callout、子标题或空白；
- `note.evidence.md` 仍完全由机器维护，不承载人工区域；
- processed 任务不再只看 checkpoint generation。Worker 会先验证六个 artifact 文件与
  checkpoint 哈希；用户编辑 `note.md` 后会触发一次确定性重建和 manifest 重算，随后重新
  回到 `already_generated`；
- 若既有 Note 非 UTF-8、不是普通文件、超过安全大小、是符号链接，或缺少 `## 我的补充`
  边界，Worker 在写任何新产物前安全失败，避免把无法识别的整篇用户内容覆盖掉；
- 新增端到端和文件安全测试验证：生成区的人为改写会被正确刷新，人工区含 Obsidian callout、任务和
  二级标题时逐字保留，审计版不混入人工内容，manifest 使用保留后的 Note 新哈希；边界丢失
  时原文件保持不变。

真实通用样例写入一段临时人工内容后执行正常（非 force）重生成，人工内容全部保留；删除
临时内容后又自动重算 manifest，最终恢复空白 `我的补充`，当前 manifest 为
`f93e02fb7691b62d229253a564a558d68d0be28d0013210125ac35b490238523`。原始性摘要继续保持：

- `transcript.raw.json`：`e1aba3f8bdf70982477ce795feb264241bb3a49b366d322dae16a96ffe06c498`；
- `transcript.md`：`8af536edf42b0bf1bb9351a8615f79d71041e6306d0cf305c6610210f14d717f`；
- `{segment_id, raw_text}`：`09031cc1e85284b7cb7928a2b8336020edbd222227581bf65fa8ec7871a50410`。

当前 `294` 项测试全部通过，Ruff、`compileall`、`git diff --check` 通过，私有 runtime 未进入
Git。下一步仍在 Stage D：实现说话人命名、文字/日期纠正的独立修订记录，再实现只重生成总结
和前后差异比较；这些完成前不进入 Stage E 或前端。

## 13. 2026-08-02 独立校订与总结差异

Stage D 剩余人工修订安全项已经完成：

- SQLite schema `6` 新增 append-only `corrections`；首版字段为 `transcript_text`、
  `speaker_display_name`、`recording_date`，每条记录有 revision 门禁、before/after 连续性、
  幂等键、author 和稳定 correction ID；
- artifact schema `1.6.0` 把台账叠加到派生输出。文字校订同步 `transcript.md` 和
  `speech-record.json.text`，说话人名称同步逐字稿标题/参与者，日期同步机器记录/审计 Note；
  原始 ASR、数据库 segment text 和 `raw_text` 不改；
- artifact checkpoint 记录校订台账摘要，新记录会自动触发正常重生成，第二次恢复
  `already_generated`；纯净 Note 的 `我的补充` 仍逐字保留；
- `--document-only` 会把文字校订送入完整逐字稿综合，复用既有 findings，不重跑 ASR、逐字稿
  润色或抽取；每次运行在私有 `summary_revisions` checkpoint 记录前后哈希和统一 diff；
- 新增命令：`add-correction`、`list-corrections`、`summary-revisions`；diff 是后续确认用私有
  状态，不新增要发布到 Obsidian 的用户文档，正式 artifact 仍是 4 Markdown + 2 机器记录；
- 真实通用任务的临时私有副本完成日期校订验证：raw hash 保持
  `e1aba3f8bdf70982477ce795feb264241bb3a49b366d322dae16a96ffe06c498`，transcript 与纯净 Note
  也未改变，speech-record 正确出现日期和校订记录；临时副本随后删除，真实基线未改；
- 当前 `297` 项测试通过，Ruff、`compileall`、CLI help、`git diff --check` 通过。私有数据
  没有加入 Git。

下一步只审阅、提交并推送当前 Stage D 改动，然后停在 Stage E 边界等待项目所有者明确确认；
不得自行进入 Vault 发布协议或 Obsidian 前端。

## 14. 2026-08-02 Stage E Worker 本地发布协议

项目所有者在 Stage D 提交并推送后明确回复“确认，继续”，据此只进入阶段 E，没有进入正式
网络 API 或 Obsidian 前端：

- Worker SQLite schema 升级为 `7`，新增 append-history `publication_leases` 和每任务唯一
  `publication_receipts`；同一任务同一时刻只能有一个 active lease；
- 实现发布声明、幂等重放、30–900 秒租约、续租、主动释放、过期接管、成功确认和重启恢复；
  通用状态转换不能绕过租约直接进入 `publishing` 或 `published`；
- 发布前验证 artifact checkpoint、manifest 哈希、六个 artifact 哈希、job/speech ID；损坏的
  Worker 产物会在申请租约前失败，任务仍保持 `processed`；
- 输出根目录只接受规范化 Vault 相对 POSIX 路径。拒绝绝对路径、遍历、反斜杠、设备路径、
  符号链接逃逸和发布期间 Vault 根目录/挂载身份变化；
- 发布包固定为四份 Markdown、两份机器记录和 `artifact-manifest.json`。先写同级隐藏临时目录，
  每个文件落盘后校验整包并续租，再原子重命名目录、同步父目录并做发布后校验；
- 发布目标已存在时只接受七个文件全部精确匹配的重试。任何用户修改、额外文件、同步差异或
  非普通文件都保留原状并报告冲突，不覆盖现有内容；
- 原子重命名已完成但 acknowledgement 中断时，任务回到 `processed`；下一发布者验证既有完整
  目录后即可重新领取并确认。发布失败始终保留 Worker 私有产物；
- 新增 `claim-publication`、`renew-publication`、`release-publication`、
  `acknowledge-publication`、`publication-status` 和 `publish-to-vault` 开发者命令；正式 HTTP
  契约、设备身份和 Vault 授权仍属于阶段 F；
- 自动化覆盖两个 SQLite 连接竞争、租约接管、恢复、幂等确认、路径验证、损坏 Worker 产物、
  原子发布、既有用户内容冲突、符号链接逃逸和 rename 后 ack 前中断恢复。当前基线为 `322`
  项测试全部通过，Ruff、`compileall`、CLI help、`git diff --check` 通过。

真实会议样例随后只在私有临时副本发布到临时 Vault：目标为
`Speech Notes/Undated/聚衣堂与优维合作推进AI组织转型会议纪要--sp_e007a3748ee4d80c9ef6`，
首次返回 `published`，第二次返回 `already_published`，revision 保持 `13`；发布目录恰好七个
文件，与 Worker artifact 目录逐字节一致，manifest SHA-256 为
`91f3f51d5917579552bd6331e7a07092ccac26aefb4ee92abf0270395a4d2392`。真实、复制品与发布包的
`transcript.raw.json` SHA-256 均为
`45bf58083c586998b690faf8bc9bfb705d3b260b5b2c971aa25ce5c73f2575ea`。

验证过程中一次只读状态查询触发了旧真实数据库的自动 schema 迁移；确认两张新增发布表均为空
后，已精确删除空表并恢复 `PRAGMA user_version = 6`，`quick_check = ok`。音频、artifact、原始
ASR、校订稿和任务内容均未改动。后续验证只操作副本；临时 runtime、Vault 和兜底数据库副本
已在验证完成后删除。

Stage E 已在提交 `9872f20 Implement atomic Vault publication` 中完成并推送到
`origin/agent/worker-core`；提交只含 16 个代码、测试和文档文件，私有 runtime、reference、
音频、逐字稿和生成 Note 未进入 Git。项目所有者随后明确回复“那继续”，下一步严格进入阶段 F
第一个工作项“协议版本与能力协商”，再建立 OpenAPI；不得提前进入阶段 G 或阶段 H 前端。

## 15. 2026-08-02 Stage E 提交与 GitHub 认证修复

- Stage E 提交：`9872f20 Implement atomic Vault publication`；已推送并与
  `origin/agent/worker-core` 同步；
- 排查确认 GitHub 反复登录并非正常到期：全局 Git 配置曾强制普通 Git 使用 GitHub CLI
  credential helper，而 CLI 同时残留两个失效账号并选错 `azhuaqiu`，导致 macOS 钥匙串中
  长期有效的 `kingqiu` 凭据被绕过；
- 已删除失效/错误账号和孤立钥匙串记录，移除全局 `gh auth git-credential` 覆盖，恢复系统
  `osxkeychain`，并在当前仓库固定 `credential.username = kingqiu`；
- GitHub CLI 复用同一条已验证有效的长期凭据，现在只保留 `kingqiu`；真实 push 和再次
  dry-run 均成功。此项只修改本机凭据配置，不产生仓库文件或密钥提交。

## 16. 2026-08-02 Stage F 协议版本、能力协商与首版 OpenAPI

- 协议版本固定为 `1.0.0`，artifact schema 协商当前范围为 `1.6.0`；版本范围使用规范
  `major.minor.patch`，客户端可在上传前得到明确兼容结果；
- 能力文档公开 Worker/协议/artifact 版本、功能、内容类型、模型档位和边界值，不返回任务、路径、
  音频、逐字稿或 Note 内容；未知的未来能力名称会返回缺失能力，而不是被旧 Worker 当成非法请求；
- FastAPI 首版只开放 `GET /v1/health`、`GET /v1/capabilities` 和
  `POST /v1/capabilities/negotiate`；当前不开放任何私人或有状态资源；
- 规范化 OpenAPI 3.1 已生成到 `packages/protocol/openapi.json`，重复生成 SHA-256 保持一致；
- FastAPI 固定为 `0.139.2`，测试客户端使用 `httpx2`；全套 `336` 项测试通过，Ruff、
  `compileall`、`git diff --check` 通过；
- `reference/`、`runtime/`、私有测试数据、音频、逐字稿和生成 Note 均未纳入本次提交。

下一步严格按阶段 F 原顺序生成 Python 和 TypeScript 类型；随后才实现带认证边界的上传、作业、
快照、更新和产物接口。不得提前进入阶段 G 或阶段 H 前端。

## 17. 2026-08-02 Stage F Python 与 TypeScript 生成类型

- 新增无第三方生成期依赖的确定性生成器，从唯一规范 `packages/protocol/openapi.json` 同时生成
  Python `TypedDict/Literal` 和只读 TypeScript `interface/type`；
- 两份生成文件均嵌入源 OpenAPI SHA-256、OpenAPI 版本和协议版本，明确标注不得手工编辑；
- 生成器支持 OpenAPI 3.1 当前使用的引用、对象、数组、枚举、常量、可空联合和可选字段，遇到
  外部引用、非法 Python 标识符、循环或未解析 schema 时明确失败；
- `--check` 不写文件，只比较规范化结果；测试覆盖当前文件无漂移、空目录两次生成逐字节一致、
  人为篡改必然失败，以及 Python 可选键/枚举与 TypeScript wire name/只读数组语义；
- 全套 `341` 项测试通过，Ruff、`compileall`、OpenAPI 重导出后类型漂移检查和
  `git diff --check` 通过；私有数据未参与或进入 Git。

下一步严格进入阶段 F 第四项：实现正式 FastAPI 健康、能力、上传、作业、快照、更新和产物接口。
发现接口已经存在；任何新增有状态或私人数据接口必须先建立认证/授权边界，不得为了扩接口而暂时
裸露任务内容。仍不得进入阶段 G 或阶段 H 前端。

## 18. 2026-08-02 Stage F 正式认证 API、断点上传与有界更新

- 正式 FastAPI 已接入 durable Worker core：上传清单、分块状态/写入/完成、作业创建/列表/详情、
  暂停/恢复/取消/重试、快照、无正文更新流、产物列表和校验后下载；
- 三个发现接口保持公开；所有任务和私人数据接口统一使用 Bearer 门禁。未配置验证器时 fail closed，
  无凭据/错误凭据返回 401；现有资源跨 Vault 查询统一伪装为 404；
- 当前验证器只保留 token SHA-256 和显式 Vault allowlist，不保留明文。持久设备配对、长期凭据、
  撤销和轮换仍是后续工作，当前不虚报完成；
- 全局请求校验错误改为稳定脱敏响应，不回显请求正文、背景、token 或 Pydantic input；每个响应带
  不含正文的 request ID；
- 分块上传支持缺块查询、SHA-256、幂等重传、流式读取和 64 MiB 硬上限；完成时继续复用核心的
  整文件校验与媒体探测；
- SQLite schema `8` 新增 `job_action_requests`，暂停/恢复/取消/重试使用 revision 与持久
  idempotency key，跨重启重放只返回首次结果，不重复迁移状态；
- 产物接口只允许七个固定文件，逐次验证 checkpoint、manifest、全部 SHA-256、目录和符号链接；
  不向客户端暴露本地绝对路径；
- OpenAPI 3.1 和 Python/TypeScript 类型已同步重生成；全套 `353` 项测试、Ruff、`compileall`、
  类型漂移和 `git diff --check` 通过。

下一步严格实现设备配对，然后按原计划完成长期凭据安全保存、Vault 授权、设备撤销/凭据轮换、
HTTPS、Tailscale/其他安全网络说明与诊断脱敏。不得进入阶段 G 或阶段 H 前端。

## 19. 2026-08-02 Stage F 设备配对与持久 Vault 授权

- 独立 `security.sqlite3` 以 `0600` 保存设备身份、Vault allowlist 和 token SHA-256，不保存
  pairing code 或 bearer token 明文，也不进入 Vault；
- 本机 `create-pairing-session` CLI 创建 30–900 秒短时会话；配对码具有高熵、只显示一次，错误
  尝试累计到 5 次后锁死；会话成功后不可重放；
- `POST /v1/pairing/confirm` 是新增的唯一公开配对接口，成功时只返回一次设备 token；该 token
  立即可访问其获批 Vault，Worker 重启后仍有效；
- 同名活跃设备不能被静默重新配对；`list-paired-devices` 不返回 token/hash，`revoke-device`
  立即使原 token 失效，撤销后重新配对 generation 递增；
- OpenAPI 与跨语言类型已同步；全套 `358` 项测试、Ruff、`compileall`、漂移和差异检查通过。

下一步严格完成凭据轮换和设备管理 API，并明确客户端长期凭据必须由后续 Obsidian 端放入系统
保护存储；随后进入 HTTPS、安全网络方案和诊断脱敏。不得进入阶段 G 或阶段 H 前端。

## 20. 2026-08-02 Stage F 设备管理与两阶段凭据轮换

- 已认证设备可在自身 Vault scope 的子集内创建短时配对会话；设备列表和撤销均按完整 scope
  过滤，越权目标不可枚举，设备响应不含 token/hash；
- 设备撤销通过正式 API 即时生效；后续 Obsidian 端仍必须把长期 token 放入操作系统保护存储，
  当前不虚报前端存储已完成；
- 独立安全数据库 schema 升至 `2`，只保存 replacement token SHA-256，并覆盖从 schema `1` 保留
  已有配对的原位迁移；
- 凭据轮换采用准备/激活两阶段：准备不撤销旧 token，激活事务原子切换；replacement token 在
  激活前无私人 API 权限，激活响应丢失后可用同一 token 安全重放；错误、过期、被替换候选均不
  会撤销当前 token；
- OpenAPI 与 Python/TypeScript 类型已同步；全套 `363` 项测试、Ruff、`compileall` 和类型漂移
  检查通过。

下一步严格配置 HTTPS，并编写 Tailscale 推荐接入及其他安全网络方案；之后完成错误消息和诊断
日志脱敏审计。不得进入阶段 G 或阶段 H 前端。

## 21. 2026-08-02 Stage F HTTPS、安全网络与脱敏收口

- 正式 `serve` 默认只监听 `127.0.0.1:8765`；非 loopback 必须是明确非公网 IP 且带完整 TLS
  证书/私钥；拒绝 wildcard、公网地址、远程 hostname 和权限过宽私钥；
- 关闭代理头信任、server/date header 和 routine access log；临时证书真实启动测试已验证 HTTPS
  `/v1/health`；
- Tailscale Serve 推荐方案和其他私网的等价安全要求已写入 `docs/private-network-setup.md`；明确
  禁止 Funnel、公网端口转发和降级为远程 HTTP；
- 对外 Worker 错误、未知异常、持久 job/upload 错误和本地 CLI error details 已完成脱敏；认证
  diagnostics summary 只返回调用者 scope 内的聚合计数、版本和数据库健康，不含稳定资源 ID、
  Vault 名、文件名、路径、正文、prompt 或凭据；
- OpenAPI 与跨语言类型同步；全套 `371` 项测试、Ruff、`compileall`、漂移和差异检查通过。

Stage F Worker 端完成；客户端 token 系统保护存储明确留到 Stage H。下一步停在 Stage G 边界，
等待项目所有者明确确认后才进入 Worker Manager 和系统后台服务；不得自行开始前端。

## 22. 2026-08-02 Stage G LaunchAgent 生命周期与真实崩溃恢复

- 项目所有者已明确确认进入 Stage G；新增 per-user LaunchAgent 生命周期核心和
  `speech-capture-manager` 开发者命令，支持 install/start/stop/restart/status/uninstall；
- plist 使用绝对 executable、无 shell 参数数组、`KeepAlive`、throttle、有限退出窗口和 `077`
  umask；相同配置幂等，不同既有配置拒绝覆盖；服务卸载不删除 Worker 数据；
- 状态和错误输出已脱敏，不包含本地路径、token、pairing code、录音正文或 prompt；
- 当前 Mac 隔离真实测试已确认：临时 Worker 首次健康后被 `SIGKILL`，launchd 自动以新 PID 拉起，
  runs 递增且健康恢复；测试 LaunchAgent 已卸载并检查无残留；
- 已明确运行边界：per-user LaunchAgent 在 FileVault 解锁并登录后启动，logout 时停止；同步 Vault
  不可用不应阻断已上传的本地处理，但必须保持待发布；网络服务提前启动不能绕过磁盘/登录边界；
- 真实冷重启和 logout/login 因会中断当前会话未自动执行，保留在维护窗口人工验收；
- 全套 `379` 项测试、Ruff、`compileall`、CLI help、依赖锁与差异检查通过。

下一步严格实现 Manager 的内容无关状态快照：模型、磁盘、内存、端口和网络；随后按 Stage G
原顺序完成空间预算、模型校验、激活/切换/回滚、诊断包和打包运行时。不得进入 Stage H 前端。

## 23. 2026-08-02 Stage G Manager 内容无关状态快照

- Manager status 已合并 launchd、Worker 数据目录、磁盘、内存、端口、TLS、Tailscale、Ollama
  和五个本地模型的可用状态；
- 只返回公开 model ID、布尔值、字节/百分比、端口、进程状态和稳定 issue code；不返回 home/
  cache/data 路径、Vault、文件名、正文、prompt、凭据、Tailscale 设备/IP/账号或命令 stderr；
- issue code 明确区分服务、目录、资源、端口、ASR/aligner 和 Ollama 各类不可用原因；pyannote
  当前作为可选能力显示，不把未安装误报为整个 Worker 不可用；
- 真实本机只读状态检查成功，确认三个 MLX 模型缓存存在；正式 LaunchAgent 未安装时正确报告
  `SERVICE_NOT_INSTALLED`，未对用户系统做永久安装；
- 当前全套 `381` 项测试、Ruff、`compileall`、CLI help、依赖锁和差异检查通过。

下一步严格实现下载模型前空间预算，并复用现有 20 GiB/10% 安全保留策略；随后才实现模型文件
校验、激活/切换和回滚。不得进入 Stage H。

## 24. 2026-08-02 Stage G 模型下载前空间预算

- 新增 `speech-capture-manager model-budget --profile accuracy|speed|all`，它复用内容无关状态快照，
  仅估算所选档位里尚缺模型的下载空间，不发起下载；
- 预算加入缺失模型体积的 15% 余量，并要求下载完成后仍至少保留 20 GiB 或卷容量 10%（取更
  大值）；返回总目录预算、剩余下载量、保留量、预计下载后空间、缺口和 `can_download`；
- 结果标记为 estimate-only，且不包含路径、用户目录、文件名、音频、逐字稿、Note 或凭据；
- 本机只读验证最初在 Ollama 服务未运行时只识别三个 MLX 模型；完整性校验确认两个 Ollama 模型
  也已安装，因此状态已改为分开判断本地安装与服务运行，修正后 all 档剩余下载量为 0；
- 预算公式、缓存扣除、低空间拦截、非法输入和 Manager 命令输出测试已补齐。

下一步严格实现模型文件校验，不再只以缓存目录存在作为可用依据；随后才做激活、切换和回滚。
不得进入 Stage H。

## 25. 2026-08-02 Stage G 模型文件完整性校验

- 新增 `speech-capture-manager model-verify --profile accuracy|speed|all`；失败时返回退出码 `3` 和
  稳定 issue code，不回显任何本地路径或私人内容；
- MLX 模型校验 revision、tree 元数据、必需配置和分片集合，拒绝链接逃逸，并对 Git blob、LFS
  文件和 Safetensors 做大小、完整内容哈希与头部校验；
- 当前发布批准的三个 Hugging Face revision 和两个 Ollama manifest digest 固定在版本目录中；
  即使文件内部自洽，未知 revision 也不会被当作可激活版本；
- Ollama 模型校验 manifest 及每个 config/layer blob 的大小与 SHA-256；服务是否运行不再影响
  “本地是否已安装”的判断；
- 本机 all 档真实全量校验通过：五个模型、30 个必需文件、约 22.9 GB 全部有效；没有下载、改写
  或激活模型；
- 自动化覆盖完整/分片模型、同大小损坏、缺少元数据、未知 revision、链接逃逸、Ollama 损坏、
  服务不可达时本地 manifest 识别和 Manager 失败退出码；全套 `396` 项测试通过。

下一步严格实现模型激活、切换和回滚：新版本必须先通过完整校验，原子切换失败时保留或恢复旧
版本。不得进入 Stage H。

## 26. 2026-08-02 Stage G 模型激活、切换和回滚

- 新增 `model-activate`、`model-switch`、`model-rollback`、`model-activation-status`；每次状态改变
  前都重新执行目标 profile 的完整哈希校验；
- 私有激活状态使用锁、`0600`/`0700` 权限、文件和目录 fsync、原子 rename；拒绝链接、宽权限、
  非法 schema/model/provider/revision，失败时当前状态逐字节不变；
- 切换保留上一 profile 的精确 revision；回滚前再次校验，且不删除模型、任务证据或产物；
- ASR/ForcedAligner 运行时会把已激活 MLX 模型解析到固定 snapshot，但 checkpoint 仍只记录公开
  model ID；结构化入口也按任务档位使用激活映射；无激活记录时保留既有兼容行为；
- 隔离真实演练已完成 accuracy generation 1、speed generation 2、rollback accuracy generation 3；
  正式 Worker 数据未修改，临时目录已删除；
- 自动化覆盖成功、幂等和各失败/篡改路径；全套 `406` 项测试通过。

下一步严格提供不含私人内容的诊断包，再完成无需开发环境的打包运行时；冷重启真实验收保留到
项目所有者安排的维护窗口。不得进入 Stage H。

## 27. 2026-08-02 Stage G 内容无关诊断包

- 新增 `diagnostic-bundle --output <new-absolute.zip>`，拒绝相对路径、非 zip、既有文件和链接；
- 固定五项为公开环境版本、内容无关 Manager 状态、激活记录、完整模型校验和逐项 hash manifest；
  原始日志不进入包，避免错误文本夹带私人文件名或正文；
- 写入采用 `0600` 临时文件、fsync、无覆盖提交；Manager 只返回条目数、大小和包 SHA-256，不
  回显用户选择的路径或文件名；
- 打包前递归拒绝敏感 key、绝对私人路径和私人 marker；失败不会覆盖或留下目标包；
- 本机真实包为 5 项、2,133 bytes，人工检查无用户目录、文件名、音频、逐字稿、Note、prompt、
  Vault 或 token；临时包已删除；全套测试基线为 `413 passed`。

下一步严格完成无需开发环境的打包运行时；冷重启验收继续等维护窗口。不得进入 Stage H。

## 28. 2026-08-02 Stage G 无开发环境独立运行时

- 锁定 PyInstaller 6.21.0，新增 macOS arm64 onedir 构建和 Worker/Manager frozen dispatcher；目标
  机不需要源码、Python、uv、venv 或 Homebrew FFmpeg；
- 包含 Python、API/数据库、MLX、pyannote/torch 和 FFmpeg/FFprobe；Ollama 与模型/私有数据外置；
- manifest 覆盖普通文件与内部 symlink，拒绝链接逃逸；构建后在最小 PATH 和 `/private/tmp` 下
  验证两个入口、SQLite、激活状态、私有构建路径扫描和本地代码签名；
- 最终本地包 3,392 项、约 703 MiB，manifest SHA-256 为
  `3ad1d8e85ffdda0b1eca85e674dd6b88892ec1ca3cc6010a23ecee4bb5984f1b`；生成包由 Git 忽略；
- 打包 Manager 已对五模型完成全哈希；打包 Worker 已真实启动回环 API并返回 Worker `0.1.0a0`、
  协议 `1.0.0`，随后干净停止，临时数据已删除；
- 全套测试基线为 `421 passed`。本地为 ad-hoc 签名；跨 Mac 分发仍需项目 Developer ID 与 notarize。

Stage G 自动化核心完成。冷重启/logout-login 只在维护窗口人工验收；下一步停在 Stage H 边界，
未经项目所有者明确确认不得开始 Obsidian 前端。

## 29. 2026-08-02 晚间收尾与明日续作入口

### 今天已经完成

- Stage G 已按既定顺序完成 LaunchAgent 生命周期与真实崩溃恢复、Manager 内容无关状态、下载
  空间预算、五个模型的完整性校验、模型激活/切换/回滚、无私人内容诊断包，以及无需开发环境
  的 macOS arm64 独立运行时；
- 独立运行时功能提交为 `94b95fa`；Stage G 其余里程碑提交见本文件开头及 `git log`；本节收尾
  提交完成后，当前分支应与远端 `origin/agent/worker-core` 同步；
- 全套 `421` 项测试通过，Ruff、`compileall`、Worker/Manager CLI、依赖锁、构建前置和差异检查
  通过；独立运行时的本地构建、清单、模型全哈希和回环健康接口已真实验证；
- 没有把 `reference/`、`runtime/`、`dist/`、`build/`、模型、音频、逐字稿、Note、数据库或其他
  私人/生成数据加入 Git。

### 尚未执行但不阻断今天收尾的外部事项

1. Stage G 的 Mac 冷重启和 logout/login 验收会中断当前登录会话，只能在项目所有者安排的维护
   窗口执行；明天不要自行重启或退出登录；
2. 若要把独立运行时分发到其他 Mac，需要项目控制的 Apple Developer ID 签名与 notarization；
   当前本机开发包只有 ad-hoc 签名，不得虚构、导出或提交证书；
3. 以上两项是人工/发布边界，不需要为了开始交互设计而重复构建、重复模型哈希或重跑真实音频。

### 明天严格从这里继续

1. 先完整读取本文件和 `docs/development-status-and-next-steps.md`，确认分支和工作区状态；不要重新
   规划，不重复 Stage G，不重跑已验收真实任务；
2. 向项目所有者说明当前停在 Stage H 边界，并等待明确确认是否进入 Stage H；
3. 若确认进入 Stage H，严格先做交互设计而不是前端编码：从研究本机 Reading Capture 的布局、
   组件、间距、排版、颜色和交互开始，再依次设计信息架构、本地/远程提交完整流程（含可选自由
   文本“补充背景”）、全部界面状态矩阵，以及 Worker 状态/错误/进度到界面的映射；
4. 随后设计任务工作台、渐进文字、说话人复核、资源警告、离线恢复、待发布、文字稿与证据定位、
   修订和重新总结，并使用合成数据制作关键交互图；
5. Stage H 的主要流程、状态、术语和视觉方向必须逐项交给项目所有者审阅并明确批准；批准前不得
   进入 Stage I，也不得开始 Obsidian 插件编码。

明天新对话最简开场可直接说：

```text
请完整读取 docs/handoff-2026-08-01.md 和 docs/development-status-and-next-steps.md，严格从交接记录
第 29 节继续。不要重复 Stage G 或重跑真实音频；先告诉我当前边界和 Stage H 的第一步，等我确认
后再进入 Stage H 交互设计，不要提前开始 Stage I 前端编码。私有数据不得加入 Git。
```

## 30. 2026-08-03 Stage H 启动与首轮设计评审入口

- 项目所有者已明确确认进入 Stage H，并要求设计稿以图片发送到当前界面讨论；Stage I 仍未开始；
- 已只读研究本机 Reading Capture 的匿名截图、交互规范和实际样式，没有打开真实 Vault；
- 交互基线已写入 `docs/design/stage-h/interaction-baseline-v1.md`，包含信息架构、本地/远程流程、
  自由文本补充背景、状态矩阵、Worker 映射、渐进文字、证据复核、重生成差异和无障碍约束；
- 首轮四张暗色桌面候选稿已保存到 `docs/design/stage-h/images/`：新建语音任务、正在处理、
  逐字稿与证据复核、修订后重新生成笔记；所有内容均为合成数据；
- 每张图的目的、prompt 版本和待确认问题见
  `docs/design/stage-h/visual-review-index-v1.md`；当前状态是等待项目所有者逐图讨论，不得标记为
  已批准，也不得据此开始 Stage I；
- 首轮骨架确认后，下一批严格补资源阻断、Worker 离线/恢复、已处理未发布、首次 Worker 设置、
  浅色主题和窄侧栏；私有数据继续不得进入图片或 Git。

下一步：先讨论本轮四张图，确认或修改入口、三栏结构、单页提交、纯净 Note 与专用证据复核的
分工，以及 Reading Capture 风格延续程度。未确认前不要批量生成后续稿，也不要写前端代码。

## 31. 2026-08-03 Stage H 上午评审完成部分与下一批设计

- 项目所有者确认 `语音工作台` 是应保留的插件首页和统一外壳：默认显示任务列表、单页新建任务
  和提交前确认；复核页仍在同一外壳内，因此删除重复的 `返回语音工作台`；
- 三栏骨架、左右栏独立/同时收起、记住上次状态、标题行内低对比收起图标已经确认；
- 纯净 Note 继续使用 Obsidian 原生阅读；专用界面只承担证据、说话人和修订；
- 单段说话人纠错改为 `这段话是谁说的？`，使用覆盖全部识别人员的可搜索多行清单，不限制为
  两人；批量显示名和单段纠错保持分离，修订不覆盖原始 ASR；
- 复核音频采用本地同源文件优先、家中 Worker 私有流式播放其次、Worker 离线时文字可读但播放
  禁用；V3 在线/离线稿已生成并获确认；
- 工作台首页保留本机/远程 Worker 架构，个人 V1 默认家中 Worker。本机不可达时显示不可选择的
  `未检测到可用 Worker`，提供重新检测和安装/启动说明，不武断区分未安装/未启动，不静默换机；
- 新增合成稿 `01-new-task-local-worker-unavailable-v2.png`，以及逐字稿复核 V2/V3 和 Worker 离线
  变体；图片、Prompt 版本和状态见 `docs/design/stage-h/visual-review-index-v1.md`；
- 当前未进入 Stage I，也没有 Obsidian 页面代码。新增的受认证复核音频读取和插件可读的内容无关
  Worker/Manager 就绪状态，是进入 Stage I 前必须补齐并测试的后端契约；
- 私有音频、真实逐字稿、Note、Vault、runtime、reference、模型、数据库、凭据和绝对私人路径
  不得进入 Git 或图像生成。
- 项目所有者确认磁盘不足页说明和操作足够清楚；连接中断后每隔 1 分钟自动重试、最多 3 次，失败
  后才提示手动重试，并删除 `稍后再试`；正常情况下自动发布，发生冲突时先查看差异，查看后才
  能选择保存到新位置。

本轮已继续制作资源阻断/安全暂停、Worker 重新连接/恢复、已处理未发布和发布冲突五张合成状态稿，
文件与 Prompt 版本见视觉索引。恢复与发布规则已经确认；下一步严格修订对应视觉稿，并完成首次
Worker 设置、左右栏组合/窄窗口/浅色主题，以及总结差异页最终规则。不得提前进入 Stage I，
不重跑真实音频，不重复 Stage G。

对应视觉稿现已完成修订，并新增首次 Worker 配对及配对后设备管理两张合成稿。首次配对不使用
多步向导；短时配对码只在授权时输入，长期凭据只保存到系统钥匙串且不显示；普通 Worker 重启不
要求重新配对。下一步先审阅这两张设置稿，再继续左右栏组合、窄窗口和浅色主题。Stage I 仍未
开始，私有数据继续不得进入 Git 或图像生成。

后续评审再次明确：三次自动重试失败后的 `重新连接书房 Mac` 必须作为 Obsidian 工作台主区的
唯一主动作，不能藏在右栏或要求前往其他应用；点击后立即尝试一次，失败则保持页面和入口。发布
冲突页删除 `保留当前版本，稍后处理`，因为两侧内容已经持久保存；用户直接离开即可，任务继续
标记为 `发布冲突`，以后从左侧任务列表选择同一任务继续处理。

项目所有者随后确认继续。Stage H 已生成左栏收起、右栏收起、左右同时收起、窄窗口单列重排和
浅色主题五张合成候选稿，文件与 Prompt 版本见视觉索引。响应式临时布局不覆盖用户记住的桌面栏
状态，所有修订上下文继续保留。窄窗初稿暴露出“修订区位于全文末尾”的长录音滚动问题，已在
V2 改为选中段落下方就地展开。下一步先审阅这组适配稿，随后完成总结差异页的窄窗排列、旧版本
入口、整版接受规则，以及最终中英文消息键和无障碍批准清单。不得进入 Stage I。

项目所有者确认浅色稿只用于验证 Obsidian 主题兼容：V1 只跟随 Obsidian 当前主题，不提供工作台
内部的主题或颜色开关；主题实时变化不能重置任务、表单、播放或修订状态。自定义配色与插件独立
主题覆盖推迟到功能流程全部跑通后的后续优化。下一步继续总结差异页最终规则。

随后已生成总结差异桌面稿、窄窗口稿和只读版本记录稿，全部使用合成内容。当前待确认的 V1 候选
边界是：只做整版接受/不采用，不做逐条合并；不采用候选版时旧版保持当前、候选版仍留在版本记录；
`我的补充` 不参与机器版本切换；版本记录只允许查看和比较，不提供回滚、合并或删除。窄窗口按
单个变化纵向展示原版本和候选版本。确认后才写入决策日志，并继续最终中英文消息键和无障碍批准
清单；未经确认不得进入 Stage I。

项目所有者已经确认按当前简化稿执行，并进一步强调 MVP 不扩大版本管理：只做整版接受或不采用，
未采用候选继续可追踪，`我的补充` 保持受保护，版本记录只读；逐条合并、回滚、删除和其他复杂
比较/管理能力全部推迟。该决定已进入决策日志。下一步严格完成最终简体中文文案、英文消息键、
键盘/减少动态效果和无障碍批准清单；完成评审前不进入 Stage I。

现已完成两份 Stage H 收尾文档：`message-catalog-v1.md` 固定简体中文文案、英文消息键和后端状态
映射底线；`accessibility-approval-checklist-v1.md` 固定键盘、屏幕阅读器、主题、减少动态效果、
响应式、安全恢复和设计门。当前只剩集中确认首次 Worker 设置的信息优先级、栏目/窄窗细节、
核心中文术语与按钮后果文案，以及本清单能否作为 Stage H 最终批准合同。未获确认前不进入 Stage I。

剩余低风险细节已经按“功能优先、MVP 简单”原则合并为一组收敛默认并写入视觉索引：简短的本机
Worker 不可用说明、有限处理中预览、当前片段内的原始证据与多人选择、单句钥匙串说明、设置页
底部撤销授权、扩大命中区的窄边缘把手，以及窄窗口一次展开一个片段。下一步只需一次集中确认这
组默认、消息目录和批准清单；不要再拆成逐图小问题，确认前不进入 Stage I。

项目所有者于 2026-08-03 明确回复 `确认 Stage H`，并补充不可放宽的实现要求：正式编码必须以
已确认的当前版本设计图为视觉目标，尽可能 1:1 还原，不得把设计图仅当灵感或在实现时自行重做
页面。行为以交互基线为准，视觉以视觉索引当前文件为准，文案以消息目录为准。Stage I 每个关键
页面都需要用合成数据截图与批准稿并排核对；实质偏差必须修正或重新评审。该要求已经进入决策
日志、设计门、PRD、路线图和批准清单。

Stage H 正式完成。下一步严格先补两个前端依赖的后端契约：鉴权并支持 seek/range 的复核音频读取，
以及插件可读取的内容无关 Worker/Manager 就绪状态；完成测试后才进入 Stage I 编码。私有数据继续
不得进入 Git、实现截图或图像生成。

## 32. 2026-08-03 Stage I 前置后端契约

- 两项前置契约已经实现：Worker 为每个完成预处理的任务生成私有、可校验的低码率复核音频，并
  通过现有认证和 Vault 授权提供元数据及标准 HTTP Range 内容；插件还可读取不含任务内容、文件名、
  路径、端点地址和凭据的 Worker readiness；
- 复核音频仅是任务生命周期内的私有派生文件，不发布到 Vault，不改变原始音频和原始 ASR；损坏、
  symlink、checkpoint 不一致或跨 Vault 访问均拒绝。离线时仍遵守 Stage H 的“文字可读、播放禁用”；
- readiness 覆盖端点模式/TLS、数据库、磁盘、内存、FFmpeg/FFprobe、Ollama、激活模型和两个模型
  profile，使前端可表达未配对、版本/资源/模型未就绪；端点完全不可达时仍只能显示
  `未检测到可用 Worker`，不能武断判断未安装；
- OpenAPI 与生成类型已同步；Worker 全套 `427 passed`，Ruff、生成类型检查、差异格式检查和私有
  数据扫描通过。波形峰值、独立长期归档/显式到期、多客户端带宽管理继续后置，不属于首版隐含
  承诺；
- 两项前置缺口已经清除。下一步正式进入 Stage I；编码必须以 Stage H 当前版本设计图、交互基线
  和消息目录为唯一实现合同，逐关键页面用合成数据截图并排对照，尽可能 1:1 还原，不能自行重新
  设计，也不得重跑真实音频。

## 33. 2026-08-03 Stage I 已启动

- Obsidian 插件已有可复现的 pnpm/TypeScript/esbuild/Vitest 构建、版本清单、Ribbon/命令入口、
  非秘密设置迁移和 Obsidian `secretStorage` 凭据边界；插件测试 `17 passed`，类型检查与生产构建
  通过，`main.js`、`node_modules` 和私有数据均不进入 Git；
- 首个语音工作台外壳已经按批准稿编码：宿主主题变量、三栏、标题行内无边框收起按钮、窄边缘
  恢复把手、1180/860px 响应式行为、可见焦点和减少动态效果均已落地；还未做完成截图，不得把
  当前中间态宣称为 1:1 验收完成；
- 新建任务已接入音频选择/拖放、类型检查、文件名/修改时间日期建议、自由文本背景和处理模式；
  Worker 探测使用 health、能力协商、受认证 readiness，只有所选 profile 可启动时才允许提交；
- 单字段配对差异已经解决：Worker 额外生成短时 `pairing_ticket`，插件只显示和提交一个配对码输入，
  旧 `session_id + pairing_code` 继续兼容；票据受原有到期、失败次数和一次消费保护，只存 hash，
  成功 Bearer 立即进入系统 Secret Storage。三栏配对页已按批准稿编码；Worker 全套 `429 passed`，
  Ruff 和生成协议检查通过；
- 当前没有执行真实上传、任务创建、真实音频或真实 Vault 操作。下一步严格实现可恢复上传与任务
  创建，不改变已批准提交页和配对页的信息层级。

## 34. 2026-08-03 Stage I 可恢复提交与任务进度

- 插件现已真正接通上传和建作业：长音频以 4 MiB 有界窗口计算 SHA-256，再按 Worker 返回的分块
  大小只补传缺失 part；上传清单与建作业均使用稳定幂等键，同一未变文件重试可复用已上传分段；
- `recording_date` 已加入正式任务请求/响应和生成协议，严格验证真实日历日期，并从任务初始元数据
  进入 `speech-record.json` 与证据 Note；后续追加式日期修订可覆盖初始日期，原始音频和 ASR 不变；
- 补充背景、日期、处理模式和可选内容类型现在随正式任务进入 Worker。插件普通设置不持久化音频、
  背景或 token，长期凭据仍只在 Obsidian Secret Storage；
- 已接授权 Vault 的任务列表、任务快照、暂停/继续和渐进逐字稿预览。处理中页面已按批准稿结构
  实现阶段轨道、进度、最近稳定片段、临时尾部和右侧当前任务动作，但尚未做合成截图对照，不能
  宣称视觉已完成；
- 连接中断遵守已批准恢复规则：每隔 1 分钟自动尝试，最多 3 次；三次失败后才在 Obsidian 主区
  显示手动 `重新连接…`，没有 `稍后再试`；
- Worker 全套测试、Ruff、协议生成检查通过；插件 `24 passed`、类型检查和生产构建通过。没有运行
  真实上传、真实 Vault 或真实音频，私有数据没有进入新增测试或页面。

下一步按 Stage I 原顺序补齐取消/失败后重试、资源阻断和安全暂停，再以合成数据制作新建任务、
配对和处理中页面的实现截图，与 Stage H 当前批准稿逐页并排核对；视觉修正后再继续证据复核，
不得跳到发布或 Stage J。

## 35. 2026-08-03 Stage I 首批视觉对照续作

- 进度 commit `3b2a5fb` 已推送 `origin/agent/worker-core`；macOS Git 凭据可正常推送，只有 `gh` CLI
  自身 token 已失效。私有音频、逐字稿、Note、数据库、运行目录、凭据和参考目录均未进入提交；
- 已用 Git 忽略的本地合成页面，对处理中、新建任务和首次配对三页做 1586×992 截图核对；未连接
  真实 Vault、Worker 或音频；
- 对照驱动的修正包括桌面三栏比例、处理中状态色、任务卡布局、阶段/进度/逐字稿密度、右侧状态卡、
  配对专用列宽和独立授权确认卡；当前仍需临时空 Vault 的真实 Obsidian 宿主验证；
- 新建任务增加只读音频时长探测，并按 Worker 相同公式显示真实的临时/总磁盘估算；临时 object URL
  在成功、失败或超时后释放，不持久化音频；
- 一分钟/三次的自动重连规则已抽成纯逻辑并测试，手动重连失败后入口保持；插件现在 `24 passed`，
  类型检查和生产构建通过。

下一步只使用临时空 Vault 做真实 Obsidian 宿主视觉验证，保存首批正式实现对照图；随后继续失败、
等待用户、磁盘阻断和安全暂停状态，不进入复核/发布完成验收或 Stage J。

## 36. 2026-08-03 Stage I 真实宿主首轮验证完成

- 项目所有者已手动打开项目内、由 Git 忽略的独立测试 Vault；后续不得由自动化强制启动、重启或
  并行打开第二个 Obsidian 实例，所有真实宿主验证都只使用这个测试 Vault；
- 使用本地合成 Worker 和合成任务，在真实宿主中验证新建任务、首次配对和正在处理三页；没有打开
  正式 Vault、读取真实音频或重跑真实任务。验证结束后模拟 Worker 已停止，端口不再监听，临时
  授权与 Worker 选择已经清除；
- 宿主验证发现并修复三个真实问题：左栏收起在紧凑宽度下压缩主区；响应式判断使用应用窗口而非
  插件 Pane；输入配对码后连接按钮仍禁用；
- 现在由 `ResizeObserver` 观察插件 Pane，并使用 `wide / compact / narrow` 三档布局。当前约
  1250 px Pane 正确使用紧凑双栏，完整三栏只在大于 1480 px 时启用；左右栏分别/同时收起和状态
  延续已验证；
- 三张正式实现截图与详细记录见 `docs/design/stage-i/host-validation-v1.md`；所有截图只含合成内容。
  插件 `26 passed`，严格类型检查和生产构建通过。

下一步严格继续 Stage I 的失败、等待用户、磁盘阻断和安全暂停状态页，使用合成状态并在同一测试
Vault 验证；不要进入复核/发布完成验收，不要开始 Stage J，私有数据继续不得进入 Git。

## 37. 2026-08-03 Stage I 中断状态真实宿主验证完成

- 磁盘阻断、安全暂停、等待用户和处理失败四类页面已实现，并在项目所有者手动打开的独立测试
  Vault 中通过真实 Obsidian 宿主验证；所有任务、逐字稿、容量和文件名均为合成内容；
- 磁盘页显示预计需要、当前可用和安全保留，明确音频、稳定逐字稿和检查点均已保留，只提供
  `重新检测空间`；安全暂停、等待用户和失败分别提供 `继续处理`、`重新检查并继续` 和
  `重试当前阶段`；每页只有一个恢复主动作；
- 真实宿主发现并修复了中断任务阶段轨道错误：暂停/等待/失败现在使用最后 `progress.stage`，不会
  被误画成已经进入质量检查；中断状态也不再显示仍在运行的预计剩余时间；
- 无法解码的源文件不会机械重试，而是引导用其他音频新建任务；状态卡不显示内部路径、异常堆栈
  或原始错误文本；
- 四张实现截图已加入 `docs/design/stage-i/images/04-*.png` 至 `07-*.png`，完整索引见
  `docs/design/stage-i/host-validation-v1.md`；插件 `32 passed`，严格类型检查和生产构建通过；
- 合成 Worker 已停止，8765 不再监听；临时凭据、Vault 授权和栏状态已清理；`tmp/` 继续不得提交。

下一步先完成任务取消和不可恢复终态的简单边界，然后进入 Stage I 逐字稿与证据复核第一批实现；
严格按 Stage H V3 在线/离线稿和六人说话人清单实现，不提前做发布页或 Stage J，不重跑真实音频。

## 38. 2026-08-03 晚间收尾与明日续作入口

### 今天最终状态

- Stage I 已完成七页真实 Obsidian 宿主验证：新建任务、首次配对、处理中、磁盘阻断、安全暂停、
  等待用户和处理失败；实现截图与说明见 `docs/design/stage-i/host-validation-v1.md`；
- 今天新增的宿主问题均已修复：Pane 宽度响应式、左栏收起压缩、配对按钮即时启用、中断任务阶段
  定位、重复恢复动作和中断状态虚假 ETA；插件最终基线为 `32 passed`，类型检查和生产构建通过；
- 合成 Worker 已停止，端口、临时授权和测试页签已清理；没有打开正式 Vault、读取或重跑真实音频；
- 今天的代码、测试、合成截图和文档在本节收尾后提交并推送到 `origin/agent/worker-core`。私有音频、
  逐字稿、Note、数据库、凭据、`reference/`、`tmp/` 和构建产物不得进入 Git。

### 明天严格从这里继续

1. 先完整读取本文件、`docs/development-status-and-next-steps.md` 和
   `docs/design/stage-i/host-validation-v1.md`，检查当前分支与工作区；不要重新规划已经批准的流程；
2. 先完成任务取消和不可恢复终态的简单边界，保持 MVP 简洁，不扩展复杂历史或恢复机制；
3. 随后进入 Stage I 逐字稿与证据复核第一批实现，严格按 Stage H 的 V3 在线/离线视觉稿和文案：
   本地同源音频优先，家中 Worker 私有 Range 流式播放其次，Worker 离线时文字仍可读和修改但播放
   禁用；
4. 多人复核必须使用完整、可搜索、约六行后滚动的说话人清单；批量显示名与单段
   `这段话是谁说的？` 分开；说话人和文字修订一起保存，原始 ASR 永不改写；
5. 每个关键页面继续用纯合成数据在同一独立测试 Vault 做真实宿主截图对照；不得强制启动第二个
   Obsidian，不打开正式 Vault，不重跑真实音频；
6. 不提前实现发布页，不进入 Stage J，私有数据继续不得进入 Git 或截图。

明天新对话最简开场可直接说：

```text
请完整读取 docs/handoff-2026-08-01.md、docs/development-status-and-next-steps.md 和
docs/design/stage-i/host-validation-v1.md，严格从交接记录第 38 节继续。先完成任务取消和不可恢复终态
的简单边界，再按 Stage H V3 已批准设计进入逐字稿与证据复核第一批实现；继续使用合成数据和独立
测试 Vault 做真实 Obsidian 宿主截图对照。不要重做 Stage G/H，不重跑真实音频，不提前进入发布页
或 Stage J，不要启动第二个 Obsidian 实例，私有数据不得加入 Git。
```

## 39. 2026-08-08 Stage I 取消终态与逐字稿复核第一批完成

### 本轮完成

- 按第 38 节先完成任务取消和不可恢复终态：只有活动任务可取消；确认弹窗说明保留已上传音频、
  稳定逐字稿和检查点，但本任务不能恢复；取消后不再显示继续或重试，唯一后续动作是新建任务；
- Worker 修订账本新增原子 `segment_review`：一次追加记录同时保存当前段文字和说话人归属，带任务
  revision、幂等键和旧值校验；新增修订列表与单段复核 API；原始 ASR、原始逐字稿段和原始说话人
  永不改写；
- 派生产物生成时叠加复核修订，`transcript.md` 与 `speech-record.json` 使用有效文字和说话人，
  `speech-record.json` 同时保留 `raw_text` 与 `raw_speaker_id`，以便核验原始证据；
- Obsidian 插件已实现逐字稿与证据复核第一批：完整分页读取逐字稿、按说话人筛选、片段选择、
  六人可搜索清单和 `暂不确定`、文字/说话人一起保存、证据状态、左右栏收起和紧凑布局；
- 音频边界已接通：同一提交会话的本地音频优先；否则读取鉴权 review-audio metadata，并用 HTTP
  Range 只获取当前段 PCM 后组装小 WAV。Worker 离线时禁用播放和滑杆，但逐字稿仍可阅读修改；
- 修复两个真实宿主问题：处理完成任务普通轮询会清空未保存草稿；Obsidian 网络异常没有映射到
  三次自动恢复状态机。现在草稿在轮询后保留，断线立即显示离线音频状态，并按每分钟、最多三次
  自动重试，失败后才提供手动重连；
- 四张真实宿主截图已加入 `docs/design/stage-i/images/08-*.png` 至 `11-*.png`，详细说明见
  `docs/design/stage-i/host-validation-v1.md`。截图、任务、逐字稿和 WAV 字节全部为合成数据；没有
  启动第二个 Obsidian，没有打开正式 Vault，也没有读取或重跑真实音频；
- 当前回归：Worker `436 passed`，Ruff 与协议生成检查通过；插件 `36 passed`，严格类型检查和生产
  构建通过。
- 合成 Worker 已停止；测试 Secret Storage 凭据、Vault ID 和 Worker 选择已清除，测试插件已停用；
  `tmp/` 中的独立测试 Vault 与合成运行数据继续由 Git 忽略。

### 下一步严格边界

1. 继续 Stage I 逐字稿与证据复核第一批的剩余项，不重做 Stage G/H；
2. 先实现“批量改显示名”，并与已完成的单段 `这段话是谁说的？` 明确分开：批量改名只改变同一
   匿名说话人的展示名称，不改每段归属；
3. 继续用合成数据验证 Range 真实播放、本地同源音频优先、多人搜索滚动和窄窗选中段下方就地
   修订；修复只限已批准 V3 交互；
4. 不进入发布页，不开始 Stage J，不重跑真实音频，不启动第二个 Obsidian；正式 Vault、音频、
   逐字稿、Note、数据库、凭据、`reference/`、`tmp/` 和构建产物不得加入 Git。

## 40. 2026-08-08 Stage I 逐字稿复核第一批收口

### 本轮完成

- Worker 新增专用的追加式 `speaker_display_name` 修订 API。批量显示名只改变同一匿名说话人的展示
  标签；单段 `segment_review` 继续独立保存当前片段的文字和归属，两者不会互相冒充；
- 第一条用户明确改名允许覆盖模型派生的显示名，后续改名仍严格校验修订链旧值。原始 ASR、原始
  `transcript_segments.text` 和 `speaker_id` 始终不变；OpenAPI 与生成的 Python/TypeScript 类型已同步；
- Obsidian 已实现批量显示名、筛选按钮、六人可搜索清单以及显示名在逐字稿行和两个选择器中的一致
  覆盖。搜索条件在重绘后会重新应用，不再出现“输入仍在但选项全部恢复”的偏差；
- 合成宿主样例把匿名 `说话人 2` 改为 `王总`。数据库只读核验确认新增修订与原始段落分离；真实
  Worker 复核音频元数据返回 200，Range 字节返回 206，Obsidian 片段播放无错误；
- 窄窗口宿主对照发现桌面右栏与行内编辑器重复显示，现已修正为只在选中段落下方展开编辑器；正式
  合成截图为 `docs/design/stage-i/images/12-speaker-display-name-host-v1.png` 和
  `13-transcript-review-narrow-host-v1.png`；
- 当前完整回归为 Worker `437 passed`，插件 `37 passed`；Ruff、严格类型检查、生产构建、协议生成
  漂移和差异格式检查通过；本轮没有真实音频、正式 Vault、发布页或 Stage J 操作；
- 收尾已完成：合成 Worker 停止且 8765 无监听，Obsidian Keychain 测试 Secret 删除，三个合成
  Worker 凭据撤销，Vault ID/首选 Worker 清空，测试插件停用。所有测试运行数据继续由 Git 忽略。

### 下一步严格边界

1. 逐字稿与证据复核第一批已经完成，不重复 Stage G/H，也不重做本轮宿主音频；
2. 下一项按 Stage I 既定计划进入“逐字稿修订后重新生成笔记”和总结差异第一批：先接通受保护的
   `我的补充`、候选 Note 与当前 Note 的版本边界，再实现已批准的整版接受/不采用；
3. 桌面总结差异必须对照 Stage H `14-summary-diff-desktop-v2.png`，窄窗使用纵向变化卡片；V1 不做
   逐条合并、回滚、删除或复杂版本管理；
4. 暂不进入发布页，不开始 Stage J，不重跑真实音频，不启动第二个 Obsidian；继续只用合成数据和
   独立测试 Vault。私有 Vault、音频、逐字稿、Note、数据库、凭据、`reference/`、`tmp/` 和构建
产物不得加入 Git。

## 41. 2026-08-08 Stage I 修订后重新生成笔记与总结差异第一批收口

### 本轮完成

- Worker 新增受认证的总结候选列表、重新生成和整版决定 API。候选记录保存生成前后结构化文档、
  checkpoint、证据差异、修订摘要和内容哈希；只有出现新的文字/单段归属/说话人显示名修订时才允许
  再生成，同一批修订不会重复创建候选；
- `接受新版笔记` 与 `不采用新版` 都带任务 revision、候选 revision 和幂等保护。接受时才重写机器
  产物，并保护 `我的补充`；不采用时恢复候选前结构化 checkpoint，不改当前 Note；原始 ASR、原始
  逐字稿和证据始终不参与版本切换；
- Obsidian 已实现逐字稿页重新生成入口、待确认任务状态、桌面三栏总结差异、窄窗纵向变化卡片、
  底部内联决定区和只读版本记录。页面文案已与 Stage H 消息目录统一为 `比较重新生成的笔记`、
  `当前版本`、`候选版本`、`接受新版笔记`、`不采用新版`、`当前使用` 和 `未采用`；
- 真实宿主发现并修复两个缺口：跨响应式断点时内部尺寸更新但 DOM 未重新绘制，窄窗操作卡缺失；
  候选决定后逐字稿页没有入口重新查看版本记录。现在断点变化会安全重绘，逐字稿页在有记录时固定
  显示 `版本记录`；
- 唯一已打开的独立测试 Vault 使用纯合成任务完成桌面、窄窗、v1→v2/v3 候选和两次不采用验证。
  只读数据库核验确认原始第一段文字/说话人未改、两个决定均为 `rejected`、当前结构化摘要仍为 v1，
  合成 `note.md` 的 `我的补充` 和哈希不变；没有真实音频或正式 Vault 操作；
- 正式实现截图新增 `docs/design/stage-i/images/14-*.png` 至 `19-*.png`；最终文案和操作状态以
  `18-summary-diff-desktop-final-host-v1.png` 与 `19-summary-diff-narrow-actions-final-host-v1.png`
  为准；测试 Vault、模拟 Worker、数据库、WAV 和凭据仍不得加入 Git；
- 当前完整回归为 Worker `442 passed`、插件 `41 passed`；Ruff、严格类型检查、生产构建、协议生成
  漂移和差异格式检查通过。
- 合成 Worker 已停止。Mac 在最后一次非必要截图替换前自动锁屏，因此本轮测试 Secret 尚未从
  Obsidian Secret Storage 清除；下次宿主操作必须先清除它。它只授权 Git 忽略的合成 Vault，正式
  Vault 和私有数据从未进入该授权。

### 下一步严格边界

1. 本批已经完成，不重做 Stage G/H，不重跑逐字稿复核或真实音频；
2. 暂停在发布页之前。下一次继续时先清除本轮测试 Secret，并只读检查本批提交/工作区与宿主状态，
   再根据 Phase 4 既定
   顺序确认是否进入“处理完成与 Vault 发布”第一批；未经新的明确继续，不提前编码发布页；
3. 若进入发布第一批，必须继续使用 Stage H 已批准的自动无冲突发布与差异优先冲突处理，不增加
   `稍后处理`、复杂版本管理、逐条合并或 Stage J 远程能力；
4. 继续只使用合成数据与同一个独立测试 Vault；不得启动第二个 Obsidian，不读取正式 Vault，不
   运行真实音频。正式 Vault、音频、逐字稿、Note、数据库、凭据、`reference/`、`tmp/` 和构建产物
   不得加入 Git。

## 42. 2026-08-08 今日收尾与明日续作入口

### 今天最终完成状态

- Stage I 的“逐字稿修订后重新生成笔记”和总结差异第一批已经收口：Worker 候选版本协议、插件
  桌面/窄窗差异页、整版接受/不采用、受保护 `我的补充`、只读版本记录和真实 Obsidian 合成宿主
  对照均已完成；
- 今天没有进入发布页或 Stage J，没有打开正式 Vault、读取真实音频、重跑真实任务或把私有数据
  加入 Git；
- 最终回归基线为 Worker `442 passed`、插件 `41 passed`；Ruff、类型检查、生产构建、协议生成漂移
  和 `git diff --check` 通过；
- 合成 Worker 已停止，8765 无监听。Mac 在最后一次非必要截图替换前自动锁屏，因此本轮合成 Vault
  的测试 Secret 仍在 Obsidian Secret Storage；它只授权合成 Vault，明天第一件宿主操作必须清除；
- 当前成果仍是本地未提交修改，分支为 `agent/worker-core`。明天必须保留并检查这些修改，不得重置、
  覆盖或把 `tmp/`、测试 Vault、数据库、WAV、凭据、私有 Note 与 `reference/` 加入提交。

### 明天严格执行顺序

1. 完整读取本文件、`docs/development-status-and-next-steps.md` 和
   `docs/design/stage-i/host-validation-v1.md`，从本节继续，不重新规划 Stage G/H，也不重做逐字稿或
   总结差异；
2. 先只读检查当前分支、工作区和 8765 监听状态，确认今天所有未提交修改仍完整；
3. 在项目所有者解锁 Mac 后，先清除本轮测试 Secret，并确认合成 Worker 已停止；不启动第二个
   Obsidian，不触碰正式 Vault；
4. 随后进入 Phase 4 / Stage I 的“处理完成与 Vault 发布”第一批：优先复用现有 Worker 发布租约、
   原子整包写入、校验与冲突保护能力，先补正式 API/插件客户端，再实现 Stage H 已批准的处理完成、
   自动无冲突发布和发布冲突差异页；
5. MVP 继续保持简单：冲突时先查看差异，再选择保存到新位置；不提供 `稍后处理`、复杂版本管理、
   逐条合并、回滚或删除；
6. 继续只使用合成任务和同一个独立测试 Vault 做真实 Obsidian 宿主截图对照；不运行真实音频，不
   进入 Stage J，不开始外出笔记本到家中 Mac 的远程链路；
7. 代码完成后运行 Worker/插件完整回归、协议漂移、构建、格式和隐私检查。除非项目所有者明确要求，
   不自行 commit 或 push。

### 明天新对话可直接发送

```text
请完整读取 docs/handoff-2026-08-01.md、docs/development-status-and-next-steps.md 和
docs/design/stage-i/host-validation-v1.md，严格从交接记录第 42 节继续。保留并检查今天在
agent/worker-core 上尚未提交的总结候选与差异页修改，不要重置或重做 Stage G/H、逐字稿复核和总结
差异。先在我解锁 Mac 后清除本轮独立测试 Vault 的测试 Secret，并确认合成 Worker 已停止；然后按
Phase 4 / Stage I 既定顺序进入“处理完成与 Vault 发布”第一批，严格按 Stage H 已批准设计实现自动
无冲突发布，以及冲突时先查看差异、再选择保存到新位置。不要提供“稍后处理”，不要进入 Stage J，
不要启动第二个 Obsidian，不运行真实音频；继续只用合成数据和同一个独立测试 Vault 做真实宿主
截图对照。私有 Vault、音频、逐字稿、Note、数据库、凭据、reference/、tmp/ 和构建产物不得加入 Git。
未经我明确要求，不要自行 commit 或 push。
```

## 43. 2026-08-09 Stage I 处理完成与 Vault 发布第一批完成

### 本轮完成

- 严格从第 42 节继续，先保留并检查 `agent/worker-core` 上尚未提交的总结候选与差异页成果；没有
  重置或重做 Stage G/H、逐字稿复核和总结差异；
- Mac 解锁后先清除上轮独立测试 Vault 的 Secret，并确认 8765 无监听。随后才进入 Phase 4 / Stage I
  的“处理完成与 Vault 发布”第一批；
- Worker 正式新增受认证、受 Vault scope 约束的发布状态、claim、release 与 acknowledgement API。
  发布者身份只来自当前认证设备；状态只说明租约是否属于调用方，不暴露其他设备 ID；所有目标均为
  经校验的 Vault 相对路径；
- Obsidian 客户端下载固定七文件产物包并逐项核对长度与 SHA-256。无冲突时先写同级隐藏临时目录、
  校验后原子改名、再次校验，再向 Worker 确认；中断时只清理自己的临时目录，已有目标不覆盖；
- 发现目标目录内容不同后不 claim、不写入。首屏只有 `查看差异`；查看后才出现
  `保存到新位置`。保存会选择同级 `（新）/（新 2）` 位置并重复完整写入与校验；没有
  `稍后处理`、覆盖、逐条合并、回滚、删除或复杂版本历史；
- 同一个独立测试 Vault 使用两个纯合成任务完成真实宿主验证：自动任务七文件逐项哈希与 Worker
  一致并获得持久回执；冲突任务的原位置人工 Note 保持不变，新位置获得完整七文件包且回执指向新
  位置；窄窗口与放大后的三栏均已对照；
- 正式合成截图新增 `docs/design/stage-i/images/20-*.png` 至 `24-*.png`，详细记录见
  `docs/design/stage-i/host-validation-v1.md`。截图不含私有数据；
- 最终回归为 Worker `443 passed`、插件 `48 passed`；Ruff、严格类型检查、生产构建、协议生成与
  `git diff --check` 通过；
- 收尾已完成：本轮 Secret 和 Vault ID 清除，本轮及上轮遗留合成设备凭据撤销，合成 Worker 停止，
  8765 无监听。没有运行真实音频、打开正式 Vault、启动第二个 Obsidian 或进入 Stage J；
- 当前全部成果仍为本地未提交修改。没有 commit 或 push；`tmp/`、测试 Vault、数据库、合成音频、
  私有 Note、凭据、`reference/` 和构建产物不得加入 Git。

### 下一步严格边界

1. 本批已完成，不重做 Stage G/H、逐字稿复核、总结差异或本轮自动/冲突发布宿主流程；
2. 继续留在 Phase 4 / Stage I，只补发布第一批之后尚未验收的简单恢复边界：另一台已授权设备持有
   租约时显示等待并自动同步，写入或最终校验失败时不覆盖现有内容并在 Obsidian 内提供明确重试；
3. 继续以 Stage H 已批准消息和页面为合同，不增加 `稍后处理`、覆盖、逐条合并、复杂版本历史；
4. 不进入发布管理页，不进入 Stage J，不开始远程个人 Alpha，不运行真实音频，不启动第二个
   Obsidian；继续只用合成数据和同一个独立测试 Vault；
5. 私有 Vault、音频、逐字稿、Note、数据库、凭据、`reference/`、`tmp/` 与构建产物不得加入 Git；
   未经项目所有者明确要求，不 commit 或 push。

## 44. 2026-08-09 Stage I Vault 发布恢复边界完成

### 本轮完成

- 严格从第 43 节继续，只验证发布第一批遗留的两条简单恢复边界；没有重做 Stage G/H、逐字稿复核、
  总结差异、自动发布或冲突保存到新位置；
- 使用新的纯合成任务模拟另一台已授权设备先持有发布租约。当前 Obsidian 显示
  `另一台已授权设备正在发布`，不争抢租约、不写入、不暴露对方身份；模拟他端完成并确认后，当前
  页面约一个轮询周期内自动切换为 `已发布到 Obsidian`；
- 使用另一个纯合成任务和同名普通文件制造写入路径失败。失败时原文件内容逐字不变，正式目标和
  隐藏临时目录均不存在，Worker 任务保持 `processed` 且没有回执；页面只提供
  `重新检测并发布`，没有 `稍后处理`；
- 移除合成阻挡条件后，仅在 Obsidian 内点击重试即完成写入、最终校验和 acknowledgement。两条任务
  的目标目录都恰好包含七个文件，并与 Worker 私有包逐文件一致；
- 四张正式合成截图新增为 `docs/design/stage-i/images/25-*.png` 至 `28-*.png`；完整结论已写入
  `docs/design/stage-i/host-validation-v1.md`；
- 全程只使用当前书房 Mac、唯一 Obsidian 实例、同一个独立测试 Vault 和纯合成数据；没有正式 Vault、
  真实音频、远程笔记本、第二个 Obsidian、发布管理页或 Stage J；
- 本轮没有修改产品代码，Git 忽略的测试夹具只新增两条合成任务。未经项目所有者明确要求，不 commit
  或 push。

### 下一步严格边界

1. 发布恢复边界已经完成，不重复 Stage G/H、逐字稿/总结复核、自动/冲突发布或本轮恢复截图；
2. 仍留在 Phase 4 / Stage I，下一批先做本地个人 Alpha 收口审计：按路线图逐项核对同机提交、处理、
   复核、重新生成和发布是否都能从 Obsidian 到达，并补齐剩余的无障碍与主题一致性检查；
3. 在审计完成前不进入 Stage J/远程个人 Alpha，不启动远程笔记本验证，不新增发布管理页，也不扩展
   `稍后处理`、覆盖、逐条合并或复杂历史；
4. 继续优先使用合成数据和同一个独立测试 Vault。任何真实音频本地 Alpha 必须另行得到项目所有者
   明确授权，不复用或上传现有私有样例；
5. 私有 Vault、音频、逐字稿、Note、数据库、凭据、`reference/`、`tmp/` 与构建产物不得加入 Git；
   未经项目所有者明确要求，不 commit 或 push。

## 45. 2026-08-09 Stage I 本机个人 Alpha 收口审计继续

### 本轮完成

- 先把上一批发布恢复验证提交并推送到 `agent/worker-core`，提交为
  `bc652e0 Document Stage I publication recovery validation`；随后按第 44 节继续本机个人 Alpha
  收口，没有进入 Stage J；
- 审计确认同机新建、提交、任务控制、渐进逐字稿、证据复核、Note 重新生成和 Vault 发布的主要
  链路均已从 Obsidian 接通；过时的 Stage I 工作项勾选和插件 README 已按真实状态更新；
- 修复逐字稿当前段草稿在右栏收起、响应式断点或普通重绘时丢失的问题。草稿按任务/片段隔离；
  右栏恢复把手显示琥珀点并有 `当前片段有未保存的修订` 可读名称；窄窗打开编辑器和保存后的焦点
  路径已补齐；
- 补齐标准进度、低频稳定段播报、发布状态播报、错误焦点、选择状态、常用目标尺寸和减少动态效果；
  来源文件区去掉无实际动作的空焦点；
- Worker 不可达页新增 `查看安装或启动说明`。弹窗不猜测具体原因，只引导打开 Worker Manager
  处理安装、启动、模型、磁盘或内存状态，再回到 Obsidian 点击 `重新检测`；没有静默切换设备或
  `稍后再试`；
- 在唯一独立测试 Vault 中输入纯合成 `聚衣堂` 背景草稿并完成深色→浅色→深色实时切换，草稿完整
  保留；使用 Obsidian 原生单实例重新加载验证新构建和帮助弹窗焦点，没有启动第二个 Obsidian；
- 新增两张纯合成截图 `29-intake-light-theme-host-v1.png` 和
  `30-worker-unreachable-help-host-v1.png`，并新增
  `docs/design/stage-i/local-alpha-closeout-audit-v1.md`；
- 完整回归通过：Worker 既有 443 项测试、Ruff、协议生成漂移；插件 `50 passed`、严格类型检查、
  生产构建和差异格式检查。

### 当前严格边界

- 本轮没有启动或连接 Worker，没有创建 Secret/Vault 授权，没有运行真实音频、打开正式 Vault、
  启动远程笔记本或第二个 Obsidian；独立测试 Vault 继续位于 Git 忽略的 `tmp/`；
- Stage I 主要产品链路已经实现，但不能标记为最终完成。仍有三道门：
  1. 明确本机运行边界：现有独立运行时和 launchd 核心已完成，但原生 Worker Manager 尚未构建；
     需决定由开发侧一次性安装后只用 Obsidian，还是现在新增最小原生 Manager；
  2. 最终安装形态的全键盘主路径与 200% 缩放人工检查；
  3. 项目所有者另行指定并明确授权的一条非仓库真实录音本机端到端验收；
- 当前审计与无障碍补缺是本地未提交修改。上一批 `bc652e0` 已推送，但本节新增代码、文档和截图
  尚未 commit/push；未经项目所有者明确要求不得自行提交。

### 下一步严格执行顺序

1. 不重做 Stage G/H、逐字稿/总结复核或发布宿主流程；先只读确认 Worker 仍停止、测试 Vault 没有
   Secret/Vault ID；
2. 当前停在一个需要项目所有者决定的范围边界：`apps/worker-manager/README.md` 明确原生 Manager
   尚未构建，只有开发 CLI，而路线图把精细化 SwiftUI Manager 放在 Phase 7。推荐保持既定路线图，
   由开发侧一次性安装现有打包运行时，随后本机 Alpha 全程只用 Obsidian；如果要求现在即有图形化
   Manager，则需明确批准一个最小原生 Manager 新实现批次；
3. 完成全键盘主路径与 200% 缩放检查，修改只限 Stage H 已批准行为和视觉；
4. 到真实音频门前必须停止并向项目所有者说明需要哪一条录音、会在私有 Vault 产生什么；只有得到
   明确授权后才运行一次本机完整链路。真实音频、逐字稿、Note、数据库和凭据不得进入 Git；
5. 上述门槛关闭前不进入 Stage J、远程个人 Alpha、发布管理页或复杂版本管理；未经明确要求不
   commit/push。

## 46. 2026-08-09 Stage I 本机打包 Worker 安装、配对与重启恢复完成

### 本轮完成

- 项目所有者确认采用第 45 节推荐边界：Stage I 由开发侧一次性安装现有打包 Worker，用户随后只在
  Obsidian 操作；不提前新增最小原生 Manager，精细化 Manager 继续留在 Phase 7；
- 安装前只读核对无同名服务、默认数据和端口占用。首次安装的 2026-08-02 旧包缺少
  `review_audio_ranges` 与 `worker_readiness`，Obsidian 正确显示不兼容，没有降低插件要求绕过；
- 从当前源码重建并完整验证独立运行时，安装副本通过健康检查、Stage I 全能力协商、launchd 正常
  重启和异常退出自动拉起。旧运行时备份在新包验收完成后移入废纸篓，可恢复；
- 使用同一个独立测试 Vault 完成首次本地配对。accuracy 所需四类已存在模型均通过完整哈希校验并
  激活，没有下载模型或运行音频；Obsidian 从 `需要连接` 经 `尚未准备好` 恢复到 `已就绪`；
- 真实宿主发现阻塞状态只有处理说明、缺少资源准备后的恢复动作。现已增加 `重新检测`，插件
  `50 passed`、严格类型检查和生产构建通过；
- launchd Worker 重启后，健康端点、模型激活、Vault scope 与 Obsidian Secret 均恢复；Obsidian
  完整重载仍显示 `已就绪`，没有再次配对。新增纯合成截图 `31` 至 `33`，记录见
  `docs/design/stage-i/host-validation-v1.md`；
- 收尾已通过 Obsidian Secret Storage 清除测试 Secret、清空测试 Vault ID，并撤销 Worker 测试设备；
  当前有效凭据和未消费配对会话均为 0。独立 Alpha Worker 的任务、上传、修订、租约和回执均为 0；
- 已安装 Worker 继续在本机 loopback 运行，供下一轮本机 Alpha 检查使用。没有真实音频、正式 Vault、
  第二个 Obsidian、远程笔记本或 Stage J；当前第 45/46 节相关代码、文档和截图尚未 commit/push。
- 最后一次状态核对发现可选 `pyannote/speaker-diarization-3.1` 缓存尚不存在。既有架构允许转写继续、
  说话人归属降级，但原 readiness 会误显示完全就绪；现已改为非阻塞
  `SPEAKER_DIARIZATION_MODEL_MISSING` warning，Obsidian 明确提示多人归属可能需要人工复核；
- 含该修正的运行时已再次重建、清单验证并替换安装副本，manifest 为
  `498885ca71ef2c5db003ede0c32aae4db5c9142263cee547253cf0e14dbcba9d`；launchd 以新进程恢复健康，测试
  设备仍保持撤销。当前回归为 Worker `444 passed`、插件 `50 passed`；Mac 随后自动锁屏。

### 下一步严格执行顺序

1. 不重做 Stage G/H、逐字稿/总结复核、发布宿主流程或本轮安装配对；不重建运行时；
2. 继续 Phase 4 / Stage I，使用当前已安装的本机 Worker 和同一个独立测试 Vault，先完成全键盘主
   路径与 200% 缩放人工检查，并补齐说话人模型缺失 warning 的最终宿主截图；修改只限 Stage H
   已批准的行为、焦点和视觉；
3. 全键盘/缩放检查完成后，到真实音频门前必须停止。向项目所有者说明将使用哪条非仓库音频、哪个
   私有 Vault、会生成什么私有文件；只有得到明确授权后才重新配对指定 Vault 并运行一次本机
   提交→处理→复核→重新生成→自动发布；
4. 真实音频、逐字稿、Note、数据库、凭据、正式 Vault、`reference/`、`tmp/` 和构建产物不得加入
   Git；不进入 Stage J、远程个人 Alpha、发布管理页、复杂版本管理或原生 Manager；
5. 未经项目所有者明确要求，不 commit 或 push。用户已明确授权以后同类独立测试 Vault 配对在既定
   测试范围内直接执行，无需再次询问；涉及真实 Vault/真实音频或扩大授权范围仍必须取得明确授权。

## 47. 2026-08-09 Stage I 最终 warning、200% 缩放与全键盘检查完成

### 本轮完成

- 严格从第 46 节继续，只使用当前已安装的 loopback Worker、同一个独立测试 Vault 和唯一 Obsidian
  实例；没有重做 Stage G/H、逐字稿/总结/发布流程，没有真实音频、正式 Vault 或 Stage J；
- 临时配对后，真实宿主确认缺失可选说话人模型时显示 `这台 Mac · 已连接 · 需注意`，正文明确
  Worker 仍可转写、多人说话人归属可能需要人工复核，并保留 `重新检测` 和 `查看处理说明`；截图为
  `34-speaker-warning-host-v1.png`；
- 精确设置宿主缩放因子为 200% 后发现窄窗仍有固定高度和嵌套滚动，导致状态卡重叠、表单与确认栏
  被压缩。现已把窄窗页头、工作区和面板改为自然高度与单一页面滚动；截图 35–36 证明内容连续、
  无重叠；
- 全键盘首轮看似在日期控件后中断。DOM 核验确认控件均可聚焦，进一步定位为每三秒任务轮询即使
  任务列表完全相同也整体重绘页面，从而替换当前焦点。新增 `sameJobListPresentation`，普通轮询在
  任务卡展示无变化且没有错误恢复时不重绘，并增加对应确定性测试；
- 修复后 200% 下焦点连续经过 Worker 状态、重新检测、处理说明、准确优先、速度优先、内容类型和
  取消按钮，跨轮询不丢失，焦点框清晰；截图为 `37-intake-200-percent-keyboard-focus-host-v1.png`；
- 插件回归为 `52 passed`，严格类型检查和生产构建通过；Worker 本轮未再改源码，沿用第 46 节
  `444 passed` 基线；
- 收尾已把 Obsidian 缩放恢复为 100%，清除测试 Vault Secret 与 Vault ID，并撤销本轮设备授权；
  Worker 有效凭据和未消费配对会话均为 0，任务、上传、逐字稿、修订和发布回执仍全部为 0；
- 当前修改仍未 commit/push。Git 忽略的测试 Vault 与仓库外 Worker 数据、凭据均不得加入 Git。

### 下一步严格边界

1. 本机运行边界和最终无障碍检查已经关闭，不重做安装、配对恢复、warning、200% 或全键盘截图；
2. 现在必须停在真实音频授权边界。下一步只有在项目所有者明确指定一条非仓库真实录音和用于接收
   私有产物的 Vault 后，才执行一次本机提交→处理→复核→重新生成→自动发布验收；
3. 开始前先向项目所有者说明所选音频、目标 Vault 和会生成的私有七文件产物；未经明确授权不得
   运行。验证结束后清除/撤销临时授权，并检查私有数据未进入 Git；
4. 该验收通过后才能判断 Stage I 完成；此前不进入 Stage J、远程个人 Alpha、发布管理页、复杂
   版本管理或原生 Manager；
5. 未经项目所有者明确要求，不 commit 或 push。私有 Vault、音频、逐字稿、Note、数据库、凭据、
   `reference/`、`tmp/` 和构建产物不得加入 Git。

## 48. 2026-08-09 多人自动识别门槛完成

### 本轮完成

- 项目所有者明确要求在提供真实音频前先完成多人自动识别。本轮因此仍留在 Phase 4 / Stage I，
  没有运行真实音频、打开正式 Vault、启动第二个 Obsidian 或进入 Stage J；
- 本机 Worker 外置模型目录现已完整安装并识别五项模型缓存，其中说话人相关模型固定为
  `pyannote/segmentation @ 660b9e20307a2b0cdb400d0f80aadc04a701fc54` 与
  `pyannote/speaker-diarization-3.1 @ 84fd25912480287da0247647c3d2b4853cb3ee5d`；模型和缓存都在仓库外；
- 排查并修复独立运行时遗漏动态导入模块和资源的问题。构建现在显式收集 MLX ASR、pyannote 子模块、
  MLX Metal 库和 pyannote 配置数据，并在构建验收中运行 `verify-model-runtime`，任何 ASR、强制对齐、
  VAD 或说话人模块导入失败都会直接阻止产出运行包；
- 原 ASR 时间戳字符流此前会被合成约 30 秒的粗段，一段可能跨过换人边界。本轮只在派生稳定逐字稿
  时把 ASR 完整文本中的标点恢复到时间戳字符流，并按句末标点生成较细段落；同一 ASR 分块内不超过
  1 秒的自然句间停顿计入前一句时间范围，避免把正常停顿误报为证据缺口。原始 ASR JSON 和原始音频
  仍保持不可改写；
- 用仓库外 120.468 秒、两种系统合成声音、三轮交替发言的 WAV 从上传开始完整跑通最终打包运行时：
  5 个 ASR 分块形成 23 个主要句子，时间线只剩预期的 5 个跨块/尾部缺口；PCM 自动确认 3 段静音，
  VAD 和缺口重转写处理其余范围后，时间线覆盖达到 100%；
- 最终 pyannote 结果为 2 位匿名说话人、14 个实际说话区间、25 个已转写段全部归属、0 个说话人不可用
  段。三轮已知换人边界的每个句子都正确映射为 `speaker_01` 或 `speaker_02`，没有句子跨越换人边界；
- 最终独立运行包为 3,405 项、普通文件总计 1,298,266,873 bytes，manifest SHA-256 为
  `5e5ba6f9beffc9dacad673aec63dd63ca5e97287c09815db5a648a1fb4816fd3`。安装副本与构建产物逐项一致，
  launchd Worker 已恢复运行，五项模型状态均为 ready；旧运行包保留在仓库外的可恢复备份；
- 新增和更新了打包依赖、自检、标点恢复、句级分段与短停顿边界测试；Worker 完整回归基线更新为
  `448 passed`，Ruff 通过。合成音频、隔离数据库、模型、运行包、备份和临时验证目录均未加入 Git；
- 当前代码和文档修改仍未 commit/push，等待项目所有者明确要求。

### 下一步严格边界

1. 多人自动识别的“模型存在、独立运行包可调用、完整音频可分人、文字段正确归属”门槛已经关闭，
   不再把 `说话人模型未准备好` 当作当前本机状态，也不重复本轮合成验证；
2. 下一步回到第 47 节已经等待的 Stage I 真实音频验收。由项目所有者提供一条仓库外多人录音并指定
   接收私有七文件产物的 Vault；实际运行前明确音频与 Vault 范围，然后执行一次本机
   提交→转写/分人→复核→重新生成→自动发布；
3. 真实验收要重点核对说话人数、匿名人物一致性、换人边界、每位人物观点、完整校订逐字稿、
   `speech-record.json`、`timeline.md` 与 `note.md` 的一致性；原始 ASR 必须保持不变；
4. 真实音频、逐字稿、Note、数据库、凭据、正式 Vault、`reference/`、`tmp/`、模型和构建产物不得
   加入 Git；验收结束后清除临时 Vault 授权并复核 Git 隐私边界；
5. 真实本机验收通过前不进入 Stage J、远程个人 Alpha、发布管理页、复杂版本管理或原生 Manager；
   未经项目所有者明确要求，不 commit 或 push。

## 49. 2026-08-09 第一条真实本机音频 Alpha 与后台执行边界

### 本轮完成

- 项目所有者明确授权一条仓库外真实短录音，并指定继续使用 Git 忽略的独立测试 Vault。本轮只处理
  这一条录音，没有打开正式 Vault、启动第二个 Obsidian、进入 Stage J 或把私有内容写入文档/Git；
- 真实提交暴露出已安装 Worker 只提供 API、不会自行推进队列的缺口。新增 restart-safe 连续后台
  执行器，由独立数据库连接顺序推进预处理、ASR、对齐、VAD/缺口重转写、说话人识别、结构化和
  产物生成；服务重启会先恢复中断任务，Obsidian 关闭后任务也能继续；
- 首轮实现暴露预处理完成后没有立即进入 ASR、会重复预处理的问题，已修复并增加确定性回归测试；
- 真实停顿复核与后台线程并发时，旧线程曾把已经推进的任务误标失败。失败落盘现在必须同时匹配
  后台线程观察到的 job revision 和 state；任务已被其他操作推进时只忽略过时异常，不覆盖新状态；
- 同一真实任务从原检查点恢复，没有重新上传或改写原始 ASR。说话人模型完成真实运行，单人录音
  得到 1 位匿名说话人、10 个有效转写段全部归属、0 个未归属段；
- 准确优先结构化和六文件内容包加清单均已完成，Obsidian 在三次自动重连失败后通过页面内
  `重新连接这台 Mac` 恢复，并自动无冲突发布到测试 Vault；七文件逐项哈希与 Worker 清单一致，
  源文件与 Worker 保存副本哈希一致；
- 资源检查曾因磁盘安全余量暂停。只删除了 1.6 GB 可重建的 PyInstaller 临时 `build/` 目录，没有
  删除音频、模型、Worker 数据、Vault 或已安装运行包，随后同一任务从暂停点继续；
- 最终独立运行包仍为 3,405 项、1,298,274,521 bytes，manifest SHA-256 为
  `e8a1b606175ddc5eaa27c3101d93cd7b014b2e62953b07adb17bfa38ebc8e07e`；安装副本与构建产物逐项一致；
- Worker 全量 `452 passed`，Ruff 和差异格式检查通过；
- 当前修改尚未 commit/push。真实音频、Worker 数据库、私有逐字稿/Note、测试 Vault、凭据、模型、
  运行包、备份和 `tmp/` 均未进入 Git。

### 当前未关闭问题与下一步严格顺序

1. 第一条真实录音尚不能判为完全通过：普通环境底噪下的自然停顿仍需要开发者显式缺口复核，
   Obsidian 尚无对应用户入口；缺口重转写还在一个停顿边界产生了一个极短可疑单字。最终 Note 已
   正确提炼主要内容，但完整校订逐字稿仍需关闭这个边界噪声；
2. 先为自然停顿建立保守、可审计的自动处理边界，并阻止低覆盖率的缺口重转写微片段进入派生
   逐字稿；保留原始 ASR 不可改写，不能通过直接编辑 Vault 文件或数据库来掩盖问题；
3. 用合成夹具补齐该边界测试，运行 Worker 全量回归、Ruff 和隐私检查；随后只重做当前真实任务
   必需的下游派生阶段并重新核对七文件一致性，不重新上传或覆盖原始 ASR；
4. 第一条录音通过后再向项目所有者报告，并等待第二条音频路径。没有通过前不请求或处理第二条，
   不切换正式 Vault；
5. 不进入 Stage J、远程个人 Alpha、发布管理页、复杂版本管理或原生 Manager；未经明确要求不
   commit/push。

## 50. 2026-08-09 第一条真实样例的自然停顿规则完成

### 本轮完成

- 按项目所有者要求，先将上一批连续后台处理与多人运行时代码提交并推送到
  `agent/worker-core`，提交为 `5785066`；随后才继续本节工作；
- 新增保守的自然停顿自动物化：VAD 明确无语音时可直接形成可审计的 `non_speech` 片段；VAD 只在
  已有文字边缘检测到不超过 350 ms 的残余声时，还必须同时存在同范围缺口重转写失败/拒绝证据，
  才能把它判定为自然停顿；内部存在语音的范围继续留给人工复核；
- 新增低覆盖边界微片段拒绝规则：至少 1 秒的缺口中，若 ASR 只在首尾产生总计不超过 350 ms、
  覆盖率不超过 10%、正文不超过 4 字的碎片，则保存原始私有证据和拒绝检查点，但不写入派生稳定
  逐字稿；原始 ASR 和原始音频始终不改写；
- `JobStore` 在落盘自然停顿前重新核验当前对齐报告、完整未覆盖范围、VAD 证据和必要的重转写证据
  身份，任何证据过时或范围变化都会 fail closed；连续后台执行器会在缺口重转写后自动执行该规则；
- 合成测试覆盖边界单字拒绝、明确静音、幂等重放、内部语音拒绝和不可变 ASR 下游修订。Worker
  全量回归更新为 `456 passed`，Ruff 与差异格式检查通过；
- 对第一条真实任务已经保存的私有检查点做了只读复算：7 个缺口中 1 个为明确无语音、6 个为安全
  边缘残余、0 个需要人工复核；此前成功进入派生逐字稿的 4 个极短缺口结果都会被新规则拒绝。没有
  重跑真实音频、重新上传或改写原始 ASR，也没有把任何私有文字或标识写入文档；
- 新独立运行包为 3,405 项、1,298,286,841 bytes，manifest SHA-256 为
  `42b5da737a75a44675da6cc691c9a83ec43bdd9145b47dadfca3e687b502be1c`；构建与安装副本逐项一致，
  launchd Worker 已恢复运行，健康、Stage I 能力和 accuracy 模型完整哈希验证通过；
- 新增正规的不可变 ASR 下游修订命令：从已处理/已发布任务复用同一个已验证上传，复制并逐项校验
  原始 ASR 证据，在新任务中只重建派生逐字稿、分人、提炼、产物和发布；操作具备幂等键、来源
  检查点和 `fresh_asr_inference=false` 审计，不修改旧任务或旧发布目录；
- 第一条真实任务已通过该路径产生新任务。两份原始 ASR 哈希与旧任务逐项一致；新逐字稿包含 6 个
  正文片段、7 个自然停顿、1 位说话人且正文 0 个未归属，4 个旧边界微片段在逐字稿、记录、时间线
  和 Note 中均不存在；
- 新任务完成准确优先提炼并由真实 Obsidian 宿主自动无冲突发布到新位置。Worker 与 Vault 七文件
  逐项一致，清单内 6 个内容文件哈希全部通过，所有正文块均可由逐字稿定位；Note 与时间线非空且
  覆盖录音的两个主题。旧七文件目录和旧清单哈希保持不变；第一条真实音频现可判为通过；
- 为满足 10% 磁盘安全预留，删除了可重建的旧 Worker 备份/构建产物、插件 `node_modules`、项目
  `.venv`、本项目不再使用的两份旧 Whisper 缓存和若干开发/浏览器缓存；Qwen、pyannote、Ollama、
  音频、数据库和两个 Vault 包均保留。下轮运行源码测试前需先用锁文件恢复 `.venv`；
- 收尾已撤销测试设备凭据、清空测试 Vault 的持久 Vault ID；有效凭据和未消费配对会话均为 0。
  本节代码和文档尚未 commit/push。

### 当前边界与下一步严格顺序

1. 第一条真实音频已经通过，不重跑其 ASR、不覆盖或删除旧/new 两个历史包；下一步等待项目所有者
   提供第二条仓库外音频路径，仍先使用同一个独立测试 Vault；
2. 收到路径后可按既有授权直接重新配对测试 Vault，执行一次正常的新建→上传→自动处理→分人→
   提炼→自动发布，不再使用下游迁移命令；重点核对真实多人数量、换人边界、各人物观点和 Note；
3. 第二条验证前先用锁文件恢复已清理的开发 `.venv`，运行当前未提交代码的全量回归与隐私检查；
   磁盘仍必须满足 Worker 安全线，不能通过降低资源阈值绕过；
4. 第二条通过前不切换正式 Vault，不进入 Stage J、远程个人 Alpha、发布管理页、复杂版本管理或
   原生 Manager；私有音频、逐字稿、Note、数据库、凭据、`reference/`、`tmp/`、模型和构建产物
   不得加入 Git；
5. 未经项目所有者再次明确要求，不提交或推送本节尚未提交的代码和文档。
