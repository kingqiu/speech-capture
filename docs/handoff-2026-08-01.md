# Speech Capture 交接记录 · 2026-08-01

> 用途：下一次恢复开发时的入口
> 当前分支：`agent/worker-core`
> 最新提交：`8738dd7 Harden forced alignment for real audio timestamps`（已推送）
> 测试基线：`237 passed`
> 工作区：应保持干净

## 1. 今天完成的内容

- 下载 Ollama 模型：
  - `qwen3:14b`（9.3 GB，默认准确配置）
  - `qwen3:8b`（5.2 GB，低资源备用）
- 新增真实录音流水线脚本 `services/speech-worker/scripts/run_real_pipeline.py`
- 真实 ASR 可读性修复：
  - 零时长/单字时间戳按停顿、长度、句末标点合并成可读段落；
  - `07-28` 录音 106 块全部转写，生成 188 个可读段落；
- 强制对齐真实模型适配：
  - int16 → float32；
  - 词时间夹紧到段长；
- 对齐后未覆盖区间从约 1374 秒降到约 388 秒；
- VAD 证据：剩余 189 个缺口中 11 个无语音（29 秒）、178 个检测到语音
  （约 359 秒）。
- 新增 VAD 语音缺口定点重转写：`GapRetranscriptionExecutor` +
  `retranscribe-gaps`，带原始证据、失败记录和幂等重放。

## 2. 真实任务当前状态

- 录音：`07-28 智能体与知识库平台建设规划.mp3`
- runtime：`services/speech-worker/runtime/real-20260801-c/`
- job：`job_08c08aaf134346eeaedc7c0c3bf12ddb`
- 状态：`aligning`（尚未到 `diarizing` / `processed`）
- 可读文字稿：`runtime/real-20260801-c/transcript-readable.md`
- 阻塞点：178 个 VAD 检测到语音的缺口还没有文字证据，需要定点重转写。

## 3. 下一步

1. 对真实 `07-28` 录音运行 `retranscribe-gaps`，补满语音缺口；
2. 刷新对齐后继续：说话人识别 → Ollama 提炼 → 生成产物；
3. 之后再进入阶段 E（Vault 发布协议）。

## 4. 敏感数据

- 真实音频、模型缓存、runtime 产物全部在本机 Git 忽略目录；
- Hugging Face token 只存钥匙串；
- 提交前检查 `git status` 和 token 模式。
