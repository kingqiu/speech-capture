# Speech Capture 当前进展与后续开发计划

> 最后更新：2026-08-01
> 用途：作为项目暂停、恢复开发和阶段验收时的交接文档
> 当前阶段：后端个人 Alpha 开发中；Obsidian 前端尚未开始实现
> 当前分支：`agent/worker-core`
> 本次恢复基线：`682b64f Add evidence-only VAD gap analysis`
> 当前 Draft PR：`#2`
> 当前自动化测试：`265 passed`
> 今晚恢复专用交接文件：[handoff-2026-07-31.md](handoff-2026-07-31.md)
> 今日恢复专用交接文件：[handoff-2026-08-01.md](handoff-2026-08-01.md)

## 1. 状态说明

本文使用以下四种状态，避免把“已经讨论过”误认为“已经写完代码”：

- **[已实现并验证]**：代码已经存在，并通过相应的自动化测试或真实模型验证。
- **[设计已确定]**：产品或技术方案已经讨论并接受，但实现尚未完成。
- **[待开发]**：属于个人版本的必要工作，尚未实现。
- **[后续扩展]**：明确不属于首个个人版本的必做范围。

## 2. 一句话现状

Speech Capture 已经从“产品构想”推进到一个具备持久化基础的本地 Worker 后端：

- 已能安全接收并校验大音频；
- 已能把音频确定性地标准化和切块；
- 已能调用本机的 MLX Qwen3-ASR 模型逐块转写；
- 已能保存不可变的原始模型结果和逐步出现的稳定文字；
- 已能在进程中断或电脑重启后保留进度并继续；
- 已有磁盘、内存和单任务调度保护；
- 已验证本机真实运行 Qwen3-ASR 1.7B；
- 但尚未完成说话人区分、内容类型判断、总结提炼、最终笔记产物、网络 API、后台系统服务和 Obsidian 交互页面。

因此，当前成果是“可靠的转写后端骨架和单块 ASR 执行链路”，还不是可以安装使用的完整插件。

---

## 3. 已经确定的产品边界

### 3.1 核心目标

**[设计已确定]**

首个个人版本必须同时满足两个最重要的质量目标：

1. **原始内容尽可能完整保存**
   - 原音频不被修改；
   - 音频中所有能够解码的时间范围都必须有明确处理结果；
   - 不能因为模型返回空内容、出错或重启而静默丢失一段音频；
   - 原始转写证据必须独立保存，不能只留下总结。

2. **提炼出的信息有效、可追溯**
   - 重要结论、行动项、决定和事实应能回到对应的文字证据；
   - 总结不能把推测写成事实；
   - 不确定、分歧和听不清的内容必须保留；
   - 结构化笔记要便于人阅读，也要便于未来 Agent 使用。

### 3.2 本地优先

**[设计已确定]**

- 默认使用用户自己拥有的 Apple Silicon Mac 上的模型处理。
- 云端 API 只作为将来的可选容错能力。
- 云端能力默认关闭，不得在用户不知情时上传音频或文字。
- Worker 协议不绑定某一家模型供应商，未来可以增加其他本地或云端后端。

### 3.3 远程使用方式

**[设计已确定]**

- 外出笔记本上的 Obsidian 插件可以把任务提交给家里的 Mac Worker。
- 处理发生在家里的 Mac，不需要远程控制家里电脑的图形界面。
- 上传完成后，即使外出笔记本关闭 Obsidian或断网，家里的 Worker 仍应继续处理。
- 重新连接后，插件从持久化快照恢复进度和已经完成的文字。
- Tailscale 是推荐的安全连接方式，但不能写死为唯一方案。
- Google Drive、iCloud、Dropbox、Obsidian Sync 等只负责 Vault 或文件同步，不承担任务协议。
- 用户更换同步方案时，插件和 Worker 的核心处理链路不应受影响。

### 3.4 电脑重启和登录边界

**[设计已确定]**

- 普通 Worker 服务重启后不应要求重新配对。
- 配对凭据应长期保存，并支持用户主动撤销。
- 后台服务应尽可能通过 macOS 系统服务自动启动。
- 如果磁盘加密要求用户先解锁系统盘，这是操作系统的安全边界，应用无法安全绕过。
- “无需图形界面”可以实现；“加密磁盘完全未解锁时仍访问用户数据”不能作为承诺。

### 3.5 多说话人内容

**[设计已确定，代码待开发]**

- 默认自动判断内容类型。
- 会议、访谈和其他多用户发言必须区分说话人。
- 初始结果可以使用“说话人 1、说话人 2”等匿名标签。
- 用户后续可把匿名标签改成真实姓名。
- 说话人识别失败时，不得丢弃已经完成的文字；应降级为“说话人未确定”并提示复核。

### 3.6 处理完成与发布到 Vault

**[设计已确定]**

- `processed` 表示 Worker 已完成处理并形成可校验的产物。
- `published` 表示产物已经成功、完整地写入当前 Obsidian Vault。
- 两者不能混为一谈。
- 远程处理完成时，如果家里 Mac 上的 Vault 同步副本可用，可以由 Worker 或获得发布租约的客户端发布。
- 如果发布失败，处理结果必须仍保留在 Worker 中，之后可以重试。

### 3.7 输出目录和日期

**[设计已确定]**

- 默认输出根目录为 Vault 相对路径 `Work/Speech Notes/`，并允许配置。
- 不把任何一台电脑上的绝对路径写入协议或笔记。
- 录音日期优先级：
  1. 用户明确指定；
  2. 可靠的媒体元数据；
  3. 可识别的文件名日期；
  4. 文件创建时间仅作为建议；
  5. 都不可靠时进入 `Undated`，等待用户确认。
- 文件上传或处理当天的日期不能自动冒充录音发生日期。

### 3.8 人工内容保护

**[设计已确定]**

- 生成内容与用户手写内容分区保存。
- `我的补充` 属于用户内容，重新总结或重新生成时不能覆盖。
- 说话人命名、转写纠正、录音日期纠正和总结重生成都要可追踪、可撤销。

### 3.9 暂缓的功能

**[后续扩展]**

- 访谈内容的深层意图识别。
- Windows 和 Linux Worker。
- 移动端直接提交。
- 可选云端 ASR、说话人识别或总结供应商。
- 翻译产物。
- 跨笔记知识整合和更复杂的 Agent 工作流。

访谈意图识别已明确记录为后续扩展，首个个人版本先不实现。

---

## 4. 已经完成的项目与文档基础

### 4.1 仓库和工程结构

**[已实现并验证]**

- 项目名称：`Speech Capture`
- 建议的 Obsidian 插件 ID：`speech-capture`
- GitHub 仓库：`kingqiu/speech-capture`
- 已建立 Git 仓库、远程仓库、开发分支和 Draft PR。
- 已建立以下单仓库目录：
  - `apps/obsidian-plugin/`
  - `apps/worker-manager/`
  - `services/speech-worker/`
  - `packages/protocol/`
  - `docs/`
- 已配置 Python Worker 的 `uv` 环境、依赖锁定、测试和代码检查。
- 已加入隐私保护规则，私人音频、文字稿、凭据、运行数据库和本地报告不得进入 Git。

注意：在将来提交 Obsidian 社区插件之前，仍需重新检查 `speech-capture` 是否已被社区注册表占用。GitHub 仓库名不会自动保留 Obsidian 插件 ID。

### 4.2 许可证

**[已实现并验证]**

- 根目录已加入官方 `PolyForm Noncommercial License 1.0.0`。
- README 已用中文说明：
  - 个人学习、研究、测试和非商业用途可以使用；
  - 商业产品、商业服务、收费交付、企业内部商业运营、转售或其他商业获益用途，需要作者另行书面许可；
  - 该许可证不是 MIT，也不是允许商业使用的 OSI 开源许可证。

### 4.3 已经形成的设计文档

**[已实现并版本化]**

当前仓库已经包含：

- 产品需求；
- 系统架构和信任边界；
- Worker API 方向；
- Worker 持久化核心说明；
- 数据模型和输出产物；
- 安全、隐私和恢复策略；
- 测试策略；
- 本地模型验证报告；
- UI 方向；
- Obsidian 前端设计门；
- 决策日志；
- 路线图；
- 同类参考项目分析。

这些文档共同构成当前实现的设计基线。后续如果更改以下内容，应先更新设计文档：

- 证据和完整性语义；
- 隐私边界；
- 破坏性操作；
- 远程授权；
- 发布和恢复方式；
- 前端信息架构。

---

## 5. 已完成的模型验证

### 5.1 本地 ASR 模型

**[已实现并验证]**

已经在目标 Mac 上安装并运行：

- `mlx-qwen3-asr==0.3.5`
- 准确模式：`Qwen/Qwen3-ASR-1.7B`
- 速度模式：`Qwen/Qwen3-ASR-0.6B`
- 时间戳对齐：`Qwen/Qwen3-ForcedAligner-0.6B`

当前策略：

- 1.7B 作为默认“准确优先”配置；
- 0.6B 作为“速度和资源优先”配置；
- 后续在真实录音上继续比较两种配置。

### 5.2 合成双语基准

**[已实现并验证]**

已完成约 47.987 秒的中英混合合成音频基准：

| 配置 | 总耗时 | 实时系数 | 峰值进程内存 | 字符错误率 |
| --- | ---: | ---: | ---: | ---: |
| Qwen3-ASR 1.7B + 对齐器 | 29.62 秒 | 0.617 | 5.75 GB | 0.00% |
| Qwen3-ASR 0.6B + 对齐器 | 11.76 秒 | 0.245 | 3.53 GB | 1.05% |

两种配置均做到：

- 完整覆盖合成音频的解码时间线；
- 每个块都有结束原因；
- 没有检测到生成截断；
- 时间戳单调且在音频范围内；
- 保留中文—英文—中文切换。

### 5.3 Worker 中的真实模型执行

**[已实现并验证]**

Worker 的持久化执行链路已经真实调用 1.7B 模型两次，使用本地生成的中文测试语音：

> 这是语音捕捉插件的本地模型测试。我们需要完整保存每一段录音，并且生成可以核对的文字。

验证结果：

- 模型成功转写；
- 原始模型结果先写入私有、带校验和的文件；
- 随后才提交为插件可见的稳定文字；
- 处理进度达到完整音频时长；
- 作业进入后续对齐阶段；
- 重放路径不会重复调用模型。

### 5.4 尚未完成的模型验证

**[待开发]**

- 用户真实录音；
- 一小时或数小时长音频；
- 噪声、回声、低音量和手机录音；
- 多人重叠说话；
- 中文、英文和中英混合的真实对话；
- 真实说话人分离准确率；
- Ollama 总结模型的速度、内存和信息提炼质量。

---

## 6. 已完成的 Worker 后端能力

### 6.1 持久化作业状态机

**[已实现并验证]**

- 已定义严格、白名单式的作业状态转换。
- 每次状态变化都会增加版本号。
- 每个版本只有一条追加式事件。
- 非法跳转不会修改作业。
- 调用方需要提供自己看到的版本号，防止并发覆盖。
- 重复创建同一任务可通过幂等键返回原任务。
- 同一幂等键对应不同请求会明确报冲突。
- 阶段检查点独立保存，可跨暂停、失败、取消和重启保留。

代表性成功流程：

`created → uploading → verifying → queued → preprocessing → transcribing → aligning → diarizing/structuring → quality_check → processed → publishing → published`

同时支持：

- `paused`
- `waiting_user`
- `partial`
- `failed`
- `cancelled`

### 6.2 SQLite 持久化

**[已实现并验证]**

- 当前数据库 schema 版本：7。
- 当前包含 13 张核心表：
  - jobs
  - job_events
  - job_checkpoints
  - uploads
  - upload_parts
  - transcript_segments
  - provisional_transcripts
  - job_progress
  - job_updates
  - asr_attempts
  - corrections
  - publication_leases
  - publication_receipts
- 启用 SQLite WAL。
- 启用外键约束。
- 使用 FULL 同步持久化。
- 配置忙等待超时。
- 关键写入使用即时事务。
- 数据库文件和应用数据目录使用限制性权限。

### 6.3 重启恢复

**[已实现并验证]**

- Worker 异常停止后，正在处理的状态会恢复到可安全继续的队列状态。
- 正在发布的任务恢复为 `processed`，避免把未完成的发布误报为成功。
- 已经提交的转写段、临时尾部、进度、上传部件、原始 ASR 尝试和检查点都会保留。
- 已完成的模型尝试可以从原始证据重放，不需要再次推理。

### 6.4 资源安全检查

**[已实现并验证]**

磁盘策略：

- 必须保留“20 GiB”和“磁盘容量 10%”中的较大值。
- 在开始处理前估算源文件、标准化 PCM 和工作产物所需空间。
- 磁盘接近危险状态时给出警告或阻止处理。
- 系统只提示用户手工清理，不自动删除文件。

内存策略：

- 准确模式最低建议 16 GB；
- 速度模式最低建议 8 GB；
- 检查可用内存、内存压力和交换空间风险；
- 警告状态可以按规则继续；
- 严重状态在下一个 ASR 块开始前安全暂停；
- 已经完成的文字和模型结果不会丢失。

调度策略：

- 同一 Worker 同时只运行一个重型任务；
- 选择最早的、已经完整验证的队列任务；
- 每次开始调度都会持久化当时的资源检查结果；
- 未绑定可靠音频源的开发任务不会被正式调度。

### 6.5 大文件可恢复上传

**[已实现并验证]**

- 默认上传块大小为 8 MiB。
- 允许配置到最高 64 MiB。
- 一个上传最多 10,000 个块。
- 块编号从 1 开始。
- 支持乱序上传。
- 每块都校验精确字节数和 SHA-256。
- 相同内容重复上传是幂等操作。
- 同一块号出现不同内容会明确报冲突。
- 可以查询缺失块列表并续传。
- 最终组装时会重新校验所有已保存块。
- 校验总字节数和整个文件的 SHA-256。
- 使用 FFprobe 验证文件确实包含正时长音频流。
- 音频验证失败时不会创建可调度任务。
- 验证过程重启后可回到上传状态，已经收到的块仍保留。
- 文件使用私有目录、原子替换、同步写入和路径边界保护。
- 已防范符号链接和路径穿越。

### 6.6 渐进文字和重连快照

**[已实现并验证]**

稳定文字段支持四种明确结果：

- `transcribed`：成功转写；
- `inaudible`：无法可靠听清；
- `non_speech`：确认是非语音；
- `failed`：该范围处理失败。

当前约束：

- 稳定段按时间顺序提交；
- 不允许互相重叠；
- 不允许超出源音频范围；
- 相同提交键重复提交是幂等的；
- 相同提交键内容变化会报冲突；
- 后续时间对齐或说话人标注只能更新元数据，不能静默改写稳定文字；
- 每个作业最多有一个明确标记为“不稳定”的临时尾部；
- 临时尾部有独立版本保护；
- 进入对齐前必须清除或提交临时尾部。

插件未来重连时可获得：

- 作业当前状态；
- 当前阶段；
- 已处理时长和总时长；
- 阶段进度；
- 已耗时和可选剩余时间；
- 说话人识别状态；
- 稳定文字分页；
- 当前临时尾部；
- 资源检查报告；
- 后续更新游标。

边界保护：

- 每页最多 500 个文字段；
- 更新事件每页最多 1,000 条；
- 普通状态事件和更新事件不包含文字正文；
- 文字正文只通过受控快照接口返回。

### 6.7 确定性的音频标准化和切块

**[已实现并验证]**

- 已验证的源音频会被转换为私有的 16 kHz、单声道、16-bit PCM WAV。
- 转换副本移除不需要的元数据。
- 用户原始音频保持不变。
- 标准化文件有 SHA-256 和持久化计划。
- 标准化音频时长与媒体验证时长的差异不能超过 250 ms 或 1% 中的较大值。
- 如果标准化副本损坏，Worker 会从已验证源文件确定性重建。

切块策略：

- 默认每块最长 30 秒；
- 尾块最短目标 5 秒；
- 在边界前最多回看 3 秒；
- 使用 100 ms 能量窗口寻找较自然的切点；
- 所有边界以 PCM 帧为准；
- 块之间不重叠、不留空隙；
- 最后一块精确结束在最后一帧。

### 6.8 可恢复的本地 ASR 执行

**[已实现并验证]**

- 已有 `MlxQwenAsrEngine` 真实本地模型适配器。
- 模型按需要延迟加载。
- 每次后端命令执行一个持久化音频块。
- 每个块开始前重新做资源检查。
- 原始模型 JSON 先原子写入私有文件并计算校验和。
- 原始证据落盘后，文字才会变成稳定可见段。
- 尝试状态包括：
  - succeeded
  - rejected
  - failed
- 尝试编号从 1 开始且不能跳号。
- 尝试键和证据不可变。
- 数据库和普通事件只保存安全元数据，不暴露原始文字或异常消息。
- 校验模型输出是否覆盖当前块、是否截断、是否有结束原因、文字是否为空、时间戳是否单调且在范围内。
- 成功结果使用确定性提交键写成稳定段。
- 如果在原始结果落盘后崩溃，恢复时从该结果重放，不第二次调用模型。
- 被拒绝的模型输出可以重试。
- 模型异常只记录安全的异常类型，不保存可能含私人内容的异常消息。
- 默认最多尝试 3 次。
- 重试耗尽后把该时间范围明确记录为 `failed`，作业进入 `partial`，不会静默跳过。
- 对容器时长与 PCM 时长的毫秒级舍入差异已有安全夹紧处理。
- 已支持一次调用跑完所有剩余 ASR 块，可设置单次批次块数上限；
- 批次中断后可安全续跑，已经完成的块只重放证据，不会重复调用模型。

### 6.9 开发者命令行

**[已实现并验证]**

目前可以通过开发者命令完成：

- 初始化和检查数据库；
- 创建上传清单；
- 写入上传块；
- 查询缺失块；
- 完成文件组装和媒体验证；
- 从已验证上传创建作业；
- 运行调度；
- 标准化音频和生成切块计划；
- 执行下一个 ASR 块；
- 一次执行完所有剩余 ASR 块（可带单次批次块数上限）；
- 完成整篇对齐验收；
- 对下一个估算时间段执行不改写文字的受控强制对齐；
- 对验收报告中的未覆盖范围生成保守 PCM 证据；
- 用 revision-pinned VAD 为仍未决范围生成不授权落盘的评测证据；
- 用私有金标准清单生成不含路径和正文的 VAD 漏检/误报指标；
- 把默认保守策略证明的静音写入稳定时间线并刷新对齐；
- 用显式人工复核把一个完整当前缺口标为非语音或听不清；
- 查询作业、事件、快照和持久化结果。

这套命令是后端验证工具，不是最终用户界面。

### 6.10 整篇对齐和完整性退出门

**[已实现并验证]**

转写块全部落盘后，Worker 现在会生成一份持久化的
`transcript_alignment_report`，而不是因为最后一个模型调用成功就直接进入
说话人识别。

报告分别验证：

- 每个标准化音频块都有匹配的成功原始 ASR 尝试；
- 每个块都有持久化的 materialization 检查点；
- 检查点引用的稳定文字段仍然存在；
- 原始模型结果文件仍能通过 SHA-256；
- 每个已转写段使用已对齐时间，而不是估算时间；
- 稳定结果在整条源时间线上不重叠；
- 是否存在未覆盖范围；
- 是否存在 `inaudible` 或 `failed` 范围；
- 当前任务是否真的可以进入说话人识别。

报告只包含：

- 安全的状态布尔值；
- 分段和音频块数量；
- 各结果类型的数量和总时长；
- 未覆盖的起止时间；
- 稳定问题代码。

报告不包含文字正文。普通事件和诊断输出仍不会泄露转写内容。

当前行为：

- 原始证据、对齐、时间线和文字完整性全部通过时，任务进入 `diarizing`；
- 存在估算时间戳时，任务保留在 `aligning`；
- 存在未覆盖范围时，任务保留在 `aligning` 并列出精确范围；
- 原始尝试或 materialization 证据缺失时，任务不能继续；
- 已完成的验收跨 Worker 重启保持幂等，不重复调用模型，也不重复转换状态。

目前该验收门能够准确识别缺口；下一节的 PCM 分析只证明最保守的确定性
静音，不会把可听但不确定的内容猜成非语音。

### 6.11 未覆盖范围的保守 PCM 证据

**[已实现并验证]**

Worker 现在可以通过 `analyze-gaps` 读取最新
`transcript_alignment_report` 中的精确未覆盖范围，并在已校验的标准化
16 kHz 单声道 PCM 上生成持久化的 `gap_audio_evidence`。

每段证据记录：

- 源时间线和 PCM 帧起止位置；
- 时长、峰值、RMS、非零采样比例和安静窗口比例；
- `definite_silence` 或 `unresolved`；
- 稳定原因码；
- 来源对齐报告的 generation 和 payload SHA-256；
- 标准化音频 SHA-256。

默认只有不少于 100 ms、并且所有采样峰值都不超过近数字静音阈值的范围
才会标成 `definite_silence`。以下情况全部继续保留为 `unresolved`：

- 短于最小静音时长；
- 存在明显可听 PCM；
- PCM 范围不可用；
- 任何无法仅靠幅度安全判断的内容。

报告不含文字正文、文件名或绝对路径。同一份对齐报告和同一标准化音频重复
分析时保持幂等；对齐报告发生变化后，新证据会记录新的来源 generation 和
SHA-256。

`analyze-gaps` 只生成可审计证据，不自动写入 `non_speech` 稳定段，也不改变
作业状态。下一节的独立命令负责使用固定保守策略完成安全落盘。

### 6.12 确定性静音的稳定时间线落盘

**[已实现并验证]**

Worker 现在可以通过 `materialize-silence` 重新运行固定默认策略，并把当前
`definite_silence` 写成：

- outcome：`non_speech`；
- timing status：`aligned`；
- 无文字正文；
- 无语言、置信度或说话人归属。

落盘前会再次验证：

- Gap checkpoint 的 generation 和 SHA-256 仍是当前值；
- Gap checkpoint 仍绑定当前整篇对齐报告；
- 策略固定为 20 ms 窗口、至少 100 ms、峰值阈值 8；
- 精确范围仍标为 `definite_silence`；
- PCM 帧数、峰值、安静窗口比例和原因码一致；
- 源时间范围完整映射到 PCM，不能用被文件尾部截断的部分采样下结论；
- 范围不与任何稳定段重叠。

自定义阈值只用于诊断，不能授权稳定时间线变更。补写可以发生在已有文字段
之前或中间，但不会改写已有 segment ID、sequence cursor 或文字正文。每个
补写范围都有独立私有 checkpoint，绑定：

- Gap 报告；
- 来源对齐报告；
- 标准化音频 SHA-256；
- PCM 聚合证据；
- 新增的稳定 segment ID 和 commit key。

命令完成后立即重新运行整篇对齐，使剩余 `unresolved` 范围和当前作业阶段
保持最新。插入稳定段后即使在证据 checkpoint 或对齐刷新之前崩溃，下一次
调用也会使用相同 commit key 幂等修复。

### 6.13 显式人工复核的未决范围落盘

**[已实现并验证]**

Worker 现在可以通过 `review-gap` 把一个经过人工试听的、完整且仍为当前值的
未决范围写成：

- `non_speech + aligned`；或
- `inaudible + aligned`。

命令只接收不透明的 `review_key`、精确起止时间和结果类型，不记录复核者
身份，也不接收自由文本备注。落盘前会验证：

- 范围在当前 `transcript_alignment_report` 中完整出现且只出现一次；
- 复核证据绑定当前对齐报告 generation 和 payload SHA-256；
- 复核 key 从未绑定到其他范围或其他结果；
- 范围不与现有稳定段重叠；
- 结果只能是 `non_speech` 或 `inaudible`。

每次复核都有独立的证据和 materialization checkpoint。相同请求重复执行会
返回原稳定段；在稳定段写入后、checkpoint 或对齐刷新前中断，也能幂等修复。
写入后立即刷新整篇对齐。`inaudible` 会解释该段时间线，但仍保持
`transcript_complete = false`，不会把部分结果伪装成完整结果。

这是“明确人工证据”通道，不是自动分类器。空 ASR、音量或可听 PCM 本身仍
不能自动区分语音、音乐、噪声和听不清内容。

### 6.14 估算时间戳的受控强制对齐回退

**[已实现并验证]**

Worker 现在可以通过 `force-align-next` 处理当前对齐报告中的下一个
`UNALIGNED_TRANSCRIBED_SEGMENT`。每次只处理一个稳定段，并保证：

- 不重新转写，不改写文字；
- 不改变 segment ID、commit key、语言、置信度或说话人字段；
- 只把原范围的标准化 PCM 和现有稳定文字交给
  `Qwen/Qwen3-ForcedAligner-0.6B`；
- 模型工作前重新检查磁盘和内存边界；
- 无语言元数据时不猜测分词方式；
- 词单元必须完整对应稳定文字，且时间有限、单调、不重叠、不越界；
- 只有首词开始到末词结束的外边界会写回稳定段。

词级原始结果先以 `0600` 权限原子写入私有文件并计算 SHA-256，然后保存安全
checkpoint。Checkpoint 绑定：

- 强制对齐前的整篇报告 generation 和 payload SHA-256；
- segment ID、revision、原范围和文字 SHA-256；
- 标准化音频 SHA-256、采样率和 PCM 帧范围；
- 模型 ID、词数、对齐后外边界和原始证据 SHA-256。

如果证据落盘后、segment 元数据更新前中断，下一次调用会直接重放证据，不
再次调用模型。整篇退出门会在每次验收时重新读取并验证私有文件；仅手工把
状态改成 `aligned`、文件缺失、内容不完整或校验和变化，都会产生
`MISSING_FORCED_ALIGNMENT_EVIDENCE` 并阻止进入 `diarizing`。

强制对齐收紧外边界后新出现的首尾静音仍会作为精确未覆盖范围交给现有 Gap
证据流程处理，不会被自动吞掉。

### 6.15 自动分类候选的 VAD 证据边界

**[已实现并验证评测边界；分类决策待私有样本验证]**

Worker 现在可以通过 `analyze-speech-activity` 对当前仍为 `unresolved` 的
范围运行 revision-pinned `pyannote/segmentation` VAD 候选。模型必须绑定
完整的 40 位 commit SHA，证据同时记录 pyannote 版本和固定配置，并锚定：

- 当前 Gap 报告 generation 和 payload SHA-256；
- 当前整篇对齐报告 generation 和 payload SHA-256；
- 标准化音频 SHA-256、采样率和精确时间范围。

检测返回的语音区间必须有限、有序、不重叠、非空且不越过标准化音频。资源
不足会在模型调用前安全暂停，任何区间异常或模型错误都不会留下半份检测
报告。

本阶段只保存 `speech_detected` 或 `no_speech_detected` 的时间证据。报告和
每一个范围都明确保存 `materialization_authorized = false`，不会新增稳定
`non_speech` 或 `inaudible` 段，也不会改变作业状态。VAD 无语音结果不能单独
证明“非语音”，更不能区分“听不清”；自动落盘仍必须等待代表性私有标注样本
的误检、漏检评测和决策阈值批准。

### 6.16 私有 VAD 金标准评测与指标门

**[已实现并验证评测工具；真实基线仍待私有样本和模型]**

新增独立的 `speech-capture-vad-probe`，可以读取 Git 已忽略目录中的私有
manifest。每个样本只使用不透明 ID、相对音频路径，以及有序、不重叠的
`speech` / `non_speech` 毫秒范围；未标注时间不参与指标。

评测工具会在内存中把音频标准化为 16 kHz 单声道 PCM，并拒绝：

- 路径逃逸或符号链接音频；
- 评测期间变化的源文件；
- 超长、无法解码或空音频；
- 重叠、越界或类型未知的标注；
- 非法 VAD 检测区间；
- 只提供一部分验收策略参数。

报告按标注时长计算语音召回率、语音漏检率、非语音特异度、非语音误报率和
四类混淆时长，同时统计出现漏检或误报的样本数。报告只含不透明 ID、源文件
SHA-256 和指标，不含路径、文件名、文字稿或凭据，并以 `0600` 权限原子
写入。

项目没有内置拍脑袋阈值。只有项目所有者同时提供两项比率上限和两项最小
标注时长后，工具才给出 pass/fail；即使通过，
`automatic_materialization_authorized` 仍固定为 `false`。

---

### 6.17 匿名说话人归属

**[已实现并验证]**

- 已新增 `SpeakerDiarizationExecutor` 和 `run-diarization` 开发者命令；
- 模型固定为 `pyannote/speaker-diarization-3.1 @ 84fd25912480287da0247647c3d2b4853cb3ee5d`；
- 模型前执行资源检查，阻塞时安全暂停；
- 原始说话人轨迹先写入私有、带校验和的证据文件，再更新文字段元数据；
- 只改 `speaker_id` 和 `speaker_label_status`，不重写稳定文字；
- 匿名说话人按首次出现顺序映射为 `speaker_01`、`speaker_02` 等；
- 模型失败或返回空结果时降级为 `UNAVAILABLE`，文字不丢失，作业仍前进；
- 证据可跨重启重放，已完成的归属不会重复调用模型；
- 完成后作业进入 `structuring`，进度里的 `diarization_status` 变为 `ready`；
- 使用确定性假引擎完成 9 项自动化测试；
- 真实模型冒烟已完成：模型下载、加载、推理全部成功，在 60 秒真实录音
  片段上识别出 18 个说话人轨迹和 3 个匿名说话人。

---

### 6.18 内容类型判断与证据化信息提炼

**[代码已实现并验证，Ollama 真实模型下载待确认]**

- 已新增 `StructuringExecutor` 和 `run-structuring` 开发者命令；
- 内容类型 schema 固定为 `meeting`、`interview`、`course`、`speech`、
  `voice_memo`、`generic`；
- 自动分类记录 traits 和 confidence；
- 提炼按最多 6000 字符分批进行，避免长稿一次性超出上下文；
- 每个结论必须引用真实存在的 transcript segment，无证据项标记为
  `unsupported` 并单独计数；
- 相同结论按 kind + 文本合并，证据取并集、置信度取最大；
- 本地 Ollama 适配器默认 `qwen3:14b`，可切换 `qwen3:8b`；
- 模型不可用时降级为 `generic + 空结论`，不编造内容，作业仍前进；
- 证据先私有落盘并校验，跨重启幂等；
- 完成后作业进入 `quality_check`；
- 使用确定性假引擎完成 7 项自动化测试，并把合成多块端到端测试延伸到
  `quality_check`。

---

### 6.19 后端数据产物包

**[已实现并验证]**

- 新增 `ArtifactGenerator` 和 `generate-artifacts` 开发者命令；
- 生成四份后端产物：
  - `transcript.raw.json`：不可变原始 ASR 尝试汇总；
  - `transcript.md`：带稳定块 ID、时间范围、说话人和不确定标记的文字稿；
  - `speech-record.json`：机器可读记录，含内容类型、段、提炼结果、证据、
    质量和来源事实；
  - `note.md`：按内容类型渲染的结构化笔记，保护 `我的补充`；
- 额外生成 `artifact-manifest.json`，记录每个文件 SHA-256 和版本；
- 产物写入 Worker 私有目录，原子写入、`0600` 权限；
- 生成后作业从 `quality_check` 进入 `processed`；
- 已生成作业跨重启返回 `already_generated`，不重复生成；
- 使用确定性假引擎完成 4 项测试，并把合成多块端到端测试延伸到
  `processed`。

---

### 6.20 真实录音可读落盘与强制对齐适配

**[已实现并验证]**

- 发现真实 Qwen3-ASR 返回大量零时长/单字时间戳，直接落盘会变成不可读的
  单字段；
- ASR 落盘逻辑改为按停顿、长度和句末标点合并成可读段落，文字不丢失；
- 新增 `scripts/run_real_pipeline.py`，可对真实录音一键续跑完整流水线，
  并在 ASR 后自动生成可读文字稿预览；
- 真实 `07-28` 录音第一次跑通 ASR（106 块），生成 188 个可读段落；
- 强制对齐适配真实模型：
  - int16 PCM 自动归一化为 float32；
  - 对齐词时间夹紧到段长，避免越界；
- 对齐后未覆盖区间从约 1374 秒降到约 388 秒；
- VAD 证据显示剩余 189 个缺口中 11 个无语音、178 个检测到语音；
- 保守静音策略因录音底噪判定 0 个确定静音，剩余语音缺口需要定点重转写。

---

### 6.21 VAD 语音缺口定点重转写

**[已实现并验证]**

- 新增 `GapRetranscriptionExecutor` 和 `retranscribe-gaps` 开发者命令；
- 读取当前对齐报告和 revision-pinned VAD 证据，只处理 `speech_detected`
  的未覆盖区间；
- 对每个缺口读取精确 PCM 范围并调用本地 ASR；
- 原始结果先写私有校验和证据，再提交稳定文字；
- 支持安全暂停、失败记录和幂等重放；
- 完成后刷新整篇对齐，缺口补满即可进入 `diarizing`；
- 新增 4 项自动化测试。

---

## 7. 当前测试和质量验证

### 7.1 已通过的检查

**[已实现并验证]**

- Python 自动化测试：248 项通过。
- Ruff 静态检查通过。
- `uv` 依赖锁文件检查通过。
- Python 编译检查通过。
- Worker 源码包和 wheel 构建通过。
- Git 差异检查通过。
- 私人路径和凭据扫描通过。

### 7.2 当前自动化测试覆盖

包括但不限于：

- 作业状态转换和并发版本冲突；
- 幂等创建；
- 数据库重启恢复；
- 数据库版本保护；
- 上传分块、乱序、重试和冲突；
- SHA-256 校验；
- 媒体格式和音频流验证；
- 符号链接和路径边界；
- 磁盘和内存阻止/警告；
- 单重型任务调度；
- 稳定文字时序和不可变性；
- 临时尾部版本冲突；
- 快照分页和更新游标；
- 普通事件不泄露文字；
- 音频标准化、帧完整切块和损坏重建；
- ASR 原始证据先落盘；
- ASR 结果拒绝和重试；
- 崩溃后重放；
- 最大重试耗尽进入 partial；
- 真实 FFmpeg/FFprobe 路径；
- 真实 MLX Qwen3-ASR 合成中文语音路径。
- 整篇原始证据、时间对齐和时间线覆盖验收；
- 缺失原始证据阻止进入说话人识别；
- 估算时间戳阻止进入说话人识别；
- 未覆盖范围的持久化和无正文报告；
- 完整验收跨重启幂等；
- `finalize-alignment` 的机器可读输出。
- 确定性静音、短静音和可听 PCM 的保守分类；
- 对齐报告字段、范围顺序和总时长校验；
- Gap 证据对来源报告 generation/SHA-256 的锚定和幂等更新；
- 标准化音频篡改阻止 Gap 分析；
- `analyze-gaps` 的机器可读输出。
- 在已有文字之前和中间安全补写确定性静音；
- 自定义阈值、可听 PCM、过期报告和重叠范围不能授权落盘；
- 每段静音的独立证据 checkpoint 和幂等恢复；
- 静音落盘后自动刷新整篇对齐；
- 当确定性静音是最后缺口时通过整篇退出门并进入 `diarizing`；
- `materialize-silence` 的机器可读输出。
- 人工复核必须匹配一个完整当前缺口；
- 复核 key 幂等且不能改判或改绑范围；
- 过期对齐报告不能授权复核落盘；
- `inaudible` 解释时间线但保持文字不完整；
- 复核段落盘中断后的幂等修复；
- `review-gap` 输出和 checkpoint 不含正文或自由文本备注。
- 强制对齐保持稳定文字、segment ID 和 commit key 不变；
- 不完整、非单调和越界的词级证据不能修改稳定段；
- 资源不足时在模型调用前安全暂停；
- 私有词级证据使用 `0600`、SHA-256 和来源版本锚定；
- 证据落盘后中断可以不重复调用模型地恢复；
- 仅修改 `aligned` 标志而没有证据不能通过退出门；
- 私有证据被篡改后会让已完成的退出门失效；
- `force-align-next` 输出不泄露文字正文。
- VAD 模型使用完整 revision SHA，浮动 revision 被拒绝；
- VAD 区间异常、重叠或越界时整份证据拒收；
- VAD 证据绑定当前 Gap、对齐和标准化音频来源；
- 资源不足在模型调用前暂停，无缺口时不加载模型；
- `no_speech_detected` 不会直接变成 `non_speech` 或 `inaudible`。
- 私有 VAD manifest 的路径、标注顺序、类别和边界被严格校验；
- 指标按帧/时长汇总，不按样本简单平均；
- 不提供完整人工策略时不会生成通过结论；
- 私有报告不含音频路径或文件名，并使用 `0600` 原子写入；
- 即使评测通过也不能授权稳定时间线自动落盘。

### 7.3 仍需增加的测试

**[待开发]**

- 真实长音频；
- 多说话人和重叠发言；
- 对齐、说话人识别、总结和发布的完整流水线；
- Worker 运行中强制退出；
- 系统重启后的自动恢复；
- 网络中断、断点续传和重连；
- 磁盘突然不足；
- 内存压力突然升高；
- 模型文件损坏或缺失；
- 数据库和产物迁移；
- 多客户端竞争发布；
- Vault 同步冲突；
- UI 状态和无障碍；
- 安装、升级、降级和卸载。

---

## 8. 已经设计、但尚未写成代码的部分

以下内容不能算“已完成”，只是方案已经确定。

### 8.1 输出产物包

**[基础产物已实现；纯净 Note、证据版拆分与时间线摘要待开发]**

Worker 内部证据与用户侧 Markdown 必须分开。计划生成：

1. `transcript.raw.json`
   - 保存原始模型证据；
   - 包含模型、块、尝试、时间范围、校验和等；
   - 不能因为后来纠正文字而被覆盖。

2. `transcript.md`
   - 面向人阅读的完整文字稿；
   - 带稳定段 ID、时间范围、说话人和不确定标记；
   - 可以从文本跳回音频证据。

3. `timeline.md`
   - 基于完整校订逐字稿，按录音先后顺序生成内容地图；
   - 按语义转折划分时间段，不机械固定每五分钟，通常每段约 3–8 分钟；
   - 每段只回答“这段时间讲了什么”，不套会议、访谈、演讲或个人备忘模板；
   - 覆盖整段录音的全部有效内容，同一话题后面再次出现时仍保留在后面的时间位置；
   - 使用纯净时间标题、简述和信息点，不插入逐字稿证据链接；
   - 听不清范围如实标注，不根据上下文猜测补写。

4. `speech-record.json`
   - 面向未来 Agent 的稳定机器可读记录；
   - 包含源文件事实、内容类型、说话人、时间线、提炼结果、证据链接、质量状态和修订信息。

5. `note.md`
   - 面向 Obsidian 默认阅读的纯净最终笔记；
   - 根据会议、访谈、课程、语音备忘等类型使用不同结构；
   - 不显示逐字稿链接、“依据”、章节导航、speech ID 或录音技术信息；
   - partial 状态只保留一句不带链接的普通文字提醒；
   - 保护 `我的补充` 等用户区域，重新生成不得覆盖。

6. `note.evidence.md`
   - 内容结论与 `note.md` 来自同一份 `speech-record.json`，不得再次调用模型独立生成；
   - 保留当前验收稿中的逐字稿链接、依据、章节导航和质量信息；
   - 作为机器维护的核验稿，不承载用户手工补充。

7. 质量与校验信息
   - 处理是否完整；
   - 哪些范围听不清、非语音或失败；
   - 哪些结论证据不足；
   - 各产物的版本和校验和。

最终面向用户的是四份 Markdown：`note.md`、`timeline.md`、`transcript.md`、
`note.evidence.md`。`transcript.raw.json`、`speech-record.json` 和
`artifact-manifest.json` 属于 Worker 内部证据，默认不进入普通 Obsidian 笔记列表。

### 8.2 结构化笔记原则

**[设计已确定，待开发]**

笔记不能只是一个通用“摘要”，而应分层：

- 先给读者一个简洁总览；
- 再呈现核心主题和脉络；
- 按内容类型呈现专门字段；
- 会议重点提取决定、行动项、负责人、期限、分歧和未决问题；
- 访谈重点保留受访者观点、理由、例子、张力和未解问题；
- 课程或演讲重点保留概念、论证、例子和可复习要点；
- 语音备忘重点提取想法、下一步和待验证假设；
- 每个重要结论链接到一个或多个 transcript 段；
- 信息不确定时明确标记，而不是补写不存在的内容。

### 8.3 前端体验方向

**[设计已确定，页面尚未设计完成，代码尚未开始]**

- 视觉和交互规范参考 Reading Capture。
- 界面必须简洁、易用，不暴露不必要的工程细节。
- 上传后要持续显示：
  - 上传进度；
  - 校验状态；
  - 排队和处理阶段；
  - 已稳定的文字；
  - 明确标记的临时文字尾部；
  - 说话人识别进度；
  - 磁盘、内存、模型和连接提醒；
  - 中断、重连和恢复结果。
- 不要求逐字实时流式输出，但一旦转写开始，就要逐步看到已经提交的数据。
- 资源不足时需要告诉用户：
  - 为什么处理慢或暂停；
  - 当前风险是什么；
  - 用户可以采取什么操作；
  - 系统不会自动替用户清理磁盘。

### 8.4 前端设计门

**[设计已确定]**

在写 Obsidian 页面代码之前，必须先完成：

- 信息架构；
- 本地和远程完整流程；
- 正常、警告、阻止、暂停、断线、失败、恢复、处理完成和发布状态矩阵；
- Worker 数据到界面元素的映射；
- 对照 Reading Capture 的组件和视觉规范；
- 用 Codex 的 GPT Image 生成关键交互图；
- 窄侧栏、独立窗口、浅色、深色、键盘和减少动态效果；
- 用户审阅、修改并明确批准。

已经明确要求：后端能力达到设计所需的稳定程度后，先做页面设计和关键交互图，获得确认后再开发前端。

---

## 9. 目前明确没有完成的能力

### 9.1 后端流水线

**[核心已实现；持续后台循环待开发]**

- 已支持一次执行全部剩余 ASR 块，但尚无持续运行所有阶段的后台服务循环。
- 已完成整篇对齐、时间线验收门和估算时间戳的受控强制对齐回退。
- 已能把默认保守策略证明的静音写入稳定时间线，也能保存显式人工复核的非语音或听不清结果；尚未自动分类其余可听或不确定范围。
- 尚未形成“每一个可解码范围都有结果”的最终完整性报告和质量门。
- 已接入持久化说话人识别并生成匿名说话人标签；不可用时明确降级。
- 已实现内容类型判断、分块提炼、全文综合、证据链接和不受支持结论拦截。
- 已渲染四份用户侧 Markdown、两份机器记录和校验 manifest。
- 已实现独立修订台账、总结差异记录和 `我的补充` 人工区域保护。
- 已实现本地核心发布租约、Vault 原子整包写入、校验、冲突保护、恢复和接管；正式网络 API
  与设备/Vault 授权仍留在阶段 F。

### 9.2 网络和远程

**[待开发]**

- 尚无 FastAPI 网络服务。
- 尚无版本化 OpenAPI 和正式 TypeScript 客户端类型。
- 尚无设备配对、长期凭据、撤销和 Vault 授权。
- 尚无 HTTPS/Tailscale 接入。
- 尚无远程上传、状态轮询/SSE 和重连。
- 尚无真实“外出笔记本 → 家中 Mac”端到端测试。

### 9.3 后台服务和模型管理

**[待开发]**

- 尚无 Worker Manager。
- 尚无 launchd 安装和系统后台服务控制。
- 尚无模型下载、校验、切换、回滚和磁盘预算界面。
- 尚无开机/登录条件诊断。
- 尚无打包后的独立 Worker 运行时。

### 9.4 Obsidian 插件

**[待开发]**

- 尚未完成正式页面设计。
- 尚未生成关键交互图。
- 尚未获得前端设计批准。
- 尚未实现源文件选择、上传、连接、进度、文字预览、复核、发布或设置页面。
- 当前没有可安装给日常使用的插件版本。

---

## 10. 后续开发计划

后续按风险和依赖顺序推进。前一个阶段的完成标准没有达到时，不应为了“看起来完整”而提前堆叠后面的页面代码。

### 阶段 A：完成转写证据流水线

**优先级：最高**

#### 工作项

- [x] 完成整篇稳定文字、原始证据和时间线覆盖的持久化验收门。
- [x] 为未覆盖范围生成与对齐报告和标准化音频绑定的保守 PCM 证据。
- [x] 把固定保守策略证明的静音写入稳定时间线并刷新整篇对齐。
- [x] 为完整当前缺口增加版本绑定、可恢复的显式人工复核结果。
- [x] 为仍是估算时间戳的稳定文字实现受控强制对齐回退。
- [x] 建立 revision-pinned、只保存证据且不授权落盘的 VAD 候选评测边界。
- [x] 建立私有金标准 manifest、时长加权指标和显式验收策略工具。
- [ ] 用代表性私有标注样本评测 VAD 漏检/误检，并决定是否存在安全自动规则。
- [ ] 评测独立的听不清判定证据；VAD 本身不能完成该分类。
- [ ] 生成覆盖整个标准化音频时间线的完整性报告。
- [ ] 明确 `complete` 和 `partial` 的判断规则。
- [ ] 确保任何空模型结果都不会被当成成功。
- [ ] 实现连续后台阶段执行器，不再需要人工逐块运行命令。
- [ ] 每块之间继续执行磁盘和内存边界检查。
- [ ] 支持安全暂停、恢复、取消和重试。
- [ ] 增加跨多个块、跨重启的集成测试。

#### 完成标准

- 每个标准化 PCM 帧都能对应到一个明确结果；
- 不存在未解释的时间间隙；
- 长任务可以连续执行到对齐/质量阶段；
- 中断后从下一个未完成块继续；
- 原始音频和已经落盘的证据不被改写；
- 用户能区分完整任务与部分完成任务。

### 阶段 B：说话人区分

**优先级：最高；这是用户明确要求的首版能力**

#### 工作项

- [ ] 接受并配置 pyannote 所需模型权限。
- [ ] 使用本地 Hugging Face 凭据下载受限模型。
- [ ] 建立持久化的 diarization 尝试和结果数据。
- [ ] 把说话人时间区间映射到对齐后的文字段。
- [ ] 生成“说话人 1、说话人 2……”匿名标签。
- [ ] 保存置信度和不确定状态。
- [ ] 对重叠说话建立明确表示。
- [ ] 说话人模型不可用时降级，不丢文字。
- [ ] 支持后续用户重命名和纠正归属。
- [ ] 增加干净双人、多人、噪声和重叠发言测试。
- [ ] 增加阶段重启和结果重放测试。

#### 完成标准

- 会议和访谈的文字稿能够区分匿名说话人；
- 说话人识别失败不会导致转写失败；
- 不确定和重叠说话不会被伪装成确定结果；
- 用户之后可安全更名和修正。

### 阶段 C：内容类型判断和信息提炼

**优先级：高**

#### 工作项

- [ ] 定义内容类型和内容特征的稳定 schema。
- [ ] 优先自动判断会议、访谈、课程/演讲、语音备忘和通用内容。
- [ ] 允许用户覆盖自动判断。
- [x] 接受每个任务可选、自由格式且可不完整的背景说明；原始 ASR 不受其改写，
  仅在逐字稿校订、语境判断和全文提炼时作为参考。
- [x] 明确证据优先级：背景说明不能直接生成逐字稿中没有的决定、待办或事实；
  与证据冲突时不得强制替换。
- [ ] 下载并评测 Ollama Qwen3 14B 和 8B。
- [ ] 确定默认总结配置和资源较低时的备用配置。
- [ ] 对每个转写块提炼有证据的局部事实。
- [ ] 对局部事实做分层合并，避免一次把整篇长稿塞给模型。
- [ ] 为每个重要结论保存 transcript 证据引用。
- [ ] 检测缺少证据或互相冲突的结论。
- [x] 实现各内容类型的专用提炼结构：访谈、课程、演讲、语音备忘/个人笔记和通用内容
  均有受 schema 约束的场景章节和专用 Note 标题；会议继续保留已验收结构。
- [ ] 对总结结果建立人工评分样本和回归测试。
- [x] 将真实会议样例 generation `19` 的用户验收结果固化为 V1 Note 质量基线。
- [ ] 分别为访谈、课程/演讲、语音备忘/个人笔记和通用内容建立代表性样本，验证它们
  达到与已验收会议纪要相同的清晰度、信息完整性、准确性和证据可追溯性。
- [x] 建立代码级跨类型质量门：共享质量标准，但按场景使用专用结构，禁止把会议章节机械套用
  到访谈、课程或个人笔记。

#### 完成标准

- 自动类型判断可以被用户复核和改正；
- 重要结论都有证据来源；
- 不能支持的关键结论不会进入最终笔记；
- 长音频不会因为上下文限制而漏掉后半段；
- 结构化笔记相对完整文字稿确实提高阅读效率；
- 会议、访谈、课程/演讲、语音备忘/个人笔记和通用内容均通过同等级人工质量门，
  但各自保留适合场景的内容组织方式。

### 阶段 D：最终产物和人工修订

**优先级：高**

#### 工作项

- [x] 定义并版本化 `transcript.raw.json` schema。
- [x] 汇总不可变 ASR 尝试为原始证据产物。
- [x] 生成带稳定段 ID 的 `transcript.md`。
- [x] 定义并版本化 `speech-record.json` schema。
- [x] 实现内容类型对应的 `note.md` 渲染。
- [x] 将当前带证据的 `note.md` 拆为纯净 `note.md` 与机器维护的 `note.evidence.md`。
- [x] 基于完整校订逐字稿生成与内容类型无关的 `timeline.md`。
- [x] 保证 `timeline.md` 覆盖全部有效时间线，且按语义边界而非固定分钟数分段。
- [x] 保证最终 Note 仍直接读取完整校订逐字稿；`timeline.md` 只作为覆盖检查和辅助输入，
  不能成为 Note 的唯一上游。
- [x] 为四份用户侧 Markdown 增加内容一致性、证据可达性和重生成回归测试。
- [x] 生成完整性和质量清单。
- [x] 加入产物哈希、模型版本和生成版本。
- [x] 实现说话人命名修订。
- [x] 实现文字纠正记录。
- [x] 实现日期纠正。
- [x] 实现只重生成总结、不破坏证据。
- [x] 支持修改任务背景说明后，只重跑下游校订和笔记提炼，不覆盖原始 ASR 证据。
- [x] 保护 `我的补充` 终端人工区域；重生成只替换其前方机器内容，边界缺失时安全失败。
- [x] 支持比较重生成前后的变化。

#### 完成标准

- 人可以完整阅读文字稿和结构化笔记；
- 人可以通过 `timeline.md` 按时间顺序快速了解整段录音，而不必只依赖最终主题提炼；
- Agent 可以稳定读取 `speech-record.json`；
- 任意重要结论能回到文字证据；
- 用户修订不会破坏原始证据；
- 重新生成不会覆盖人工内容。

### 阶段 E：处理完成和 Vault 发布

**优先级：高**

#### 工作项

- [x] 建立 Worker 私有产物存储。
- [x] 处理完成后进入 `processed`。
- [x] 实现发布声明、租约、续租、成功确认和失败恢复。
- [x] 防止两个客户端同时发布同一任务。
- [x] 在 Vault 内先写临时目录。
- [x] 校验所有文件和哈希。
- [x] 通过原子重命名完成整包发布。
- [x] 发布失败时保留 Worker 产物。
- [x] 区分 `processed`、`publishing`、`published`。
- [x] 处理目录已存在、用户改动和同步冲突。
- [x] 支持之后重试发布或由另一设备接管。

实现边界：以上是 Worker 核心和本地开发者 CLI 契约；阶段 F 才把租约与确认暴露为正式
FastAPI，并加入设备身份和 Vault 授权。阶段 H 的 Obsidian 前端尚未开始。

#### 完成标准

- Vault 中不会出现“只有一半文件”的成功任务；
- 处理完成但发布失败的任务可恢复；
- Google Drive 或其他同步方案不是协议依赖；
- 输出根目录使用可配置 Vault 相对路径；
- 已发布包可以校验来源和完整性。

### 阶段 F：正式 Worker API 和安全

**优先级：高；远程使用的前置条件**

#### 工作项

- [ ] 定义协议版本和能力协商。
- [ ] 建立 OpenAPI。
- [ ] 生成 Python 和 TypeScript 类型。
- [ ] 实现 FastAPI 健康、能力、上传、作业、快照、更新和产物接口。
- [ ] 实现大文件断点续传。
- [ ] 实现有限制的轮询或 SSE 更新。
- [ ] 实现设备配对。
- [ ] 实现长期凭据的安全保存。
- [ ] 实现 Vault 级授权。
- [ ] 实现设备撤销和凭据轮换。
- [ ] 配置 HTTPS。
- [ ] 编写 Tailscale 推荐接入方案，同时允许其他安全网络方案。
- [ ] 对错误消息和诊断日志做脱敏。

#### 完成标准

- 插件可以只依赖版本化协议，不依赖 Worker 内部数据库；
- 外出笔记本可以安全提交和重连；
- Worker 重启不要求重新配对；
- 未授权设备无法查看任务、文字或产物；
- 协议升级有兼容判断。

### 阶段 G：Worker Manager 和系统后台服务

**优先级：中高**

#### 工作项

- [ ] 实现 macOS launchd 服务安装、启动、停止和重启。
- [ ] 验证普通 Worker 崩溃后自动拉起。
- [ ] 验证 Mac 重启后的真实启动条件。
- [ ] 明确 FileVault 解锁、用户登录和同步盘挂载状态。
- [ ] 显示模型、磁盘、内存、端口和网络状态。
- [ ] 下载模型前显示空间预算。
- [ ] 校验模型文件。
- [ ] 支持模型激活、切换和回滚。
- [ ] 提供不含私人内容的诊断包。
- [ ] 最终提供无需开发环境的打包运行时。

#### 完成标准

- 日常使用不需要打开终端；
- 服务异常后可以自动恢复；
- 用户能看懂为什么服务不可用；
- 普通服务重启不导致重新配对；
- 模型安装失败不会破坏已有可用版本。

### 阶段 H：Obsidian 交互设计

**优先级：后端契约稳定后立即进行；前端编码前必须完成**

#### 工作项

- [ ] 详细研究本机 Reading Capture 的布局、组件、间距、排版、颜色和交互。
- [ ] 设计插件信息架构。
- [ ] 设计本地和远程提交完整流程。
- [ ] 在音频提交流程中设计一个可选的自由文本“补充背景”入口，不限制句数、
  段落形式或必填信息，并清楚说明它仅供二次加工参考。
- [ ] 建立全部界面状态矩阵。
- [ ] 把 Worker 状态、错误、进度和操作映射到界面。
- [ ] 设计任务工作台。
- [ ] 设计渐进文字显示。
- [ ] 设计说话人复核。
- [ ] 设计磁盘不足、内存压力和安全暂停。
- [ ] 设计离线、重连和快照恢复。
- [ ] 设计处理完成但尚未发布。
- [ ] 设计文字稿阅读和证据定位。
- [ ] 设计修订和重新总结。
- [ ] 使用合成数据和 Codex GPT Image 生成关键交互图。
- [ ] 验证浅色、深色、窄侧栏、键盘和减少动态效果。
- [ ] 与用户逐项审阅并获得明确批准。

#### 第一批关键交互图

- [ ] 选择音频、显示源文件信息、填写可选补充背景和确认录音日期；
- [ ] 选择本地/远程 Worker 和准确/速度模式；
- [ ] 上传和媒体验证；
- [ ] 转写中持续出现稳定文字和临时尾部；
- [ ] 说话人识别进度；
- [ ] 磁盘阻止、内存警告和安全暂停；
- [ ] Worker 离线、重连和恢复；
- [ ] 已处理但未发布；
- [ ] 文字稿阅读、音频证据定位和说话人改名；
- [ ] 纠正后重新总结，并保护 `我的补充`；
- [ ] 首次 Worker 设置。

#### 完成标准

- 用户明确批准主要流程、状态、术语和视觉方向；
- 设计与 Reading Capture 属于同一套体验语言；
- 资源和恢复状态对非技术用户可理解；
- 页面代码实现可以直接按设计和协议映射推进。

### 阶段 I：Obsidian 插件实现

**优先级：设计批准后**

#### 工作项

- [ ] 插件清单、构建和设置基础。
- [ ] 源文件选择和拖放。
- [ ] 录音日期建议与确认。
- [ ] 可选自由文本补充背景的提交、保存、修改和下游重新处理。
- [ ] Worker 选择、连接和配对。
- [ ] 可恢复上传。
- [ ] 排队、暂停、继续、取消和重试。
- [ ] 渐进文字预览。
- [ ] 资源和连接提醒。
- [ ] 断线重连和快照恢复。
- [ ] 处理完成和发布操作。
- [ ] 文字稿阅读。
- [ ] 音频证据定位。
- [ ] 说话人重命名和归属纠正。
- [ ] 文字纠正和总结重生成。
- [ ] 保护人工内容。
- [ ] 主题、窄窗、键盘和无障碍。

#### 完成标准

- 用户可以不用开发命令完成本机真实音频的提交、处理、复核和发布；
- 界面与批准的设计一致；
- 关闭再打开 Obsidian 后可恢复任务；
- 不因界面断开而中断家中 Worker。

### 阶段 J：远程个人 Alpha、可靠性和打包

**优先级：个人本机流程通过后**

#### 工作项

- [ ] 外出笔记本和家中 Mac 的真实安全连接。
- [ ] 大文件远程断点续传。
- [ ] 提交后关闭笔记本，家中 Worker 继续。
- [ ] 之后重连并恢复文字和进度。
- [ ] 家中 Worker 重启后恢复。
- [ ] 发布租约和多设备接管。
- [ ] Google Drive 同步环境测试。
- [ ] 验证更换同步方案不会影响任务协议。
- [ ] 真实长音频和多说话人验收。
- [ ] 故障注入和性能回归。
- [ ] 模型和数据库升级测试。
- [ ] 干净 Mac 安装、升级、降级和卸载。
- [ ] 后续朋友试用前进行签名、公证、第三方许可证审计和文档整理。

#### 完成标准

- 外出使用链路完整可用；
- 断网、重启、磁盘和内存风险不会造成静默丢失；
- 一台干净 Mac 可以不依赖开发工具完成安装；
- 用户可以检查、撤销和重新建立设备授权。

---

## 11. 下次继续时的建议顺序

为了避免下次重新判断，建议从下面的顺序直接开始：

1. **先运行现有 198 项测试，确认恢复点没有变化。**
2. **继续阶段 A：把代表性私有音频填入金标准 manifest 并实测固定 revision VAD。**
3. 不能仅凭 PCM 能量或空 ASR 猜测，自动结果必须有独立可验证证据。
4. 保留 `review-gap` 作为人工明确证据通道，不把它写成自动检测。
5. [已实现] 自动连续执行所有 ASR 块和安全恢复。
6. [已实现] 用合成的多块音频完成第一轮端到端测试。
7. [已实现并验证] 阶段 B 接入 pyannote 说话人识别。
8. [已完成] 下载 Ollama Qwen3 14B/8B（`qwen3:14b` 约 9.3 GB、`qwen3:8b` 约 5.2 GB）。
9. [已实现] VAD 语音缺口的定点重转写。
10. [下一步] 用真实 `2026-07-24` WAV 跑完缺口策略：VAD 无语音标
    `non_speech`，重转写失败的语音缺口标 `inaudible`，再继续说话人、提炼和
    产物到 `processed`。
11. [提炼与产物已实现] 完成发布协议和正式 API。
12. 后端为页面提供的状态和数据稳定后，暂停前端编码，先完成 UI 设计与 GPT Image 关键交互图。
13. 用户批准页面设计后，再写 Obsidian 插件页面。

如果只剩较短的开发时间，优先完成一个可独立提交、带测试的后端增量，不要同时打开说话人、总结、API 和前端四条线。

---

## 12. 后续需要用户提供或确认的材料

这些内容现在不阻塞继续开发，但到相应阶段需要：

### 12.1 真实音频测试

用户已经同意在开发完成相应能力后提供真实音频。建议样本包括：

- 单人语音备忘；
- 双人访谈；
- 多人会议；
- 课程或演讲；
- 超过一小时的长录音；
- 有噪声、回声或低音量的录音；
- 有重叠发言的录音；
- 中文、英文和中英混合。

音频及其私人原始文字稿应放在 Git 忽略的本地测试目录，不能提交到仓库。

### 12.2 质量参考

为了验证“内容是否全部转成文字”和“信息提炼是否有效”，最好为一部分测试音频提供：

- 人工核对的参考文字稿；
- 预期应该提取的事实；
- 会议的决定和行动项；
- 已知说话人；
- 应当保留的不确定或分歧；
- 用户对最终笔记是否有用的评分。

### 12.3 说话人模型权限

接入 pyannote 时可能需要：

- 用户接受 Hugging Face 上对应模型条款；
- 一个仅保存在本机安全存储中的 Hugging Face token。

凭据不得写入配置示例、日志、测试快照、文档或 Git。

### 12.4 总结模型下载

进入阶段 C 时需要确认：

- 是否现在下载 Ollama Qwen3 14B 和 8B；
- 模型所需磁盘预算；
- 哪一个作为默认，哪一个作为资源备用。

### 12.5 前端设计确认

进入阶段 H 后需要用户逐项确认：

- 信息架构；
- 关键流程；
- 实时文字呈现方式；
- 资源和网络提示；
- 说话人复核；
- 处理与发布的区别；
- Reading Capture 风格一致性；
- GPT Image 生成的关键交互图。

### 12.6 当前不需要再提供的内容

- GitHub 远程仓库已经创建并配置。
- Reading Capture 的本机参考代码已经提供。
- 本地 Qwen3-ASR 模型已经完成基础下载和验证。

---

## 13. 不应在后续开发中被破坏的原则

1. 原始音频永不被修改。
2. 所有可解码时间范围都必须有明确结果，不能静默遗漏。
3. 原始证据先于总结落盘。
4. 会议和访谈必须支持说话人区分。
5. 说话人识别失败不能使文字稿消失。
6. 自动总结的重要结论必须能追溯到文字证据。
7. 本地模型优先，云端默认关闭。
8. Worker 不依赖 Google Drive 或其他特定同步产品。
9. Obsidian 关闭或外出笔记本断网时，家中 Worker 可以继续任务。
10. `processed` 不等于 `published`。
11. 发布必须是可校验、可恢复的原子过程。
12. 磁盘和内存危险时要提示和安全暂停，不自动清理用户文件。
13. 同一 Worker 同时只运行一个重型任务。
14. 用户手写内容不能被重新生成覆盖。
15. 普通重启不应要求重新配对。
16. 私人音频、文字、凭据、数据库、绝对路径和原始报告不能进入 Git。
17. Obsidian 页面必须先设计、生成关键交互图并经用户批准，再开始开发。
18. 首版先服务个人使用，访谈意图识别属于后续扩展。
19. 项目许可证保持 PolyForm Noncommercial 1.0.0，除非用户明确决定更换。

---

## 14. 当前版本的提交记录

从设计基线到当前 Worker 核心的主要提交：

| 提交 | 内容 |
| --- | --- |
| `5d6abcc` | 建立 Speech Capture 产品与架构设计基线 |
| `ace9704` | 验证本地 Qwen3-ASR 模型配置和基准 |
| `6e0ecd0` | 建立持久化 Worker 核心 |
| `2cf11ed` | 建立前端设计批准门 |
| `2d73965` | 实现可恢复的大音频接收 |
| `ad24f50` | 实现已验证任务调度 |
| `04c6f1d` | 实现持久化渐进文字快照 |
| `c372536` | 实现可跨重启的本地 ASR 执行 |
| `735a17a` | 保存详细开发交接文档 |
| `bffefff` | 实现持久化的整篇对齐和完整性退出门 |
| `12c1eb9` | 为未覆盖范围增加保守 PCM 证据和开发者 CLI |
| `bf1cd75` | 把确定性静音安全写入稳定时间线 |
| `12b2c45` | 为完整未决范围增加显式人工复核落盘 |
| `140b32f` | 为估算时间段增加受控强制对齐回退 |
| `682b64f` | 建立不授权落盘的 revision-pinned VAD 候选证据边界 |
| 本次提交 | 建立私有 VAD 金标准清单、指标和显式验收门 |

恢复开发时应以 `git log` 显示的当前分支最新提交为准，不要仅依赖本表中的
历史短哈希。

---

## 15. 恢复开发时的快速检查清单

- [ ] 确认当前分支为 `agent/worker-core`。
- [ ] 确认工作区没有来源不明的未提交改动。
- [ ] 阅读本文件第 9、10、11 节。
- [ ] 运行 Worker 全部测试。
- [ ] 运行静态检查和依赖锁检查。
- [ ] 确认当前磁盘空间满足模型和测试预算。
- [ ] 不把真实音频、文字稿或模型凭据加入 Git。
- [ ] 从“阶段 A”的真实私有 VAD 基线运行继续。
- [ ] 每个独立增量都补齐测试、文档和提交。
- [ ] 到达前端阶段时先做设计，不直接写页面代码。

这份文档应在每个重要后端阶段、前端设计批准、个人 Alpha 完成和远程 Alpha 完成时更新，以便它始终反映真实状态。

---

## 16. 2026-07-30 夜间开发记录

本次只完成整篇对齐之后的未覆盖范围 PCM 证据，没有打开说话人识别、总结、
网络 API 或前端实现。

完成内容：

- 检查并修正未提交的 `gap_analysis.py` 草稿；
- 对输入策略、对齐报告、范围顺序和总时长增加严格校验；
- 把 Gap 报告绑定到来源对齐报告 generation/SHA-256 和标准化音频 SHA-256；
- 保留“只有确定性近数字静音才下结论”的保守边界；
- 增加 `analyze-gaps` 开发者 CLI；
- 增加 13 项自动化测试；
- 更新根目录、Worker、API、测试、路线图、决策日志和本交接文档。

本次验证：

- `153 passed`；
- Ruff 全目录静态检查通过；
- `uv lock --check` 通过；
- `compileall` 通过；
- source distribution 和 wheel 构建通过，wheel 已确认包含 `gap_analysis.py`；
- CLI 顶层和 `analyze-gaps --help` 冒烟检查通过；
- Git 差异检查和敏感路径/凭据扫描通过。

下一次从“把 `definite_silence` 证据安全写成稳定时间线结果”继续。其余
`unresolved` 范围仍需更强证据，不能仅凭 PCM 能量判定为非语音。

---

## 17. 2026-07-31 开发记录

本次只完成确定性静音的稳定时间线落盘，没有扩展到可听非语音分类、强制
对齐、说话人识别、总结、API 或前端。

完成内容：

- 新增仅限 `aligning` 阶段的证据约束静音回填入口；
- 固定可落盘策略为 20 ms 窗口、至少 100 ms、峰值阈值 8；
- 拒绝自定义阈值、可听 PCM、过期对齐报告和重叠范围；
- 支持在已有文字段之前或中间补写 `non_speech + aligned`；
- 为每段落盘静音保存独立私有证据 checkpoint；
- 补写后立即刷新整篇对齐报告；
- 增加 `materialize-silence` 开发者 CLI；
- 增加 7 项自动化测试并更新相关设计、API 和交接文档。

本次验证：

- `160 passed`；
- Ruff 全目录静态检查通过；
- `uv lock --check` 通过；
- `compileall` 通过；
- source distribution 和 wheel 构建通过；
- `materialize-silence --help` 冒烟检查通过；
- Git 差异和敏感路径/凭据扫描通过。

下一次从“使用强于 PCM 幅度的证据处理其余 `unresolved` 范围”继续。可听
或不确定音频仍不能直接标成 `non_speech`。

---

## 18. 2026-07-31 显式人工复核开发记录

本次只增加完整当前缺口的显式人工复核证据通道，没有把人工结果包装成自动
分类，也没有打开强制对齐、说话人识别、总结、API 或前端。

完成内容：

- 新增 `review-gap` 开发者 CLI；
- 仅允许把一个完整当前未决范围标成 `non_speech` 或 `inaudible`；
- 用对齐报告 generation 和 payload SHA-256 锚定复核证据；
- 使用不透明、不可改绑的 review key 保证幂等；
- 不保存复核者身份或自由文本备注；
- 为证据和 materialization 分别保存私有 checkpoint；
- 支持稳定段写入后中断的幂等修复；
- 写入后立即刷新整篇对齐；
- 保证 `inaudible` 只解释时间线，不会产生错误的完整转写结论；
- 增加 7 项自动化测试并更新核心、API、数据模型、测试和交接文档。

本次验证：

- `167 passed`；
- Ruff 全目录静态检查通过；
- `uv lock --check` 通过；
- `compileall` 通过；
- source distribution 和 wheel 构建通过；
- wheel 已确认包含 `gap_review.py`；
- `review-gap --help` 冒烟检查通过；
- Git 差异和敏感路径/凭据扫描通过。

下一次从“为仍是估算时间戳的稳定文字增加受控强制对齐回退”继续。自动
非语音/听不清分类仍需单独评测和验证，不能依赖音量或空 ASR。

---

## 19. 2026-07-31 受控强制对齐开发记录

本次只完成估算时间戳的强制对齐回退，没有打开自动非语音分类、说话人
识别、总结、API 或前端。

完成内容：

- 核实 pinned `mlx-qwen3-asr` 的独立 `ForcedAligner` API；
- 新增每次处理一个估算段的 `force-align-next` 开发者 CLI；
- 只对现有稳定文字和精确标准化 PCM 范围做对齐；
- 严格校验文字覆盖、时间有限性、单调性、重叠和边界；
- 私有词级结果先落盘、校验后才修改 segment 元数据；
- 保持文字、segment ID、commit key、语言和说话人字段不变；
- 增加模型前资源边界和无语言时的安全拒绝；
- 增加证据 checkpoint、SHA-256、`0600` 权限和中断重放；
- 让整篇退出门持续验证强制对齐证据，拒绝手工标志和篡改文件；
- 对齐后立即刷新整篇报告，新出现的首尾缺口继续进入 Gap 证据流程；
- 增加 11 项自动化测试并更新核心、API、数据模型、测试和交接文档。

本次验证：

- `178 passed`；
- Ruff 全目录静态检查通过；
- `uv lock --check` 通过；
- `compileall` 通过；
- source distribution 和 wheel 构建通过；
- wheel 已确认包含 `forced_alignment.py`；
- `force-align-next --help` 冒烟检查通过；
- Git 差异和敏感路径/凭据扫描通过。

下一次从“评测并验证自动非语音/听不清分类方案”继续。该工作仍需先选定
独立于音量和空 ASR 的证据来源，再决定是否进入稳定时间线。

---

## 20. 2026-07-31 自动分类候选证据边界开发记录

本次选定 revision-pinned `pyannote/segmentation` VAD 作为第一项独立候选
证据，但没有把候选观察包装成自动分类，也没有新增稳定时间线结果。

完成内容：

- 新增 `analyze-speech-activity` 开发者 CLI；
- 强制使用完整模型 commit SHA，拒绝浮动 revision；
- 固定并记录 pyannote 版本与 DIHARD3 VAD 参数配置；
- 只对当前 Gap 报告中的 `unresolved` 范围生成时间证据；
- 严格校验检测区间的有限性、顺序、重叠和音频边界；
- 证据绑定 Gap 报告、对齐报告和标准化音频 SHA-256；
- 模型前执行资源边界检查，阻塞时不加载或调用模型；
- 无未决范围时跳过模型；
- 报告和每个范围都显式禁止自动落盘；
- 增加 7 项自动化测试并更新核心、API、测试和交接文档。

受限于仓库不包含私人音频、当前也没有可提交的代表性金标准样本，本次没有
下载或运行真实 pyannote 模型。因此 VAD 的实际漏检/误检率尚未成立，
`no_speech_detected` 仍只是评测观察，不是 `non_speech` 证据；VAD 也不能
单独判定 `inaudible`。

下一次先准备不进入 Git 的私有标注样本，运行固定 revision 的真实 VAD，
记录语音漏检和非语音误检，再决定是否存在足够保守的自动规则。若没有安全
规则，继续保留 `review-gap` 人工通道，不强行自动化。

---

## 21. 2026-07-31 私有 VAD 金标准评测工具开发记录

本次完成真实 VAD 运行前的可重复评测与指标边界，没有下载模型、处理私人
音频或批准自动分类规则。

完成内容：

- 新增 `speech-capture-vad-probe`；
- 新增可复制的无私人内容 manifest 模板；
- 强制 manifest 内相对路径不能逃逸或指向符号链接；
- 支持显式 `speech` / `non_speech` 时间标注，未标注范围排除；
- 在内存中使用 FFmpeg 标准化私有样本，不生成可提交的中间音频；
- 用源文件前后 SHA-256 拒绝评测期间变化；
- 按帧计算四类混淆时长、语音漏检率和非语音误报率；
- 聚合指标按总标注时长计算，不对样本比率做错误的简单平均；
- 验收策略四项参数必须同时由所有者提供，不提供时保持 unapproved；
- 通过策略仍不授权 `non_speech` / `inaudible` 自动落盘；
- 私有报告不含路径、文件名、正文或凭据，以 `0600` 权限原子写入；
- 增加 13 项自动化测试并更新核心、测试和交接文档。

当前本机没有缓存 `pyannote/segmentation`，仓库也没有真实私有金标准样本。
现有 25 个音频候选全部来自虚拟环境依赖或合成测试夹具，因此不能用来声称
真实模型质量。官方模型入口未授权时返回 `GatedRepo`，无法解析 main 的
commit SHA 或下载权重。下一次需要项目所有者先在 Hugging Face 接受模型
条款并完成本机凭据登录，再把代表性音频和标注放进
`test-data-private/`，解析完整模型 revision 后运行真实基线。令牌不能写入
命令行、manifest、报告或 Git。

---

## 22. 2026-07-31 长音频 VAD 金标准评测开发记录

本次按项目所有者确认的 1–2 小时长录音需求，移除金标准评测工具的 30 分钟
样本上限并改为有界窗口检测，没有打开说话人识别、总结、API 或前端。

完成内容：

- 完成 Hugging Face 登录验证（账号 `imfashionai`）；
- 解析并固定 `pyannote/segmentation` 完整 revision：
  `660b9e20307a2b0cdb400d0f80aadc04a701fc54`；
- 同步 `diarization` 依赖，并补齐 pyannote.audio 实际需要但未声明的
  `omegaconf`；
- 用 10 秒合成音频完成真实模型冒烟测试，确认登录、下载、加载和评测链路
  可用；
- 移除 `decode_audio_source` 的 30 分钟时长拒绝；
- 检测改为固定 10 分钟核心窗口 + 两侧 2 秒边距；
- 跨窗口连续语音通过边距和裁剪保留，不会丢失边界；
- 边距内的检测不会泄漏到核心时间线之外；
- 每个窗口的检测仍经过原有严格校验，合并结果保持有序不重叠；
- 窗口参数可在构造器注入，单元测试使用小窗口验证；
- 增加 3 项自动化测试并更新 README、决策日志和本交接文档。

本次验证：

- `201 passed`；
- Ruff 全目录静态检查通过；
- `uv lock --check` 通过。

下一次从“填入真实私有金标准样本并运行固定 revision 的 VAD 基线”继续。

---

## 23. 2026-07-31 自动连续 ASR 批次执行开发记录

本次按交接顺序实现“自动连续执行所有 ASR 块和安全恢复”，没有打开说话人
识别、总结、API 或前端。

完成内容：

- `AsrChunkExecutor` 新增 `run_all`，一次调用连续执行剩余全部 ASR 块；
- 沿用单块执行器的重试、资源暂停、失败范围和证据重放语义；
- 支持 `max_chunks` 单次批次上限，便于开发者分批观察进度；
- 新增 `run-asr-all` 开发者 CLI，输出机器可读批次结果；
- 批次中断后可从已完成块继续，已落盘证据不会重复调用模型；
- 增加 6 项自动化测试并更新 Worker README、核心文档和本交接文档。

本次验证：

- `207 passed`；
- Ruff 全目录静态检查通过；
- `compileall` 通过。

下一次从“用合成的多块音频完成第一轮端到端测试”继续。

---

## 24. 2026-07-31 合成多块音频端到端测试开发记录

本次完成第一轮合成多块音频的 Worker 端到端测试，没有打开说话人识别、
总结、API 或前端。

完成内容：

- 新增 `tests/test_end_to_end.py` 集成测试；
- 95 秒合成音频生成 4 个以上 ASR 块；
- 使用真实 FFprobe 完成媒体验证，走完上传、组装、建作业和调度；
- `run_all` 自动连续执行全部 ASR 块并进入 `aligning`；
- `finalize-alignment` 完成证据、对齐和时间线验收后进入 `diarizing`；
- 跨 Worker 重启后验收幂等，不重复调用模型，不重复转换状态；
- 增加 1 项集成测试并更新核心文档和本交接文档。

本次验证：

- `208 passed`；
- Ruff 全目录静态检查通过；
- `compileall` 通过。

下一次从阶段 B 接入 pyannote 说话人识别继续。

---

## 25. 2026-07-31 匿名说话人归属开发记录

本次完成阶段 B 的说话人归属代码和确定性测试，没有打开总结、API 或前端。

完成内容：

- 新增 `DiarizationFailed` 稳定错误码；
- 新增 `diarization_execution.py`：
  - revision-pinned `pyannote/speaker-diarization-3.1` 真实引擎适配器；
  - 匿名说话人映射和最大重叠归属；
  - 私有原始证据先落盘、校验后才更新段元数据；
  - 跨重启证据重放和幂等完成；
  - 模型失败或空轨迹降级为 `UNAVAILABLE`，不丢文字；
  - 资源阻塞时安全暂停；
- 新增 `run-diarization` 开发者 CLI；
- 新增 9 项测试并扩展合成多块端到端测试到 `structuring`；
- 更新 Worker README、核心文档和本交接文档。

本次验证：

- `218 passed`；
- Ruff 全目录静态检查通过；
- `compileall` 通过。

真实模型冒烟完成：先补齐 `pyannote/segmentation-3.0` 和
`pyannote/speaker-diarization-community-1` 授权，然后修复 pyannote 4.0.7
`DiarizeOutput` 返回值适配；在 60 秒真实录音片段上识别出 18 个说话人
轨迹和 3 个匿名说话人。下一步进入内容类型判断和信息提炼（阶段 C）。

---

## 26. 2026-07-31 内容类型判断与信息提炼开发记录

本次完成阶段 C 的内容分类和证据化提炼代码，没有打开最终产物、发布或 API。

完成内容：

- 新增 `StructuringFailed` 稳定错误码；
- 新增 `structuring_execution.py`：
  - 内容类型与 traits/confidence schema；
  - 按字符上限分批的局部提炼；
  - 相同结论合并、证据并集、无证据项单独标记；
  - 本地 Ollama 适配器（`qwen3:14b` 默认、`qwen3:8b` 备用）；
  - 私有原始证据先落盘、校验后才推进；
  - 模型失败降级为 `generic + 空结论`，不编造内容；
  - 资源阻塞时安全暂停；
- 新增 `run-structuring` 开发者 CLI；
- 新增 7 项测试并把合成多块端到端测试延伸到 `quality_check`；
- 更新 Worker README、核心文档和本交接文档。

本次验证：

- `225 passed`；
- Ruff 全目录静态检查通过；
- `compileall` 通过。

下一步需要项目所有者确认 Ollama 下载预算（Qwen3 14B 约 9 GB，8B 约 5 GB，
当前磁盘可用约 62 GB），确认后启动 Ollama 服务并 `ollama pull`，再跑真实
提炼冒烟；之后进入阶段 D 最终产物和人工修订。

---

## 27. 2026-07-31 后端数据产物包开发记录

本次完成阶段 D 的后端数据产物生成，没有打开 Vault 发布、正式 API 或
Obsidian 页面。

完成内容：

- 新增 `ArtifactGenerationFailed` 稳定错误码；
- 新增 `artifact_generation.py`：
  - 确定性的 `speech_id` 和稳定块 ID；
  - `transcript.raw.json` 汇总不可变 ASR 尝试；
  - `transcript.md` 带时间、说话人和不确定标记；
  - `speech-record.json` 记录内容、段、提炼结果、证据、质量和来源；
  - `note.md` 按内容类型渲染并保护 `我的补充`；
  - `artifact-manifest.json` 记录全部文件 SHA-256；
- 产物原子写入私有目录，`0600` 权限；
- 生成后作业进入 `processed`，跨重启幂等；
- 新增 `generate-artifacts` 开发者 CLI；
- 新增 4 项测试并把合成多块端到端测试延伸到 `processed`；
- 更新 Worker README、核心文档、决策日志和本交接文档。

本次验证：

- `229 passed`；
- Ruff 全目录静态检查通过；
- `compileall` 通过。

下一步进入阶段 E：处理完成与 Vault 发布协议（发布租约、原子写入、重试）。

---

## 28. 2026-08-01 真实录音流水线与可读落盘修复开发记录

本次用真实 `07-28` 录音跑通 ASR 并修复可读性问题，没有打开发布协议或前端。

完成内容：

- 下载 Ollama `qwen3:14b` 和 `qwen3:8b`；
- 新增 `scripts/run_real_pipeline.py`；
- 修复真实 ASR 零时长/单字时间戳：按停顿、长度和句末标点合并为可读段落；
- 修复强制对齐器输入类型和越界时间；
- 生成 `07-28` 可读文字稿（188 段，约 1.2 万字）；
- 保存对齐、PCM 缺口和 VAD 证据到本机 runtime。

本次验证：

- `233 passed`；
- Ruff 全目录静态检查通过。

当前真实任务状态：`aligning`，剩余 189 个缺口（VAD：11 个无语音、
178 个检测到语音）。下一步实现定点重转写，把这些语音缺口补成文字证据。

---

## 29. 2026-08-01 VAD 语音缺口定点重转写开发记录

本次实现“对 VAD 检测到语音的缺口定点重转写”，没有打开发布协议或前端。

完成内容：

- 新增 `GapRetranscriptionFailed` 稳定错误码；
- 新增 `gap_retranscription.py`：
  - 只处理当前 VAD 证据中 `speech_detected` 的缺口；
  - 精确读取缺口 PCM 并调用本地 ASR；
  - 私有原始证据先落盘、校验后再提交稳定文字；
  - 失败记录、安全暂停、幂等重放；
- 允许在 `aligning` 阶段提交缺口重转写的稳定段；
- 新增 `retranscribe-gaps` 开发者 CLI；
- 新增 4 项测试并更新计划文档和 handoff。

本次验证：

- `237 passed`；
- Ruff 全目录静态检查通过；
- `compileall` 通过。

下一步：对真实 `07-28` 录音运行 `retranscribe-gaps`，补满 178 个语音缺口后
继续说话人识别、Ollama 提炼和产物生成。

---

## 30. 2026-08-01 真实 WAV 验证与缺口策略开发记录

本次用 `2026-07-24 09_36_19.wav` 做第二轮真实验证，并确认缺口处理策略。

完成内容：

- WAV 前 60 秒 ASR 诊断：249 个时间戳片段、零时长 11 个（4.4%）、覆盖
  60.5%，文字清晰度明显好于 `07-28`；
- WAV 完整 ASR：116/116 块完成，生成 162 个可读段落；
- 可读文字稿：`runtime/real-20260801-wav/transcript-readable.md`；
- 修复驱动脚本的大文件分块上传和幂等续跑；
- 确认缺口策略（项目所有者已批准）：
  - VAD 无语音缺口 → `non_speech`；
  - VAD 有语音缺口 → 定点重转写；
  - 重转写失败的语音缺口 → `inaudible`。

本次验证：

- `237 passed`；
- Ruff 全目录静态检查通过。

已知问题：

- `07-28` 底噪重，保守静音策略判定 0 个确定静音；
- 真实 ASR 时间戳碎片化，需要段落合并和强制对齐适配；
- 部分极短缺口单独重转写返回空结果，只能按批准策略标 `inaudible`；
- 自动审批通道出现过两次临时故障，不是代码问题。

下一步：对 WAV 任务执行“刷新对齐 → VAD → 定点重转写 → 按策略补齐缺口 →
说话人识别 → Ollama 提炼 → 生成产物”，随后再决定是否回头处理 `07-28`。

---

## 31. 2026-08-01 会议观点演变与背景输入决策记录

本次按最新 handoff 的质量顺序完成会议议题内观点演变，没有进入 Vault 发布协议或
Obsidian 页面实现。

完成内容：

- 结构化 schema 升级到 `1.3.0`，新增有证据的最初建议、后续修正、当前方向和状态；
- 只有原文存在明确修正措辞的议题才进入演变章节，重复或普通补充说明自动移除；
- 后续证据必须晚于初始提议，并可从完整逐字稿补入更晚的同议题证据；
- 无明确确认证据的内容不再作为“已确认决定”，相应摘要确定性句子和过时核心结论降级；
- 真实 WAV 已明确呈现“销售预测/排产初始场景 → 排产优先、销售预测弱化 → AI 接管
  APS 排程”的演变；
- 项目所有者确认每任务增加可选自由文本“补充背景”，不限制句数、段落和信息完整度；
  它只在原始 ASR 之后作为校订和提炼参考，不能代替逐字稿证据；
- 该背景输入已写入产品需求、总体阶段计划和 Roadmap，前端入口仍受阶段 H 设计门约束。

本次验证：

- `242 passed`；
- Ruff 全目录静态检查通过；
- `compileall` 和 Git 差异检查通过；
- 真实结构化 checkpoint generation `17`，产物 manifest SHA-256 为
  `6bd611d9520e4aa3d7c8078fcc85ad18b8d7135ebeecb5580a8daf6d61d1e3c4`。

下一步：先实现 Worker 侧每任务自由文本背景契约和开发者测试入口，再用用户确认的
“正确公司名是聚衣堂”只重做当前样例的下游校订和笔记；用户确认质量后进入阶段 E。

---

## 32. 2026-08-02 每任务补充背景与真实样例纠正记录

本次严格继续阶段 C/D，没有进入阶段 E、正式 API 或 Obsidian 前端。

完成内容：

- 新增每任务可选 `recording_context` 数据契约，允许自由文本、多行、多段和不完整信息；
- 新增 revision-guarded 更新方法，支持保存、替换和清除；普通事件只记录是否提供及
  SHA-256，不记录背景正文；
- 新增 `set-recording-context` 开发者命令，并允许创建任务时从 UTF-8 文件读取背景；
- 所有分类、逐字稿校订、候选提取、全文综合、多人观点、讨论演变和决定核对提示均明确
  把背景视为“不可信参考而非证据”，禁止由背景单独生成决定、待办或事实；
- `document-only` 在背景发生变化时强制重新阅读完整校订稿，而不是复用旧文档；完整
  `--force` 路径则让背景同时进入逐字稿校订和全文提炼；
- 新增 `--context-corrections-only`：只对用户明确确认的旧名/正确名，或重复出现的
  单字近似专名执行有界确定性纠正，不调用 ASR 或长上下文总结模型；
- 结构化 schema 升级到 `1.4.0`，产物 schema 升级到 `1.2.0`；`speech-record.json`
  记录背景哈希、是否应用和纠正来源，但不把背景正文写成事实；
- 真实 WAV 任务已保存“正确公司名是聚衣堂”，结构化 checkpoint 升至 generation `19`；
  `transcript.md`、`speech-record.json` 的派生 `text`、`note.md` 已统一为“聚衣堂”；
  `speech-record.json.raw_text` 仍保留原始 ASR 的“聚一堂”，用于审计；
- 原始 `transcript.raw.json` SHA-256 前后均为
  `45bf58083c586998b690faf8bc9bfb705d3b260b5b2c971aa25ce5c73f2575ea`，确认未改写 ASR。

本次验证：

- `248 passed`；
- Ruff、`compileall`、CLI help 和 Git 差异检查通过；
- 真实派生文字中错误名称计数为 0，正确名称覆盖 14 个文字段；
- 补充背景原句没有进入 document、findings、决定或待办；
- 最新 manifest SHA-256：
  `6ba6498689d2044ffbc265eddee283c8506535ff25c00a73c48c9210f4fffb03`。

下一步：generation `19` 的 `note.md` 已由项目所有者明确确认为高质量版本，并正式作为
V1 质量基线。接下来先把同等级的清晰度、信息完整性、准确性和证据可追溯性落实到访谈、
课程/演讲、语音备忘/个人笔记和通用内容的专用结构与回归样本；不是把会议模板原样套用。
这项跨类型质量门落实前，不进入阶段 E，也不开始前端。

---

## 33. 2026-08-02 generation 19 质量验收与跨类型基线

项目所有者已明确确认当前 generation `19` 的 `note.md` 输出质量很高。该版本不再处于
“等待用户验收”状态，而是作为后续所有结构化笔记的 V1 质量参照。

该参照约束的是质量，不是统一模板：

- 所有类型都要完整读取校订后的全文，不能只依赖分块摘要或固定 5～8 条重点；
- 都要保证关键信息不因缺乏背景或分块合并而丢失，专名、数字、观点和行动项准确；
- 都要优先呈现真正有用的内容，并能回到逐字稿证据，避免 Meta 信息和过程性废话占据正文；
- 多人内容要按发言人整理核心观点和分歧；单人内容则保留个人意图、判断、想法、任务和
  待验证假设，不强行生成参会者或会议决定；
- 访谈应突出问答关系、受访者背景、核心观点、论据和未回答问题；课程/演讲应突出知识
  层级、概念、论证、案例和可复习结论；通用类型应根据证据选择结构，不为填模板造内容。

当前状态：会议类型已有真实样例通过人工验收；其他类型虽已有场景化 profile 和渲染
基础，但尚未经过同深度的代表性样例验收，因此不能标记为达到该基线。

下一步严格先补齐跨类型的专用结构、代表性样本和回归质量门，再按总体计划判断是否进入
阶段 E；本次仅固化验收标准，没有开始阶段 E、正式 API 或 Obsidian 前端。

---

## 34. 2026-08-02 跨类型专用 Note 契约与代码级质量门

本次严格继续阶段 C/D，没有进入阶段 E、正式 API 或 Obsidian 前端。

审查发现：之前虽然已有按内容类型区分的提示词，但访谈、课程、演讲、语音备忘和通用
内容最终仍共用偏会议的 document schema 与“核心结论、议题与讨论、待办事项”等标题。
这只能算场景提示，不能算真正的场景化输出。

本次完成：

- 结构化 schema 升级到 `1.5.0`，产物 schema 升级到 `1.3.0`，当前提示版本升级到
  `2026-08-02.5`；`1.4.0` 仍作为可读取的历史结构化版本；
- 非会议类型新增有证据的 `scene_sections`，每项包含稳定 `kind`、标题、概述、细节和
  1～3 个逐字稿证据；meeting 保留 generation `19` 已验收的原有契约，不被新字段扰动；
- 访谈支持受访者背景、关键问答、核心观点、判断依据、经历案例、分歧保留和待追问；
- 课程支持学习目标、核心概念、原理、方法步骤、案例、适用边界和复习要点；
- 演讲支持主题、论点、论据、案例、推论影响和结论启发；
- 语音备忘/个人笔记支持记录意图、想法、当前判断、明确任务、待验证假设、约束和跟进；
- 通用内容支持背景、主题、洞察、重要细节、结果、行动和开放问题；
- Note 渲染按类型使用独立标题与正文语义，非会议内容不再显示空的会议式待办章节；
- 移除强制 3～6 条 highlights/topics 的约束，数量由实际证据决定，防止为了凑数把过程性
  内容误当重点；
- 兼容旧结构：历史非会议文档没有 `scene_sections` 时，可从既有有证据 topics 保守迁移；
- 新增五类合成代表样本，验证专用 schema、完整校订稿输入、类型限定、证据链接、章节标题
  和不套用会议模板。

本次验证：

- `265 passed`；
- Ruff 全目录通过；
- 访谈、课程、演讲、语音备忘和通用类型的结构化契约与 Note 渲染回归通过；
- 会议原有“背景与参与方、议题与讨论、讨论演变、参与者观点”等渲染回归继续通过。

验收边界：仓库当前只有会议真实样例，另有的本地音频尚未形成可直接用于上述四类的已校订
逐字稿和人工参考 Note。因此，本次只能确认跨类型代码契约通过，不能把合成测试宣称为真实
内容质量已经达到 generation `19`。

下一步：获得或建立访谈、课程/演讲、语音备忘/个人笔记的代表性真实样例后，按同一质量
评分维度逐类运行与人工验收；发现问题只调整对应 profile 和共享证据门，会议已验收模板保持
稳定。在真实样例质量门完成前，不进入阶段 E 或前端。

---

## 35. 2026-08-02 真实演讲样例、内容类型覆盖与可恢复增量重做

本次继续阶段 C/D，用本机私有音频前 15 分钟验证演讲 Note；没有进入阶段 E、正式 API
或 Obsidian 前端。

完成内容：

- 新增 revision-guarded 的每任务 `content_type_override` 保存/清除能力和
  `set-content-type` 开发者命令；创建任务时也可指定六种受支持类型；
- 自动分类与用户指定分开保存：结构化证据和 `speech-record.json` 同时记录最终类型、
  来源及原自动类型；
- 用户改变类型时，`document-only` 不再复用旧类型的候选事实：复用现成校订逐字稿，
  只重做类型相关提取和最终 Note，不重跑 ASR、对齐、说话人识别或逐字稿润色；
- 类型相关提取增加版本和批次大小标识；6000 字单批在真实样例中达到 30 条上限并漏掉
  后半段，因此调整为 4000 字分批，并明确要求均匀覆盖批次开头、中段和结尾；
- 全文综合现在同时读取完整校订逐字稿和候选信息索引；索引只用于查漏，不能代替原文证据；
- speaker 的姓名、单位和角色若不能在所引证据中直接找到会自动清空；
- 演讲的待办在产物层要求与独立 `action_item`/`next_step` 候选证据重合，避免把普通启发
  直接渲染为任务；
- `document-only` 的最终模型若返回坏 JSON，会保存已经完成的类型相关提取并记录文档不可用，
  后续重试只需重新综合，不再重复昂贵的 8B 抽取；
- VAD 与说话人识别都允许最多 50ms 的检测器尾部舍入误差并夹紧到真实音频边界，超过
  50ms 仍拒绝，避免真实 15 分钟样例因 14～25ms 尾部越界失败。

真实样例：

- runtime：`services/speech-worker/runtime/profile-course-speech-20260802/`（Git 忽略）；
- job：`job_02de4d59c9a940dcbf58183f533f47ad`；类型由自动 `meeting` 人工指定为 `speech`；
- 32 个原始 ASR attempt 保持不变；63 个 transcribed、44 个 inaudible、4 个 non_speech，
  时间轴完整覆盖，产物状态正确为 `partial`；
- 说话人识别得到 2 位匿名发言者；
- 最新结构化 checkpoint generation `6`，prompt `2026-08-02.5`，2 个抽取批次覆盖
  `seg_00000001` 到 `seg_00000068`，合并 41 条有证据候选；
- 最新 manifest SHA-256：
  `11ed4fc671509398d7c2bf6ff17d4108a0a69b7be91ae76e8ddf77a1054001e1`；
- Note 证据链接全部可解析，展示逐字稿块均存在于 speech-record；speech-record 的
  `raw_text` 与数据库稳定段逐条一致；最后一次 ASR attempt 时间仍为
  `2026-08-02T02:12:06.522890+00:00`。

人工验收结论：该演讲 Note 已从通用发现项列表提升为有背景、论点、场景正文、发言者观点、
限制和证据导航的可用笔记，但仍未达到 generation `19` 会议基线。网络投资和机房管理虽然
进入概述/核心论点，却没有各自形成正文案例；个别已经发生的项目仍可能被写为后续事项。
因此演讲真实质量门保持未通过，不能用合成回归或格式完整代替人工内容验收。

本次验证：

- `276 passed`；
- Ruff 全目录、`compileall`、`git diff --check` 通过。

下一步严格继续跨类型质量门：先实现基于全时间线候选事实的“独立案例覆盖审计/缺失章节修复”，
再实现区分“已经发生、正在进行、明确未来安排”的行动时态门禁；随后用同一演讲样例复验。
演讲通过后再处理访谈、语音备忘/个人笔记和通用真实样例。此前不得进入阶段 E 或前端。

---

## 36. 2026-08-02 演讲独立案例覆盖修复与行动时态门禁

本次在上一提交已推送后继续阶段 C/D，没有进入阶段 E 或前端。实现并验证：

- 对非会议 Note 计算 `scene_sections` 尚未覆盖的高置信事实，并按未覆盖时间段、后半段优先
  选取有限候选；8B 只补确实缺失的章节，不重新调用 14B 主综合；
- 覆盖修复版本为 `2026-08-02.2`，最多补 4 个章节；已有案例的接口、安全、上线和规则等
  实施细节不能再次拆成独立案例；
- 第一次真实修复暴露了“证据编号不同不等于语义未覆盖”的问题，已恢复修复前主文档并调整
  优先级，保留错误修复与旧主文档作为本地可审计证据；
- 第二次修复补回机柜管理案例，并将网络投资相关的数据/项目整合、业务方参与和 AI 图表生成
  带入正文；没有再重复拆分 DBA 的上线、安全和规则细节；
- 演讲待办新增时态门禁：只有引用原文中明确面向未来的安排才保留；“已经、完成了、开始搞、
  上了”等历史描述优先于更早的过程用语；
- 真实样例原有 3 条待办全部属于已发生事项，最终 `actions` 为 0，Note 不再渲染空待办章节；
- 覆盖修复完成后，同版本再次 `document-only` 只执行确定性重校验，耗时接近即时，不再调用模型。

真实样例当前正文包含原主题、运维论点、DBA、资源评价、信息推送、机柜管理，以及网络投资
相关的三个有证据角度。机房遗漏和历史待办问题已经解决；项目所有者已确认网络投资三个角度
无需合并，演讲类型通过人工质量门，当前结构作为演讲代表样例基线保留。

本次验证：

- `278 passed`；
- Ruff、`compileall`、三份产物证据链接与原始文字一致性检查通过；
- 原始 ASR attempt 数量与最后创建时间均未变化；私有 runtime 和产物继续由 Git 忽略。

下一步：按既定顺序进入访谈真实样例，通过后再处理语音备忘/个人笔记和通用真实样例。跨类型
人工质量门全部落实前不进入阶段 E 或前端。

---

## 37. 2026-08-02 访谈真实样例、失败批次恢复与专用质量复核

演讲类型由项目所有者确认通过后，按计划建立独立真实访谈候选。没有新增或上传私有录音；从
已验收演讲素材中确认后半段具备持续双人追问与回答，复制本地持久化状态到 Git 忽略的
`profile-interview-20260802`，人工指定类型为 `interview` 并补充自由文本背景。原演讲任务、
ASR、校订逐字稿、时间对齐和说话人结果均未改变。

首版 generation `11` 未达质量线：正文用“受访者背景、关键问答、核心观点、判断依据、经历
与案例”等类别名重复同一概述；具体问答没有展开；三个已完成系统被误写为后续事项；当场已
回答的问题被列为待追问；口头停顿被误写成分歧。针对这些真实缺口完成：

- prompt 升级为 `2026-08-02.6`，要求按具体问题、回答或案例成章，允许同类多章但禁止类型
  标签式填充；提问者摘要必须写实际追问方向；历史项目、口头停顿和已回答问题分别禁止进入
  actions、risks/tension 和 open_questions；
- 访谈与演讲共用明确未来时态门禁；普通名词“投资计划”不再因包含“计划”误触发待办；
- 长候选批次失败后自动拆半重试；恢复运行只重试失败批次，不重复成功批次。真实 generation
  `12` 的前半批坏 JSON 由此恢复，generation `13` 达到 40 条有证据候选；
- 多人访谈 speaker summary 新增本人发言证据检查与缺失角色补写，避免混合问答段的单一
  speaker 归属使整篇文档失败；私有 raw 可保留失败候选与通用校验原因供增量审计；
- 新增 8B 访谈质量复核版本 `2026-08-02.1`，在 14B 主综合后检查问答、案例、时态、风险、
  未回答问题和证据归属；版本相同的后续复验只做确定性重校验；
- 确定性访谈后处理要求风险证据包含真实安全、限制、困难或成本等约束，清除无证据的未来/探索
  表述；当同一章节引用相距很远的案例证据时，删除只贡献上线、发布、安全扫描等状态的远处
  证据，防止 DBA 上线信息嫁接到机柜管理案例；没有真实问句的章节改为经历与案例。

真实访谈当前 generation `17`、8 个场景章节、2 位匿名参与者观点、0 个 actions、0 个 risks、
1 个录音末尾未讲完的 open question。DBA、资源评价、网络投资桑基图、机柜功率算法、
AIcoding 模型阶段、自然语言图表和消息推送均进入正文；跨案例上线信息和无证据未来表述已清除。

验证结果：

- `281 passed`，Ruff、`compileall`、`git diff --check` 通过；
- Note 的 19 个证据目标全部存在于 transcript，transcript 的 53 个展示块全部存在于
  speech-record，speech-record 的 111 个 raw segment 与数据库逐条一致；
- 原始 ASR 仍为 32 次，最后创建时间未变；私有 runtime 与产物继续由 Git 忽略。

项目所有者已确认 generation `17` 访谈 Note 达到会议/演讲基线。后续不再重跑该任务的 ASR、
逐字稿校订、候选抽取或长上下文综合。下一步严格进入语音备忘/个人笔记真实质量门，通过后再
处理通用类型；跨类型质量门完成前不进入阶段 E 或前端。

---

## 38. 2026-08-02 语音备忘真实样例、完整方法保留与快速文档层修复

按既定顺序完成访谈人工验收后，进入语音备忘/个人笔记质量门。现有私有素材中没有专门录制的
语音备忘，因此诚实选取已验收真实业务录音中一段 165 秒连续单人规划陈述作为代表候选；内容
包含个人判断、企业 AI 落地六步方法、优先级和明确的当日讨论事项。该候选用于验证单人备忘的
内容提炼能力，不冒充专门录制的独立语音备忘。

私有 runtime 为 `services/speech-worker/runtime/profile-voice-memo-20260802/`，job 为
`job_9fccb7ca84d2497486c19b87bb2a6931`。完整 Worker 流程得到 6 个原始 ASR 分块；最终时间线
159.121 秒文字、5.620 秒无语音、0.259 秒听不清、0 秒未处理，`timeline_accounted=true`。
全部 8 个文字分段归属同一匿名说话人。原始 ASR 后续未重跑、未覆盖。

首版 Note 未达质量线：把“记录意图、当前判断、明确任务、待验证假设”等内部类型直接当标题，
六步方法只列名称或复制口语逐字稿，方法步骤被误当待办，单人观点重复生成 participant summary。
针对真实问题完成：

- prompt 升级为 `2026-08-02.7`，要求标题写实际内容，完整有序方法必须集中成章，方法和原则
  不自动等于待办，MVP 验证方法不自动等于假设，单人备忘不生成重复人物观点；
- 覆盖修复版本升级为 `2026-08-02.4`，发现逐字稿明确说“六步”而正文未展开时，按完整
  校订逐字稿恢复有序步骤；每步保留自己的直接证据，纯语气词分段不会成为 Note 链接；
- 新增语音备忘 8B 专用质量复核版本 `2026-08-02.2`，检查具体标题、方法完整性、任务时态、
  假设可验证性、真实约束和跨章节重复；同版本复验只走确定性校验；
- 确定性后处理清除 Meta 意图章节、错误任务/假设/约束和单人 speaker summary；从原文明确
  的未来陈述补出下一步；完整六步方法会替代证据已被其覆盖的碎片章节；
- 现有文档的完整方法会从原始有序表述重建，并利用同一结构化文档中的精炼主题和背景把口语
  步骤改成书面表达，避免重复运行长上下文模型；
- 修复 `processed` 任务结构化 generation 更新后产物仍被误判为 already-generated 的问题；
  artifact checkpoint 现在记录对齐和结构化 generation，新一代文档可自动触发产物刷新；
- 文档综合失败时只从 job ID 和内容类型均匹配的既有 `speech-record.json` 恢复已验证文档，
  不会跨任务或跨类型借用；一次 14B 非法 JSON 失败据此安全恢复，没有破坏已验收产物；
- Note 对语音备忘不再显示 kind 类型前缀；小于 1 秒的不确定区间显示为“合计不足 1 秒”，
  不再错误显示 `00:00:00`。

真实候选当前 checkpoint generation `10`，19 条合并候选、0 条无证据候选、1 个正文意图章、
1 个完整六步方法章、1 条明确下一步、0 条人物观点重复。最终 Note 依次呈现战略对齐、高价值
场景筛选、技术底座、MVP 业务闭环验证、灰度发布与规模化复制、长期运营，并保留成本、安全、
能力适配、数据监控与合规等约束。当前 manifest SHA-256 为
`dd4d7fb7b6ba4d31623e551bb1fdc45a9cf6900c86eccb43f2c74b9f7b1c0f02`。

验证结果：

- `287 passed`，Ruff、`compileall`、`git diff --check` 通过；
- Note 的 6 个唯一证据目标全部存在于 transcript；transcript 有 7 个有效展示块；
- speech-record 的 14 个 `raw_text` 与数据库原始分段逐条一致，原始 ASR 未改变；
- 时间线完整记账、0 个未处理缺口、0 条无证据候选；私有音频、runtime 和产物继续由 Git 忽略。

项目所有者已确认 generation `10` 语音备忘 Note 达到会议/演讲/访谈同等级基线。后续不再
重跑该任务的 ASR、逐字稿校订、候选抽取或长上下文综合。下一步严格进入通用类型真实样例；
跨类型质量门完成前仍不进入阶段 E 或前端。

---

## 39. 2026-08-02 通用真实样例、Meta 去重与未确认事项门禁

语音备忘由项目所有者确认通过后，按既定顺序进入最后一个通用类型质量门。先评估已有 60 秒
真实短录音，但该片段有约 12 秒开头空白、结尾未讲完，且内容明显来自会议讨论，因此拒绝把
它作为验收样例。随后从 `07-28` 真实业务交流中截取 129 秒连续说明片段，内容围绕 AI 需求的
运营侧/业务侧分类、共用能力模块和前期切入方式。它不是独立录制文稿，尾端边界仍有轻微截断，
但缺少完整会议、问答、演讲或个人备忘结构；系统自动分类为 `generic`，与人工覆盖一致。

私有 runtime 为 `services/speech-worker/runtime/profile-generic-real-20260802/`，job 为
`job_9e7e11fec1664519b1b1678b991fe334`。5 个原始 ASR 分块完成后，VAD 判断 2 个缺口无语音、
5 个缺口有语音；定点重转写恢复 2 个短片段，其余 5 个范围仅在重试失败后标为 `inaudible`。
最终时间线 123.440 秒文字、0.620 秒无语音、4.940 秒听不清、0 秒未处理，
`timeline_accounted=true`；9 个文字分段均完成匿名说话人归属。后续文档修复没有重跑或覆盖
原始 ASR。

首版 Note 未达质量线：标题直接复制整句处理目标；正文用“背景、核心信息、结果、后续行动、
开放问题”等 kind 标签凑章节并重复同一概述；单人说明被重复为人物观点；已经制作的方案图
被误写成未来待办；真正的运营侧、业务侧和能力模块没有展开。针对这些问题完成：

- prompt 升级为 `2026-08-02.8`，明确 generic 只用于缺少其他场景结构的材料；标题必须简洁
  具体，kind 不得直接显示为标题，Meta 套话不能成章，独立信息层应展开；
- 单人通用材料清空重复 `speaker_summaries`；Note 渲染不再显示 generic kind 前缀；
- 通用 action 要求原文明示未来承诺、安排或执行要求；已经完成的方案图和仍待选择的切入点
  均不能成为待办或已确认结果；
- 确定性通用后处理压缩 Meta 章节和同义重复，将不确定措辞保留为“拟”，删除无依据的“后续
  将实施”，并把真正未确定的切入点只保留在开放问题；
- 最终正文按实际内容保留“需求分类与能力模块”“需求选择与方案延展”两章：运营侧展开合规、
  审核、培训、财务，业务侧展开新业务开通、金融数据库洞察和业务处理；能力模块保留知识库、
  智能体、小模型、语音语义识别、ASR/TTS；
- 第一次 14B 全文综合返回非法 JSON，系统安全保留 7 条候选；随后仅用 8B 重做文档层恢复，
  没有重新校订逐字稿或抽取候选。相同版本的后续修复只运行确定性校验，耗时约 0.2 秒。

真实候选当前 checkpoint generation `6`、7 条合并候选、0 条无证据候选、2 个具体正文章节、
0 条决定、0 条待办、0 条人物观点重复、1 个开放问题。当前 manifest SHA-256 为
`f295a7d55a41628677deb502feec2083ba7da9055b3a3b683635aece31469014`。

验证结果：

- `289 passed`，Ruff、`compileall`、`git diff --check` 通过；
- Note 的 5 个唯一证据目标全部存在于 transcript；transcript 有 8 个有效展示块；
- speech-record 的 16 个 `raw_text` 与数据库原始分段逐条一致，原始 ASR 未改变；
- 时间线完整记账、0 个未处理缺口、0 条无证据候选；私有音频、runtime 和产物继续由 Git 忽略。

当前通用 Note 已完成自动质量门，等待项目所有者人工审读。只有确认达到会议、演讲、访谈和
语音备忘同等级基线后，跨类型真实质量门才算完成；此前不进入阶段 E 或前端。

---

## 40. 2026-08-02 最终用户侧四份 Markdown 契约

项目所有者确认：当前带大量逐字稿链接的 `note.md` 是质量验收和事实核验稿，内容可以作为
最终笔记基线，但不能作为 Obsidian 中唯一默认阅读版本。阶段 E 发布前必须把用户阅读层和
Worker 证据层拆开。

最终用户侧固定生成四份 Markdown：

1. `note.md`：纯净最终笔记。保留标题、摘要、场景化正文、真实结论/任务/开放问题和
   `我的补充`；不显示证据链接、“依据”、章节导航、speech ID、录音文件等技术信息。
2. `timeline.md`：基于完整校订逐字稿生成的全时间线摘要。严格按录音顺序，以语义转折划分
   时间段；不套会议、访谈、演讲、语音备忘或 generic 模板，只描述每段时间讲了什么。
3. `transcript.md`：完整校订逐字稿，继续保留时间、匿名说话人、稳定块 ID 和不确定标记。
4. `note.evidence.md`：带逐字稿证据链接的核验版 Note，承接当前 `note.md` 的用途；与纯净
   Note 从同一 `speech-record.json` 确定性渲染，不单独调用模型，不承载人工补充。

`timeline.md` 的约束：

- 分段不机械固定为每五分钟，通常约 3–8 分钟，但以一次主题或叙述单元完整结束为准；
- 所有有效逐字稿分段都必须被时间线覆盖，不能只选择最后 Note 使用的重点；
- 同一主题在后段再次出现时仍保留其后段位置，不跨时间合并成一个主题章；
- 每段包含时间范围、具体标题、简述和必要信息点；默认不插证据链接；
- `inaudible` 范围只如实说明，不猜测内容；
- 它可以作为最终 Note 的覆盖检查和辅助输入，但最终 Note 仍必须直接读取完整校订逐字稿，
  防止多层摘要造成信息丢失。

Worker 内部继续保存 `transcript.raw.json`、`speech-record.json` 和
`artifact-manifest.json`，这些默认不进入普通 Obsidian 笔记列表。当前契约已确认并写入计划，
代码实现状态见下一节。严格顺序仍是完成 Stage D 发布前兼容与审计，之后停在阶段 E 边界；
不提前开发前端。

---

## 41. 2026-08-02 四份 Markdown Worker 实现与真实样例验证

项目所有者同意第 40 节契约后，已在 Worker 完成实现，没有进入阶段 E 或前端：

- artifact schema `1.4.0` 固定产出 `note.md`、`timeline.md`、`transcript.md`、
  `note.evidence.md` 四份用户 Markdown，以及 `transcript.raw.json`、`speech-record.json`
  两份机器记录；manifest 对六个文件逐一保存 SHA-256；
- 纯净 `note.md` 不含 YAML 属性、证据链接、“依据”、章节导航和录音技术表，保留场景化内容、
  真实分类项、普通完整度说明和 `我的补充`；审计版从同一结构化文档确定性渲染，不做第二次
  Note 模型调用，也不带 `我的补充`；
- 结构化 schema 升为 `1.6.0`，`timeline_sections` 是所有内容类型共有且与场景模板无关的字段。
  同一次 14B 完整逐字稿综合给出语义起点、标题、摘要和信息点，因此没有新增一轮长模型调用；
- Worker 只接受严格递增的语义起点，并确定性计算每段结束位置，使所有有效校订文字恰好属于
  一个时段。模型采用“前段结束点也是后段起点”或省略中间分段时不会丢文字；听不清范围只
  显示数量提示，不猜内容；
- schema 1.5 及更早的已验收文档可兼容成一个覆盖全篇的时间段；artifact checkpoint 增加
  schema 比较，旧 processed 任务即使结构化 generation 未变也会自动生成新增文件；
- 文档候选只因边界严格校验失败时会保留并在校验规则修复后恢复，不需要再次调用模型；
  generic 后处理同时删除单人/无分歧等 Meta 句，并避免把同一未确定问题重复写成风险。

真实通用样例 `job_9e7e11fec1664519b1b1678b991fe334` 只重做了完整文档综合和确定性产物，
没有重跑 ASR、逐字稿校订或候选抽取。最终时间线分为“AI需求分类概述”“涉及的能力模块”
“初期切入点选择”三个顺序时段，覆盖运营侧/业务侧例子、知识库/智能体/小模型/ASR/TTS 和
切入点保留意见。当前 generation `11`、prompt `2026-08-02.9`、manifest：
`7d52d3677fa8b26ef5704134fcf55e581dacab289f53e195ca483cc4ceae2a0b`。

原始性验证前后完全一致：

- `transcript.raw.json`：`e1aba3f8bdf70982477ce795feb264241bb3a49b366d322dae16a96ffe06c498`；
- `transcript.md`：`8af536edf42b0bf1bb9351a8615f79d71041e6306d0cf305c6610210f14d717f`；
- 16 个 segment 的 `{segment_id, raw_text}` 集合摘要：
  `09031cc1e85284b7cb7928a2b8336020edbd222227581bf65fa8ec7871a50410`。

验证基线为 `291` 项测试全部通过，Ruff、`compileall`、`git diff --check` 通过。私有音频、
runtime、逐字稿和生成文档继续由 Git 忽略。

其余已验收类型随后完成了无模型兼容重建：meeting、speech、interview、voice memo 的新 manifest
分别为 `91f3f51d5917579552bd6331e7a07092ccac26aefb4ee92abf0270395a4d2392`、
`f2687518737a6b4676551d1d25df1321a763a197995425947c39a2b7da01ddd0`、
`87fe52d2b30f10ad2d11d4914fc89e1dab71b57f127b673aef2c372eea47a1c3`、
`5bdfe0ad3469c61bf085d8f9c83c50eb1e07645e38bd65952ddf530a003d6ffd`。四个任务的 manifest
六项哈希、纯净/审计 Note 分工、审计链接目标、timeline 无链接和 speech-record timeline 同源
全部通过。旧任务兼容为一个全范围时段，不为迁移重跑已验收长模型；新任务生成多段语义时段。

Stage D 的本轮实现与发布前兼容审计完成。下一步先审阅当前 diff 并决定提交/推送，随后停在
阶段 E 边界等待项目所有者明确确认；不得提前实现 Vault 发布协议或 Obsidian 前端。

---

## 42. 2026-08-02 `我的补充` 终端人工区域保护

继续 Stage D 后，artifact schema 升级为 `1.5.0`，实现了纯净 Note 的人工区域保护：

- `## 我的补充` 被定义为 `note.md` 的终端用户区域；重生成只替换其前方机器内容，从该标题
  开始到 EOF 的 Markdown 字节内容原样保留，支持任务、wikilink、callout 和用户自己的标题；
- 审计 Note 不复制人工区域，继续保持机器可重复生成；
- processed 幂等门禁会核对六个实际文件哈希。人工编辑导致 Note 哈希改变时，Worker 自动
  读取并保留人工区域、重建机器内容、更新 manifest；下一次无变化运行恢复幂等；
- 既有 Note 缺少人工边界、不是 UTF-8、文件类型异常或超过安全大小时，在写入任何文件前
  失败，优先避免用户内容丢失；
- 新增两项端到端回归和一项文件安全测试，覆盖人工 Markdown 原样保留、机器区刷新、manifest
  重算、审计版隔离、再次幂等、边界缺失拒绝覆盖、符号链接和非法 UTF-8。

真实通用任务完成写入—重生成—删除临时内容—再次重生成验证，最终 manifest 为
`f93e02fb7691b62d229253a564a558d68d0be28d0013210125ac35b490238523`；原始 ASR、校订逐字稿
和 16 个 raw segment 摘要与此前一致。当前基线为 `294` 项测试全部通过，Ruff、`compileall`、
`git diff --check` 通过；私有 runtime 继续被忽略。

下一步仍属 Stage D：实现说话人命名、文字纠正和日期纠正的独立修订记录；随后支持只重生成
总结并生成前后差异比较。完成这些人工修订安全项前，不进入 Stage E 或 Obsidian 前端。

---

## 43. 2026-08-02 独立校订台账、总结重生成与差异记录

Stage D 剩余人工修订安全项已经实现：

- Worker SQLite schema 升级为 `6`，新增 append-only `corrections` 台账；首版支持
  `transcript_text`、`speaker_display_name`、`recording_date`，每条记录含 correction ID、
  job revision、目标、before/after、author、幂等键和创建时间；
- 校订只接受 processed 任务。重复相同幂等请求返回原记录；相同键不同内容、旧 job revision、
  不连续 before 值、未知 segment/speaker、非法日期或空值均安全失败；
- artifact schema 升级为 `1.6.0`。新校订会使旧 checkpoint 自动失效；文字校订进入
  `transcript.md` 与 `speech-record.json.segments[].text`，说话人显示名进入逐字稿标题和参与者
  展示，日期进入机器记录与审计 Note 属性；数据库 segment text、`raw_text` 和原始 ASR 不变；
- 新增 `add-correction`、`list-corrections` 开发者命令。当前六份 artifact 契约不增加第七份
  用户文档，校订历史随 `speech-record.json.corrections` 输出；
- `--document-only` 现在会把文字校订叠加到完整校订逐字稿后再运行全文综合，复用已有 finding
  索引，不调用 ASR、逐字稿润色或候选抽取；
- 每次总结重生成会在私有 `summary_revisions` checkpoint 保存 before/after SHA-256、是否变化、
  统一 JSON diff 和截断标志；`summary-revisions` 命令可读取，供后续界面做确认；
- 端到端测试覆盖文字/说话人/日期三类校订、幂等重试、旧 revision 冲突、自动 artifact 重建、
  再次幂等、总结模型确实读到校订文字、diff 可读，以及原始 ASR/稳定 segment 不变；数据库
  旧版本迁移测试同步覆盖 schema 6。

在真实通用样例的私有临时副本中追加录音日期并正常重生成：`speech-record.json` 从
`1.5.0` 升至 `1.6.0` 且出现日期和校订记录；`transcript.raw.json` 哈希仍为
`e1aba3f8bdf70982477ce795feb264241bb3a49b366d322dae16a96ffe06c498`，`transcript.md` 与
`note.md` 哈希也保持不变；第二次生成返回 `already_generated`。临时副本验证后已删除，仓库中
没有加入任何私有数据。

当前 `297` 项测试全部通过，Ruff、`compileall`、CLI help、`git diff --check` 通过。
Stage D 既定人工修订安全项已经完成；下一步停在 Stage E 边界，先审阅并提交当前改动，未经
项目所有者明确确认不实现 Vault 发布协议，也不开始 Obsidian 前端。

---

## 44. 2026-08-02 Stage E Worker 本地发布协议

项目所有者明确确认进入 Stage E 后，已实现本地 Worker 核心发布契约，没有进入正式 API 或
Obsidian 前端：

- SQLite schema `7` 增加 append-history 发布租约和最终确认；一个任务只能有一个 active
  publisher，支持续租、释放、过期接管、幂等重试和重启恢复；
- 发布状态只能通过专用协议进入，不能借通用状态转换绕过；`processed`、`publishing`、
  `published` 保持不同语义；
- 发布前校验 Worker checkpoint、manifest、六个 artifact 文件哈希和身份；输出只使用规范化
  Vault 相对路径，拒绝遍历、设备路径、符号链接逃逸和挂载变化；
- 四份 Markdown、两份机器记录与 manifest 先写入隐藏同级临时目录，全部 fsync 和复核后原子
  重命名，再做发布后复核；失败始终保留 Worker 产物；
- 既有目录只有七个文件全部精确匹配才作为可恢复重试；用户修改、额外文件和同步差异均保留并
  报告冲突，不执行覆盖；
- 自动化覆盖双连接并发、租约过期接管、重启恢复、rename 后 ack 前中断、用户冲突、路径逃逸、
  损坏 Worker artifact 和 CLI 生命周期。共 `322` 项测试通过，Ruff、`compileall`、CLI help、
  `git diff --check` 通过；
- 真实会议样例仅在私有临时副本发布：首次 `published`、重试 `already_published`，七文件与
  Worker package 逐字节一致；manifest 为
  `91f3f51d5917579552bd6331e7a07092ccac26aefb4ee92abf0270395a4d2392`，三处
  `transcript.raw.json` 哈希均保持
  `45bf58083c586998b690faf8bc9bfb705d3b260b5b2c971aa25ce5c73f2575ea`。临时数据验证后删除。

Stage E 已在 `9872f20 Implement atomic Vault publication` 提交并推送到
`origin/agent/worker-core`，私有 runtime、reference、音频和生成数据未进入 Git。项目所有者
随后明确确认继续，下一步严格进入阶段 F：先定义协议版本与能力协商，再建立 OpenAPI；不提前
进入阶段 G 或阶段 H 前端。
